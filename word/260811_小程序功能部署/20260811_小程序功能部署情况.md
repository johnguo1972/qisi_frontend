# 2026-08-11 小程序功能前后端部署情况

## 一、部署结论

本次本地前后端代码已部署到生产服务器，服务重启成功，线上基础检查通过。

- 后端服务：`qisi-gunicorn`，状态 `active`；
- Nginx 容器：`qisi-nginx`，状态 `running`；
- H5 前端：已同步到 Nginx 当前使用目录；
- 数据库迁移：无待执行迁移；
- Nginx 配置：本次没有修改配置文件，仅执行配置检查并重启容器；
- 小程序 MP-WEIXIN 构建代码不直接部署到服务器，需要继续通过微信开发者工具上传体验版/正式版。

## 二、部署时间和服务器路径

- 部署日期：2026-08-11；
- 服务器：`42.194.195.78`；
- 后端目录：`/mnt/datadisk0/qisi/backend`；
- H5 当前目录：`/mnt/datadisk0/qisi/frontend-h5`；
- H5 临时部署目录：`/mnt/datadisk0/qisi/frontend-h5.deploy_20260811_170306`；
- Nginx 容器：`qisi-nginx`；
- Gunicorn 服务：`qisi-gunicorn`。

## 三、后端部署文件

本次只备份和覆盖了以下 9 个后端业务文件，没有备份或覆盖服务器整个后端目录：

| 文件 | 主要修改内容 |
|---|---|
| `apps/qrcode/views.py` | 增加家长当前孩子上下文对应的学生关系校验 |
| `apps/study/answer_views.py` | 增加草稿答题创建、提交接口及家长上下文权限支持 |
| `apps/study/guidance_views.py` | 引导式做题接口支持学生和有效家长孩子上下文 |
| `apps/study/knowledge_views.py` | 知识掌握接口支持学生和有效家长孩子上下文 |
| `apps/study/permissions.py` | 增加有效学生身份和家长当前孩子上下文权限类 |
| `apps/study/student_urls.py` | 注册 `/student/attempts/start` 和草稿提交路由 |
| `apps/study/student_views.py` | 学生首页、任务、关卡、PDF 导出支持家长上下文 |
| `apps/wrongbook/variant_views.py` | 同类题练习支持家长上下文权限 |
| `apps/wrongbook/views.py` | 错题本列表、详情和变式接口支持家长上下文 |

### 未部署的本地文件

- 本地 `.env` 未上传，服务器原有环境变量和 AppSecret 未被覆盖；
- `tests/integration/test_student_attempt_flow.py` 仅为测试文件，未部署到生产业务目录；
- 数据库迁移文件没有新增或修改；
- Nginx、systemd、Docker 配置文件没有修改。

## 四、数据库迁移检查

部署前在本地执行：

```text
python manage.py makemigrations --check --dry-run
No changes detected
```

部署后在服务器执行：

```text
python manage.py check
System check identified no issues (0 silenced).

python manage.py migrate --plan
No planned migration operations.
```

结论：本次没有数据库表结构变更，不需要执行 `python manage.py migrate`。

## 五、Nginx 配置检查

服务器 Nginx 运行在 Docker 容器 `qisi-nginx` 中，配置文件挂载位置为：

```text
/opt/qisi/nginx.conf
```

本次没有修改该配置文件。配置检查结果：

```text
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

现有配置告警如下，属于服务器原有配置告警，不影响本次部署：

- `listen ... http2` directive deprecated；
- SSL 证书没有 OCSP responder URL，因此 `ssl_stapling` 被忽略。

## 六、备份情况

### 1. 后端变动文件备份

后端变动文件已按原目录结构备份到：

```text
/mnt/datadisk0/qisi/backups/deploy_20260811_170128/backend-files
```

该目录只包含本次将覆盖的 9 个后端文件，可用于单文件回滚。

### 2. H5 前端回滚副本

为保证 Nginx 固定挂载目录可安全同步，部署前保留了当前 H5 前端副本：

```text
/mnt/datadisk0/qisi/frontend-h5.prev_20260811_170306
```

该副本不是后端代码备份，专门用于 H5 前端回滚。

## 七、前端部署内容

本地先执行并确认通过：

```text
npm run build:h5
```

部署内容为：

```text
uniapp/dist/build/h5/
```

该构建产物已同步到：

```text
/mnt/datadisk0/qisi/frontend-h5/
```

本次 H5 前端包含此前小程序功能相关的共享代码更新，例如答题接口、图片上传、PDF 导出地址、登录态处理和共享 API 调整；H5 构建已通过，未带入小程序专用页面模板和小程序专用样式。

隐私协议目录不属于本次 H5 构建产物，因此同步后已从前端回滚副本恢复到：

```text
/mnt/datadisk0/qisi/frontend-h5/privacy/
```

## 八、服务重启情况

### Gunicorn

```text
sudo systemctl restart qisi-gunicorn
状态：active (running)
```

### Nginx

```text
docker exec qisi-nginx nginx -t
结果：syntax is ok / test is successful

docker restart qisi-nginx
状态：running
```

## 九、线上验证结果

| 检查项 | 结果 |
|---|---|
| `https://qisi.chengxuelu.com/health` | HTTP 200，返回 `OK` |
| H5 首页 `/` | HTTP 200 |
| H5 首页字节校验 | 与本地 `dist/build/h5/index.html` 一致 |
| `https://qisi.chengxuelu.com/privacy` | HTTP 200 |
| `/api/v1/student/attempts/start` 未认证访问 | HTTP 401，说明路由存在且鉴权生效 |
| `/api/v1/student/export/pdf` 未认证访问 | HTTP 401，说明路由存在且鉴权生效 |
| Django system check | 通过 |
| 数据库迁移计划 | 无待执行迁移 |

Gunicorn 重启后没有发现新的启动异常。线上日志中的未认证请求 401 是本次路由探测的预期结果；历史日志中的非法 HTTP 请求属于公网扫描流量，不是本次部署产生的应用错误。

## 十、回滚方法

### 后端单文件回滚

仅回滚某个后端文件时，将备份目录对应文件复制回后端原路径，然后重启服务：

```bash
sudo cp /mnt/datadisk0/qisi/backups/deploy_20260811_170128/backend-files/apps/study/answer_views.py \
  /mnt/datadisk0/qisi/backend/apps/study/answer_views.py
sudo systemctl restart qisi-gunicorn
```

其他文件按相同目录结构处理。

### H5 前端回滚

```bash
sudo rsync -a --delete \
  /mnt/datadisk0/qisi/frontend-h5.prev_20260811_170306/ \
  /mnt/datadisk0/qisi/frontend-h5/
docker restart qisi-nginx
```

## 十一、后续建议

- 使用真实登录凭证验证学生和家长业务，不要仅依据未认证 401 探测判断业务完整性；
- 重点回归答题图片上传、引导式做题、错题图片、PDF 导出和家长代提交；
- 小程序代码仍需在微信开发者工具中使用最新 `dist/build/mp-weixin` 上传体验版；
- 观察发布后 Gunicorn 和 Nginx 日志，确认没有新增 5xx 错误。
