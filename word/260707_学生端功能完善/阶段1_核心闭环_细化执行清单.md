# 阶段 1 · 核心闭环（F3 → F1 → F4）细化执行清单

> 配套：`学生端功能完善实施方案.md`（总纲）、`00_协作速查-文件归属与冲突清单.md`。
> 本文档**只产出执行步骤，不改代码**；按步骤顺序执行即可，每步可独立验证、可回滚。
> 严格遵循：代码独立、改动最小、风格与现网一致、无假数据、不删除/破坏既有代码、每文件归属标注。

---

## 0. 前置与约定

> **执行与提交纪律**：直接在 `main` 分支开发，不切分支；执行者一律不 `git add` / `git commit` / `git push`，改动保留为工作区未提交状态，**由用户审阅后手动提交**；`main` 现有未提交改动保持不动、照常叠加 F 改动。

### 0.1 环境就绪（执行前确认）
- 后端运行：`venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001`（改完后重启）。
- 数据库：Docker `qisi-postgres` 已起、迁移可执行。
- 前端运行：`cd uniapp && npm run dev:h5`（http://localhost:5273）。
- 命令前缀：后端命令统一用 `venv/Scripts/python.exe manage.py ...`（Git Bash）。

### 0.2 测试数据准备（用于验证，可复用）
A. 取一个**学生 token**（绕过短信，仅测试用）——开 `venv/Scripts/python.exe manage.py shell`，执行：
```python
from apps.accounts.models import UserAccount
from rest_framework_simplejwt.tokens import RefreshToken
u, _ = UserAccount.objects.get_or_create(
    mobile='13800000001', defaults={'role_type':'student','display_name':'测试学生'})
print('USER_ID', u.id)
print('TOKEN', str(RefreshToken.for_user(u).access_token))
```
复制打印的 `TOKEN`，后续 curl 用 `Authorization: Bearer <TOKEN>`。

B. 取一个**真实题目 id**（用于 F3/F4 作答验证）：
```python
from apps.parser.models import ExamQuestion
# 找一道客观题（有 answer 字段）
obj = ExamQuestion.objects.filter(question_type__in=['single_choice','multiple_choice']).first()
# 找一道主观题
sub = ExamQuestion.objects.exclude(question_type__in=['single_choice','multiple_choice']).first()
print('OBJ_QID', obj.id if obj else None, obj.question_type if obj else None)
print('SUB_QID', sub.id if sub else None, sub.question_type if sub else None)
```
记下 `OBJ_QID`、`SUB_QID`。如无主观题，临时改一道题类型测试：`ExamQuestion.objects.filter(pk=OBJ_QID).update(question_type='short_answer')`（测完记得改回）。

### 0.3 本阶段完成定义（DoD）
- ✅ 客观题答错才进错题本；主观题提交显示"待批阅"、**不进错题本**、**不计入正确率分母**。
- ✅ 学生在任务发布后加入班级，首页能看到任务（无需教师重新发布）。
- ✅ 错题本 → 同类题 → 进入独立做题页 → 提交 → 判分 → 计数/掌握度更新，整条闭环可走通，且数据全部来自真实接口。
- ✅ 既有客观题作答、错题入库、关卡进度、成长统计行为不回归。
- ✅ 全程未改教师端业务代码、未删既有函数。

---

## F3. 主观题判分三态（对 / 错 / 待批阅）

**问题**：`apps/study/answer_views.py:29` `_check_answer` 对非客观题 `return False` → 主观题一律判错、进错题本、0 分。
**目标**：客观题自动判分不变；主观题标记 `is_subjective_pending=True`，不进错题本、不计正确率、反馈"已提交，待批阅"。
**涉及文件**（学生端专有）：
- 改 `apps/study/models.py`（+1 字段）
- 新建 `apps/study/migrations/0003_answerattempt_is_subjective_pending.py`
- 改 `apps/study/answer_views.py`（替换 2 个函数）
- 改 `apps/study/student_views.py`（growth 排除待批阅）
- 改 `uniapp/src/pages/student/answer.vue`（反馈区）

### 步骤 1.1 　models.py 新增字段
文件：`apps/study/models.py`，在 `AnswerAttempt` 的 `is_correct` 字段**下方**插入一行：
```python
    is_correct = models.BooleanField(default=False)
    is_subjective_pending = models.BooleanField(default=False)   # ← 新增：主观题待批阅
    score = models.DecimalField(max_digits=5, decimal_places=2, default=0.00)
```
其余字段、类不变。

### 步骤 1.2 　生成迁移
推荐自动生成（最稳）：
```bash
venv/Scripts/python.exe manage.py makemigrations study
```
预期输出：`apps/study/migrations/0003_answerattempt_is_subjective_pending.py`，内容仅 `AddField`。

若想手写，新建 `apps/study/migrations/0003_answerattempt_is_subjective_pending.py`：
```python
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [('study', '0002_favorite')]

    operations = [
        migrations.AddField(
            model_name='answerattempt',
            name='is_subjective_pending',
            field=models.BooleanField(default=False),
        ),
    ]
```

### 步骤 1.3 　应用迁移并验证
```bash
venv/Scripts/python.exe manage.py migrate study
```
验证字段已存在：
```bash
venv/Scripts/python.exe manage.py shell -c "from apps.study.models import AnswerAttempt; f=AnswerAttempt._meta.get_field('is_subjective_pending'); print(f.name, f.default)"
```
预期输出：`is_subjective_pending False`。

### 步骤 1.4 　answer_views.py 替换两个函数
文件：`apps/study/answer_views.py`。**整段替换** `_check_answer` 与 `_handle_submit_answer` 两个函数（现有第 15–88 行），其余 import / `submit_answer` / `retry_answer` / `get_mode_a` 不动：
```python
SUBJECTIVE_TYPES = ('fill_blank', 'short_answer', 'essay', 'true_false', 'computation', 'proof')


def _check_answer(question, answer_content: dict):
    """客观题返回 True/False；主观题返回 None（待批阅）。"""
    if question.question_type in ('single_choice', 'multiple_choice'):
        selected = set(answer_content.get('selected_options', []))
        correct_str = getattr(question, 'answer', '') or ''
        if correct_str:
            correct = set(correct_str.replace(' ', '').upper())
        else:
            # Fallback: try to get from ai_answer_a
            ai_a = question.ai_answer_a or {}
            correct = set(str(ai_a.get('answer', '')).replace(' ', '').upper())
        return selected == correct
    # 主观题无法自动判分 → None
    return None


def _handle_submit_answer(request, question_id, answer_content, mission_id, level_id, source):
    """Core answer submission logic (shared by submit_answer and retry_answer)."""
    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    result = _check_answer(q, answer_content)
    is_pending = result is None                       # 主观题
    is_correct = False if is_pending else result

    attempt_no = AnswerAttempt.objects.filter(
        student_user_id=request.user, question_id=question_id
    ).count() + 1

    attempt = AnswerAttempt.objects.create(
        student_user_id=request.user,
        mission_id=mission_id, level_id=level_id,
        question_id=question_id, attempt_no=attempt_no,
        answer_content=answer_content, is_correct=is_correct,
        is_subjective_pending=is_pending,
        score=100.00 if is_correct else 0.00,
        submit_source=source,
    )

    if level_id:
        lp = StudentLevelProgress.objects.filter(
            level_id=level_id, student_user_id=request.user
        ).first()
        if lp:
            lp.attempt_count += 1
            if is_correct:
                lp.pass_score = max(lp.pass_score, 100.00)
            lp.save()

    # 仅"客观题答错"才进错题本；主观题待批阅不进
    if (not is_correct) and (not is_pending):
        WrongBookItem.objects.get_or_create(
            student_user_id=request.user, question_id=question_id,
            defaults={'status': 'not_reviewed'}
        )

    if is_pending:
        feedback = '主观题已提交，等待老师批阅'
    else:
        feedback = generate_feedback(is_correct, q, attempt_no)

    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'is_correct': is_correct,
            'is_pending': is_pending,
            'score': float(attempt.score),
            'feedback': feedback,
            'attempt_id': attempt.id,
            'suggest_guidance': (not is_correct) and (not is_pending),
        }, 'trace_id': make_trace_id(),
    })
```

### 步骤 1.5 　student_views.py growth_summary 排除待批阅
文件：`apps/study/student_views.py`，替换 `growth_summary` 函数体（现有第 147–176 行）：
```python
@api_view(['GET'])
@permission_classes([IsAuthenticated])
def growth_summary(request):
    """S-12: Growth summary."""
    from apps.wrongbook.models import WrongBookItem, MasteryRecord

    base = AnswerAttempt.objects.filter(student_user_id=request.user)
    total_attempts = base.exclude(is_subjective_pending=True).count()   # 排除待批阅
    total_correct = base.filter(is_correct=True).count()
    mastered_count = MasteryRecord.objects.filter(
        student_user_id=request.user, mastery_status='mastered'
    ).count()
    wrong_count = WrongBookItem.objects.filter(
        student_user_id=request.user
    ).count()

    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'total_attempts': total_attempts,
            'total_correct': total_correct,
            'accuracy': round(total_correct / max(total_attempts, 1) * 100, 1),
            'mastered_count': mastered_count,
            'wrong_book_count': wrong_count,
        },
        'trace_id': make_trace_id(),
    })
```

### 步骤 1.6 　answer.vue 反馈区支持"待批阅"
文件：`uniapp/src/pages/student/answer.vue`。

(a) 模板：把反馈卡片的图标/标题改为三态（替换现有 `feedback-card` 内的 `feedback-header` 区块）：
```vue
      <view v-if="feedback" class="feedback-card" :class="feedbackType">
        <view class="feedback-header">
          <text class="feedback-icon">{{ feedbackType === 'correct' ? '✅' : (feedbackType === 'pending' ? '⏳' : '❌') }}</text>
          <text class="feedback-title">{{ feedbackType === 'correct' ? '回答正确' : (feedbackType === 'pending' ? '已提交，待批阅' : '回答错误') }}</text>
        </view>
        <text class="feedback-text">{{ feedback }}</text>
        <view class="feedback-actions">
          <button v-if="suggestGuidance" @click="startGuidance" class="btn-guidance">进入引导模式</button>
          <button v-else @click="nextQuestion" class="btn-next">{{ hasNext ? '下一题' : '完成' }}</button>
        </view>
      </view>
```
(b) 脚本：`submitAnswer` 内设置 `feedbackType`（替换原两行）：
```ts
    feedback.value = res.data?.feedback || ''
    feedbackType.value = res.data?.is_pending ? 'pending'
      : (res.data?.is_correct ? 'correct' : 'incorrect')
    suggestGuidance.value = res.data?.suggest_guidance || false
```
(c) 样式：在 `<style scoped>` 内 `.feedback-card.incorrect {...}` 之后追加：
```css
.feedback-card.pending {
  background: #eef6ff;
}
```

### 步骤 1.7 　重启后端
停掉旧 runserver（Ctrl+C），重新：
```bash
venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001
```

### 步骤 1.8 　验证（真实接口，无假数据）
用 0.2 拿到的 TOKEN、OBJ_QID、SUB_QID。

(a) 客观题答错 → 进错题本：
```bash
curl -X POST http://localhost:8001/api/v1/student/attempts \
  -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" \
  -d "{\"question_id\":<OBJ_QID>,\"answer_content\":{\"selected_options\":[\"Z\"]},\"level_id\":null}"
```
期望 JSON：`"is_correct": false, "is_pending": false, "suggest_guidance": true`。Shell 复核错题本已生成：
```bash
venv/Scripts/python.exe manage.py shell -c "from apps.wrongbook.models import WrongBookItem; print(WrongBookItem.objects.filter(question_id=<OBJ_QID>).count())"
```
预期：`1`。

(b) 主观题提交 → 待批阅、不进错题本：
```bash
curl -X POST http://localhost:8001/api/v1/student/attempts \
  -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" \
  -d "{\"question_id\":<SUB_QID>,\"answer_content\":{\"text\":\"我的解答...\"},\"level_id\":null}"
```
期望 JSON：`"is_correct": false, "is_pending": true, "feedback": "主观题已提交，等待老师批阅", "suggest_guidance": false`。复核错题本未新增：
```bash
venv/Scripts/python.exe manage.py shell -c "from apps.wrongbook.models import WrongBookItem; print(WrongBookItem.objects.filter(question_id=<SUB_QID>).count())"
```
预期：`0`。

(c) 成长正确率分母不含主观题：`GET /api/v1/student/growth`（带 token）→ `total_attempts` 只统计客观题作答次数。

(d) 前端：浏览器做题页答一道主观题 → 反馈区显示 ⏳"已提交，待批阅"，无"进入引导模式"按钮。

### 步骤 1.9 　回归检查
- 客观题答对：`is_correct=true`、`score=100`、关卡 `pass_score` 更新、不进错题本（行为同改前）。
- 错题自动入库逻辑（客观错）仍生效。
- `retry_answer` 复用 `_handle_submit_answer`，主观题重做同样标 pending。

### 步骤 1.10 　回滚
- 还原 `answer_views.py`（`_check_answer` 改回 `return False`、`_handle_submit_answer` 还原、删 `is_subjective_pending` 入参）。
- 还原 `student_views.py` `growth_summary`、`answer.vue` 三处。
- 反向迁移：新建 `0004_revert_pending.py`（`RemoveField('is_subjective_pending')`）→ `migrate`；或直接 `migrate study 0002_favorite` 回退到 0002（会删字段）。

### F3 · DoD
☑ 主观题 `is_pending=true`、不进错题本、不计正确率  ☑ 前端显示"待批阅"  ☑ 客观题行为无回归  ☑ 迁移可正向/反向

---

## F1. 后入班学生任务可见性（信号回填）

**问题**：`apps/institutions/student_views.py:50-68` `join_by_code` 加入班级后不回填 `StudentMissionProgress` → 在任务发布**之后**才入班的学生首页永远空。
**目标**：学生加入班级时，自动为该班级所有已发布任务补建 `StudentMissionProgress`。**不改 join_by_code**（避免动共享/教师端代码），用学生端信号。
**涉及文件**（学生端专有）：
- 新建 `apps/study/receivers.py`
- 改 `apps/study/apps.py`（+`ready()`）

### 步骤 2.1 　新建 receivers.py
文件：`apps/study/receivers.py`（新文件，完整内容）：
```python
"""学生端信号接收器：学生加入班级后回填任务进度。"""
from django.db.models.signals import post_save
from django.dispatch import receiver
from apps.institutions.models import ClassStudent
from apps.missions.models import LearningMission
from .models import StudentMissionProgress


@receiver(post_save, sender=ClassStudent)
def backfill_mission_progress_on_join(sender, instance, created, **kwargs):
    """新增活跃班级成员时，为该班级所有已发布任务补建 StudentMissionProgress。"""
    if not created or instance.status != 'active':
        return
    missions = LearningMission.objects.filter(
        class_obj_id=instance.class_obj_id, status='published'
    )
    for mission in missions:
        StudentMissionProgress.objects.get_or_create(
            mission=mission,
            student_user_id_id=instance.student_id,
            defaults={'progress_status': 'not_started', 'progress_percent': 0},
        )
```

### 步骤 2.2 　apps.py 注册信号
文件：`apps/study/apps.py`，当前内容（8 行，无 `ready()`）。新增 `ready()` 方法：
```python
from django.apps import AppConfig


class StudyConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.study'
    verbose_name = '学习作答'

    def ready(self):
        # 注册学生端信号接收器（导入即注册）
        from . import receivers  # noqa: F401
```

### 步骤 2.3 　重启后端
信号在 `ready()` 注册，需重启进程（步骤 1.7 同款命令）。

### 步骤 2.4 　验证（真实数据）
前置：需有「一个已发布且关联班级的任务」与「一个尚未入班的真实学生 USER_ID」。Shell 内造一次入班动作触发信号：
```bash
venv/Scripts/python.exe manage.py shell
```
```python
from apps.institutions.models import ClassStudent, Class
from apps.accounts.models import UserAccount
from apps.missions.models import LearningMission
from apps.study.models import StudentMissionProgress

cls = Class.objects.first()                      # 任取一个班
stu = UserAccount.objects.filter(role_type='student').exclude(student_classes__class_obj=cls).first()
print('CLASS', cls.id, 'STU', stu.id if stu else None)

# 清掉旧进度（便于观察）
StudentMissionProgress.objects.filter(mission__class_obj=cls, student_user_id=stu).delete()

# 触发信号：新建一条活跃成员记录（模拟 join_by_code 的最终落库）
cs = ClassStudent.objects.create(class_obj=cls, student=stu, join_type='manual', status='active')

n = StudentMissionProgress.objects.filter(mission__class_obj=cls, student_user_id=stu).count()
print('BACKFILLED', n)
```
预期：`BACKFILLED` = 该班级已发布任务数（≥1 即成功）。再删测试记录：
```python
cs.delete()
```

HTTP 层复核（端到端）：用该学生 token 调 `GET /api/v1/student/home` → 能看到该班级任务。

### 步骤 2.5 　回归检查
- `mission_publish`（教师发布时 fan-out）行为不变（信号只在"新建 ClassStudent"时触发，不重复、不冲突——`get_or_create` 幂等）。
- `quit_class`（删 ClassStudent + 清进度）行为不变；退班再加入仍会触发回填。

### 步骤 2.6 　回滚
- 还原 `apps/study/apps.py`（删 `ready()`）→ 重启 → 信号不再注册。
- 删 `apps/study/receivers.py`。

### F1 · DoD
☑ 后入班学生首页能看到已发布任务  ☑ 退班再加入仍生效  ☑ 教师端发布逻辑无回归  ☑ 未改 `institutions` 任何文件

---

## F4. 变式题练习闭环 ⭐

**问题**：
- `apps/wrongbook/services.py:5-28` `find_variant_questions` 只返回极简字段（无题干/选项）→ 无法答题。
- `apps/wrongbook/urls.py` 无提交接口；`WrongBookItem.variant_done_count` 从未被写。
- `wrongbook-variants.vue:128` `startPractice` 跳到 guidance 占位页（跳错）。

**目标**：错题本 → 同类题（完整题目）→ 独立做题页 → 提交判分 → 更新 `variant_done_count`/掌握度，闭环全真实数据。
**涉及文件**：
- 改 `apps/wrongbook/services.py`（返回完整字段）
- 新建 `apps/wrongbook/variant_views.py`（提交接口）
- 改 `apps/wrongbook/urls.py`（+1 路由）
- 改 `uniapp/src/api/student.ts`（+1 方法，共享·追加）
- 新建 `uniapp/src/pages/student/variant-practice.vue`
- 改 `uniapp/src/pages/student/wrongbook-variants.vue`（改跳转）
- 改 `uniapp/src/pages.json`（+1 页，共享·追加）

> 依赖：F3 的 `AnswerAttempt.is_subjective_pending` 已存在（本阶段已先做 F3）。若尚未合入 F3，`variant_views` 里去掉 `is_subjective_pending=is_pending` 一行即可。

### —— 后端 ——

### 步骤 3.1 　services.py 返回完整字段
文件：`apps/wrongbook/services.py`，**整文件替换**为：
```python
"""Wrong book services: variant question recommendation."""
from apps.parser.models import ExamQuestion


def _question_brief(q):
    """组装一道题的完整展示数据（含选项/图片）。"""
    return {
        'id': q.id,
        'question_no': q.question_no,
        'question_type': q.question_type,
        'subject': q.subject,
        'difficulty': float(q.difficulty) if q.difficulty else None,
        'stem': q.stem,
        'stem_html': getattr(q, 'stem_html', None),
        'images': [{'url': img.file_path} for img in q.images.all().order_by('sort_order')],
        'options': [{'label': o.option_label, 'content': o.content}
                    for o in q.options.all().order_by('sort_order')],
    }


def find_variant_questions(question_id: int, limit: int = 3) -> list:
    """同学科 + 同难度（可空）+ 排除原题，返回完整题目数据。"""
    try:
        original = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return []

    qs = ExamQuestion.objects.filter(subject=original.subject).exclude(pk=question_id)
    if original.difficulty is not None:
        qs = qs.filter(difficulty=original.difficulty)
    qs = qs.prefetch_related('images', 'options')[:limit]
    return [_question_brief(q) for q in qs]
```

### 步骤 3.2 　新建 variant_views.py
文件：`apps/wrongbook/variant_views.py`（新文件，完整内容）：
```python
"""变式题练习：提交答案、判分、更新错题计数与掌握度。"""
import uuid
from django.db import transaction
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from apps.study.models import AnswerAttempt
from .models import WrongBookItem, MasteryRecord


def make_trace_id():
    return uuid.uuid4().hex[:16]


OBJECTIVE_TYPES = ('single_choice', 'multiple_choice')


def _grade(question, answer_content):
    """客观题 True/False；主观题 None（待批阅）。"""
    if question.question_type in OBJECTIVE_TYPES:
        selected = set(answer_content.get('selected_options', []))
        correct_str = getattr(question, 'answer', '') or ''
        if correct_str:
            correct = set(correct_str.replace(' ', '').upper())
        else:
            ai_a = question.ai_answer_a or {}
            correct = set(str(ai_a.get('answer', '')).replace(' ', '').upper())
        return selected == correct
    return None


def _update_mastery(student, question_id: int, is_correct: bool):
    """按题目维度更新掌握度：答对 +50（满 100 mastered），答错 -20。"""
    mr, _ = MasteryRecord.objects.get_or_create(
        student_user_id=student, mastery_type='question', target_code=str(question_id),
        defaults={'mastery_status': 'not_mastered', 'mastery_score': 0},
    )
    if is_correct:
        mr.mastery_score = min(100, float(mr.mastery_score) + 50)
        if mr.mastery_score >= 100:
            mr.mastery_status = 'mastered'
    else:
        mr.mastery_score = max(0, float(mr.mastery_score) - 20)
        mr.mastery_status = 'reviewing'
    mr.save()


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def variant_submit(request, item_id):
    """POST /api/v1/student/wrong-book/<item_id>/variant-submit
    Body: { question_id, answer_content }
    """
    trace_id = make_trace_id()
    question_id = request.data.get('question_id')
    answer_content = request.data.get('answer_content', {})

    try:
        item = WrongBookItem.objects.get(pk=item_id, student_user_id=request.user)
    except WrongBookItem.DoesNotExist:
        return Response({'code': 404, 'message': '错题不存在', 'data': None, 'trace_id': trace_id}, status=404)

    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': trace_id}, status=404)

    result = _grade(q, answer_content)
    is_pending = result is None
    is_correct = False if is_pending else result

    with transaction.atomic():
        attempt = AnswerAttempt.objects.create(
            student_user_id=request.user,
            mission_id=None, level_id=None,
            question_id=question_id,
            attempt_no=AnswerAttempt.objects.filter(
                student_user_id=request.user, question_id=question_id).count() + 1,
            answer_content=answer_content,
            is_correct=is_correct,
            is_subjective_pending=is_pending,
            score=100.00 if is_correct else 0.00,
            submit_source='variant',
        )
        # 错题状态推进：not_reviewed → reviewing(首次练) → mastered(累计练对≥3)
        if item.status == 'not_reviewed':
            item.status = 'reviewing'
        if is_correct and not is_pending:
            item.variant_done_count = (item.variant_done_count or 0) + 1
            if item.variant_done_count >= 3:
                item.status = 'mastered'
        item.save(update_fields=['variant_done_count', 'status'])
        _update_mastery(request.user, question_id, is_correct and not is_pending)

    if is_pending:
        feedback = '主观题已提交，等待老师批阅'
    elif is_correct:
        feedback = '做对了！该错题掌握度已提升。'
    else:
        feedback = '再想想，注意与原题的相同考点。'

    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'is_correct': is_correct,
            'is_pending': is_pending,
            'score': float(attempt.score),
            'feedback': feedback,
            'variant_done_count': item.variant_done_count,
            'item_status': item.status,
        }, 'trace_id': trace_id,
    })
```

### 步骤 3.3 　urls.py 增加路由
文件：`apps/wrongbook/urls.py`，**整文件替换**为：
```python
from django.urls import path
from . import views
from . import variant_views

app_name = 'wrongbook'
urlpatterns = [
    path('', views.wrongbook_list, name='wrongbook-list'),
    path('<int:item_id>', views.wrongbook_detail, name='wrongbook-detail'),
    path('<int:item_id>/variants', views.wrongbook_variants, name='wrongbook-variants'),
    path('<int:item_id>/variant-submit', variant_views.variant_submit, name='variant-submit'),  # 新增
    path('mastery', views.mastery_list, name='mastery-list'),
]
```

### 步骤 3.4 　重启后端
```bash
venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001
```

### 步骤 3.5 　后端验证（真实接口）
用 0.2 的 TOKEN，并取一个错题 item_id（先造一条错题）：
```bash
venv/Scripts/python.exe manage.py shell -c "from apps.wrongbook.models import WrongBookItem; it=WrongBookItem.objects.first(); print('ITEM', it.id if it else None, 'QID', it.question_id if it else None)"
```
(a) 同类题列表 → 应返回完整题干/选项：
```bash
curl -H "Authorization: Bearer <TOKEN>" http://localhost:8001/api/v1/student/wrong-book/<ITEM_ID>/variants
```
期望 `data` 数组中每项含 `stem`、`options`、`images`、`question_type`（不再只有 id/type）。

(b) 提交变式题答案（客观题答对）：
```bash
curl -X POST http://localhost:8001/api/v1/student/wrong-book/<ITEM_ID>/variant-submit \
  -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" \
  -d "{\"question_id\":<VARIANT_QID>,\"answer_content\":{\"selected_options\":[\"<正确选项>\"]}}"
```
期望 JSON：`"is_correct": true, "variant_done_count": 1`。Shell 复核计数：
```bash
venv/Scripts/python.exe manage.py shell -c "from apps.wrongbook.models import WrongBookItem; print(WrongBookItem.objects.get(pk=<ITEM_ID>).variant_done_count)"
```
预期：`1`，且 `mastery_record` 新增一条（mastery_type=question）。

### —— 前端 ——

### 步骤 3.6 　api/student.ts 追加方法
文件：`uniapp/src/api/student.ts`（共享·追加），在 `wrongbookApi` 对象内追加 `variantSubmit`：
```ts
export const wrongbookApi = {
  list: () => get('/student/wrong-book'),
  detail: (id: number) => get(`/student/wrong-book/${id}`),
  variants: (id: number) => get(`/student/wrong-book/${id}/variants`),
  variantSubmit: (itemId: number, data: { question_id: number; answer_content: object }) =>
    post(`/student/wrong-book/${itemId}/variant-submit`, data),   // ← 新增
}
```

### 步骤 3.7 　新建 variant-practice.vue
文件：`uniapp/src/pages/student/variant-practice.vue`（新文件，代码独立，不 import answer.vue）：
```vue
<template>
  <view class="answer-page">
    <view class="question-panel">
      <view class="question-header">
        <text class="q-no">同类题 {{ currentIndex + 1 }}/{{ questions.length }}</text>
        <text class="q-type">{{ typeLabel(currentQuestion.question_type) }}</text>
      </view>

      <view v-if="currentQuestion.stem" class="stem">{{ currentQuestion.stem }}</view>
      <image v-for="(img,i) in (currentQuestion.images||[])" :key="i"
             :src="img.url" class="stem-img" mode="widthFix" />

      <view v-if="isObjective" class="options-grid">
        <view v-for="opt in (currentQuestion.options||[])" :key="opt.label"
              class="option-card" :class="{ selected: selectedOptions.includes(opt.label) }"
              @click="toggleOption(opt.label)">
          <view class="option-label">{{ opt.label }}</view>
          <text class="option-content">{{ opt.content }}</text>
        </view>
      </view>
      <view v-else class="subjective-area">
        <textarea v-model="textAnswer" placeholder="请输入答案" class="text-input" />
      </view>

      <button class="submit-btn" @click="submit" :disabled="submitting">
        {{ submitting ? '提交中...' : '提交' }}
      </button>
    </view>

    <view class="feedback-panel">
      <view v-if="feedback" class="feedback-card" :class="feedbackType">
        <text class="feedback-title">
          {{ feedbackType === 'correct' ? '✅ 正确' : (feedbackType === 'pending' ? '⏳ 待批阅' : '❌ 不正确') }}
        </text>
        <text class="feedback-text">{{ feedback }}</text>
        <button v-if="!submitting" class="btn-next" @click="next">
          {{ hasNext ? '下一题' : '完成' }}
        </button>
      </view>
      <view v-else class="feedback-placeholder"><text>提交后显示结果</text></view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { wrongbookApi } from '@/api/student.ts'

interface Opt { label: string; content: string }
interface Q { id: number; question_type: string; stem?: string; options?: Opt[]; images?: { url: string }[] }

const itemId = ref(0)
const questions = ref<Q[]>([])
const currentIndex = ref(0)
const selectedOptions = ref<string[]>([])
const textAnswer = ref('')
const feedback = ref('')
const feedbackType = ref('')
const submitting = ref(false)

const currentQuestion = computed(() => questions.value[currentIndex.value] || ({} as Q))
const isObjective = computed(() =>
  ['single_choice', 'multiple_choice'].includes(currentQuestion.value.question_type))
const hasNext = computed(() => currentIndex.value < questions.value.length - 1)

onMounted(async () => {
  const pages = getCurrentPages()
  const page = pages[pages.length - 1] as any
  itemId.value = parseInt(page.options.itemId || '0')
  if (!itemId.value) { uni.showToast({ title: '缺少错题ID', icon: 'none' }); return }
  try {
    const res = await wrongbookApi.variants(itemId.value)
    questions.value = res.data || []
    if (!questions.value.length) uni.showToast({ title: '暂无同类题', icon: 'none' })
  } catch { uni.showToast({ title: '加载失败', icon: 'none' }) }
})

function toggleOption(label: string) {
  const i = selectedOptions.value.indexOf(label)
  if (i >= 0) selectedOptions.value.splice(i, 1)
  else selectedOptions.value.push(label)
}

async function submit() {
  if (!currentQuestion.value.id) return
  submitting.value = true
  const answer_content = isObjective.value
    ? { selected_options: selectedOptions.value }
    : { text: textAnswer.value }
  try {
    const res = await wrongbookApi.variantSubmit(itemId.value, {
      question_id: currentQuestion.value.id, answer_content,
    })
    feedback.value = res.data?.feedback || ''
    feedbackType.value = res.data?.is_pending ? 'pending'
      : (res.data?.is_correct ? 'correct' : 'incorrect')
  } catch { uni.showToast({ title: '提交失败', icon: 'none' }) }
  finally { submitting.value = false }
}

function next() {
  feedback.value = ''; feedbackType.value = ''
  selectedOptions.value = []; textAnswer.value = ''
  if (hasNext.value) currentIndex.value++
  else uni.navigateBack()
}

function typeLabel(t: string) {
  return ({ single_choice: '单选', multiple_choice: '多选', fill_blank: '填空',
    short_answer: '简答', essay: '论述', computation: '计算', proof: '证明',
    true_false: '判断' } as Record<string, string>)[t] || t
}
</script>

<style scoped>
.answer-page { display:flex; min-height:100vh; background:#f0f2f5; }
.question-panel { flex:1; padding:30rpx 40rpx; }
.question-header { display:flex; justify-content:space-between; margin-bottom:20rpx; }
.q-no { font-size:24rpx; color:#999; }
.q-type { font-size:28rpx; font-weight:bold; color:#333; }
.stem { font-size:28rpx; color:#333; line-height:1.6; margin-bottom:16rpx; }
.stem-img { width:100%; border-radius:8rpx; margin-bottom:16rpx; }
.options-grid { display:grid; grid-template-columns:repeat(2,1fr); gap:16rpx; margin-bottom:30rpx; }
.option-card { border:2rpx solid #ddd; border-radius:12rpx; padding:24rpx; background:#fff; }
.option-card.selected { border-color:#409eff; background:#ecf5ff; }
.option-label { width:40rpx;height:40rpx;border-radius:50%;background:#eee;
  display:flex;align-items:center;justify-content:center;font-size:22rpx;font-weight:bold;margin-bottom:12rpx; }
.option-card.selected .option-label { background:#409eff;color:#fff; }
.option-content { font-size:26rpx;color:#333; }
.subjective-area { margin-bottom:30rpx; }
.text-input { width:100%;min-height:300rpx;border:1rpx solid #ddd;border-radius:12rpx;
  padding:20rpx;box-sizing:border-box;background:#fff;font-size:26rpx; }
.submit-btn { background:#409eff;color:#fff;font-size:28rpx;padding:20rpx 0;border-radius:8rpx; }
.submit-btn[disabled]{background:#ccc;}
.feedback-panel { width:340px;padding:30rpx 24rpx;background:#fff;border-left:1rpx solid #e8e8e8;display:flex;align-items:center; }
.feedback-card { padding:30rpx;border-radius:12rpx;width:100%; }
.feedback-card.correct{background:#e8f5e9;} .feedback-card.incorrect{background:#fff3e0;} .feedback-card.pending{background:#eef6ff;}
.feedback-title{font-size:28rpx;font-weight:bold;display:block;margin-bottom:12rpx;}
.feedback-text{font-size:24rpx;color:#333;display:block;margin-bottom:20rpx;line-height:1.6;}
.btn-next{background:#4caf50;color:#fff;font-size:24rpx;}
.feedback-placeholder{text-align:center;color:#ccc;font-size:26rpx;}
@media (max-width:768px){ .answer-page{flex-direction:column;} .feedback-panel{width:100%;border-left:none;border-top:1rpx solid #e8e8e8;} .options-grid{grid-template-columns:1fr;} }
</style>
```

### 步骤 3.8 　wrongbook-variants.vue 改跳转
文件：`uniapp/src/pages/student/wrongbook-variants.vue`：
(a) 模板把按钮调用改为无参：`@click="startPractice(item.id)"` → `@click="startPractice()"`（不再需要 item.id）。
(b) 替换 `startPractice` 函数（现有第 126–130 行）：
```ts
function startPractice() {
  // 进入独立“同类题练习”页，传错题 id
  uni.navigateTo({ url: `/pages/student/variant-practice?itemId=${wrongId.value}` })
}
```
（列表卡片模板无需改——后端现已返回完整 `stem/options`，正常展示题干。）

### 步骤 3.9 　pages.json 注册新页
文件：`uniapp/src/pages.json`（共享·追加）。当前第 35 行是最后一项（`export`，无尾逗号）。在其后追加新行：把第 35 行末尾加逗号，再加一行：
```json
    { "path": "pages/student/export", "style": { "navigationBarTitleText": "导出 PDF" } },
    { "path": "pages/student/variant-practice", "style": { "navigationBarTitleText": "同类题练习" } }
  ],
```
即：`export` 行尾补 `,`，下一行新增 `variant-practice`，再是数组结尾 `]`。

### 步骤 3.10 　前端验证（端到端）
浏览器（已登录学生）：
1. 进入"错题本" → 点某错题"同类题" → 同类题列表**能看到题干**。
2. 点"开始练习" → 进入"同类题练习"页（不再是 guidance 页）→ 显示第 1 道同类题（题干/选项）。
3. 客观题答对 → 显示"✅ 正确 · 做对了！掌握度已提升" → "下一题"。
4. 回错题本看该错题 → `variant_done_count` 已 +1（后端 step 3.5 复核过）。
5. 同一原错题练对 3 次 → `status` 推进为 `mastered`。

### 步骤 3.11 　回归检查
- 原错题本列表/详情、`mastery_list` 接口不受影响。
- `find_variant_questions` 仍兼容旧调用方（返回 list，字段是超集）。
- `wrongbook-variants.vue` 的"导出 PDF"按钮仍可用（未动）。

### 步骤 3.12 　回滚
- 还原 `apps/wrongbook/urls.py`（删 variant-submit 路由）、`services.py`（还原旧 `find_variant_questions`）。
- 删 `apps/wrongbook/variant_views.py`、`uniapp/src/pages/student/variant-practice.vue`。
- 还原 `wrongbook-variants.vue` 的 `startPractice`、`pages.json`（删新页注册）、`api/student.ts`（删 `variantSubmit`）。

### F4 · DoD
☑ 同类题返回完整题干/选项  ☑ 有提交接口且判分/计数/掌握度真实更新  ☑ 前端独立做题页可走通  ☑ 跳转修复（不再进 guidance）  ☑ 无假数据  ☑ 未碰教师端

---

## 4. 本阶段端到端冒烟（一个学生账号，从头点到尾）

前置：教师账号已建班、建任务（关卡含 1 道客观题 + 1 道主观题）、**先不发布**或发布后让测试学生后入班。

| # | 操作 | 期望 |
|---|---|---|
| 1 | 测试学生在任务**发布后**加入班级 → 刷新首页 | 能看到任务（**F1**） |
| 2 | 进任务 → 关卡 → 答客观题（错） | 进错题本、显示 ❌（F3） |
| 3 | 答主观题 | 显示 ⏳ 待批阅、**不进错题本**（F3） |
| 4 | 看成长页正确率 | 主观题不计入分母（F3） |
| 5 | 错题本 → 同类题 → 开始练习 | 进入"同类题练习"页，题目有题干选项（**F4**） |
| 6 | 同类题答对 3 次 | 错题 `variant_done_count=3`、`status=mastered`、掌握度更新（**F4**） |

全通 → 本阶段 DoD 达成。

## 5. 本阶段涉及文件复核（对照协作速查）

| 文件 | 类型 | 归属 | 功能 |
|---|---|---|---|
| `apps/study/models.py` | 改 | 学生端 | F3 |
| `apps/study/migrations/0003_*.py` | 新 | 学生端 | F3 |
| `apps/study/answer_views.py` | 改 | 学生端 | F3 |
| `apps/study/student_views.py` | 改 | 学生端 | F3 |
| `uniapp/src/pages/student/answer.vue` | 改 | 学生端 | F3 |
| `apps/study/receivers.py` | 新 | 学生端 | F1 |
| `apps/study/apps.py` | 改 | 学生端 | F1 |
| `apps/wrongbook/services.py` | 改 | 学生端 | F4 |
| `apps/wrongbook/variant_views.py` | 新 | 学生端 | F4 |
| `apps/wrongbook/urls.py` | 改 | 学生端 | F4 |
| `uniapp/src/pages/student/variant-practice.vue` | 新 | 学生端 | F4 |
| `uniapp/src/pages/student/wrongbook-variants.vue` | 改 | 学生端 | F4 |
| `uniapp/src/api/student.ts` | 改(追加) | **共享·@对方** | F4 |
| `uniapp/src/pages.json` | 改(追加) | **共享·@对方** | F4 |

共享文件仅 2 个、且为末尾追加；提交前在群里 @ 对方即可。其余全为学生端专有，零冲突。

## 6. 执行顺序小结
F3（含迁移）→ 重启 → F3 验证 → F1（信号）→ 重启 → F1 验证 → F4（后端）→ 重启 → F4 后端验证 → F4（前端）→ 前端验证 → 端到端冒烟。
