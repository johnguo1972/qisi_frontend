# 阶段 3 · 增强与协作（F9 → F8 → F2）细化执行清单

> 前置：阶段 1、阶段 2 已完成并通过冒烟。
> 本文档只产出执行步骤，不改代码；每步可独立验证、可回滚。
> 严格遵循：独立、最小改动、风格一致、无假数据、不破坏既有、文件归属标注。

---

## 0. 前置与约定

> **执行与提交纪律**：直接在 `main` 分支开发，不切分支；执行者一律不 `git add` / `git commit` / `git push`，改动保留为工作区未提交状态，**由用户审阅后手动提交**；`main` 现有未提交改动保持不动、照常叠加 F 改动。

- 后端运行（`venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001`）、Docker `qisi-postgres` 已起、前端 `npm run dev:h5`。
- 学生 token（沿用阶段 1 0.2）。
- F9 依赖题目 `knowledge_points` 已被 AI 流水线回填；F8 需安装 `reportlab`；F2 为教师端协作。

### 本阶段 DoD
- ✅ 知识图谱页展示按知识点的真实掌握度（正确率/等级），数据来自学生已有作答聚合。
- ✅ 导出错题本/任务得到真实 `.pdf`（中文正常），不再是 HTML。
- ✅ 克隆任务到新班级后，目标班级学生能看到（教师端协作者实施）。

---

## F9. 学生知识掌握度（替换占位页）

**问题**：`uniapp/src/pages/student/knowledge-graph.vue:11-12` 仅"功能开发中/敬请期待"；无学生侧后端。
**目标**：按知识点聚合学生作答正确率（从 `AnswerAttempt` × `ExamQuestion.knowledge_points` 实时算），真实数据；可视化先做"知识点掌握度列表"（弱项在前）。
**涉及文件**：
- 新建 `apps/study/knowledge_views.py`（学生端专有）
- 改 `apps/study/student_urls.py`（+1 路由）
- 改 `uniapp/src/api/student.ts`（+1 方法，**共享·@对方**·追加）
- 改 `uniapp/src/pages/student/knowledge-graph.vue`（整文件替换）

### 步骤 1.1 　新建 knowledge_views.py
文件：`apps/study/knowledge_views.py`（新文件，完整内容）：
```python
"""学生知识掌握度：按知识点聚合作答正确率。"""
import uuid
from collections import defaultdict
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from .models import AnswerAttempt


def make_trace_id():
    return uuid.uuid4().hex[:16]


@api_view(['GET'])
@permission_classes([IsAuthenticated])
def knowledge_mastery(request):
    """GET /api/v1/student/knowledge-mastery
    返回每个知识点的 attempt/correct/accuracy（排除主观题待批阅）。弱项在前。
    """
    attempts = AnswerAttempt.objects.filter(
        student_user_id=request.user, is_subjective_pending=False
    )
    qids = [a.question_id for a in attempts]
    q_map = {q.id: q for q in ExamQuestion.objects.filter(id__in=qids)}

    agg = defaultdict(lambda: {'attempt': 0, 'correct': 0})
    for a in attempts:
        q = q_map.get(a.question_id)
        if not q:
            continue
        for kp in (q.knowledge_points or []):
            label = (kp.get('module') if isinstance(kp, dict) else str(kp)) or '未分类'
            agg[label]['attempt'] += 1
            if a.is_correct:
                agg[label]['correct'] += 1

    items = []
    for label, v in agg.items():
        acc = round(v['correct'] / max(v['attempt'], 1) * 100, 1)
        items.append({
            'knowledge': label,
            'attempt': v['attempt'],
            'correct': v['correct'],
            'accuracy': acc,
            'mastery': 'mastered' if acc >= 85 else ('reviewing' if acc >= 60 else 'weak'),
        })
    items.sort(key=lambda x: x['accuracy'])  # 弱项在前

    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'items': items, 'total': len(items)}})
```

### 步骤 1.2 　student_urls.py 增加路由
文件：`apps/study/student_urls.py`，新增 import 与一条路由（在现有 urlpatterns 列表内追加）：
```python
from django.urls import path
from .student_views import (
    student_home, student_mission_detail, student_level_detail, growth_summary, export_pdf, upload_attempt_image
)
from .answer_views import submit_answer, retry_answer, get_mode_a
from .guidance_views import start_guidance, guidance_reply
from .knowledge_views import knowledge_mastery   # ← 新增 import

app_name = 'student'
urlpatterns = [
    path('home', student_home, name='student-home'),
    path('missions/<int:mission_id>', student_mission_detail, name='student-mission'),
    path('levels/<int:level_id>', student_level_detail, name='student-level'),
    path('attempts', submit_answer, name='submit-answer'),
    path('attempts/<int:attempt_id>/retry', retry_answer, name='retry-answer'),
    path('guidance/sessions', start_guidance, name='start-guidance'),
    path('guidance/sessions/<int:session_id>/reply', guidance_reply, name='guidance-reply'),
    path('questions/<int:question_id>/mode-a', get_mode_a, name='mode-a'),
    path('growth', growth_summary, name='growth-summary'),
    path('export/pdf', export_pdf, name='export-pdf'),
    path('attempts/<int:attempt_id>/upload-image', upload_attempt_image, name='upload-attempt-image'),
    path('knowledge-mastery', knowledge_mastery, name='knowledge-mastery'),   # ← 新增
]
```

### 步骤 1.3 　api/student.ts 追加方法
文件：`uniapp/src/api/student.ts`（共享·追加），在 `studentApi` 对象内追加：
```ts
  knowledgeMastery: () => get('/student/knowledge-mastery'),   // ← 新增
```

### 步骤 1.4 　knowledge-graph.vue 整文件替换
文件：`uniapp/src/pages/student/knowledge-graph.vue`，**整文件替换**为：
```vue
<template>
  <view class="kg-page">
    <view class="nav"><text class="title">知识掌握度</text></view>
    <view v-if="loading" class="hint">加载中...</view>
    <view v-else-if="!items.length" class="hint">暂无作答数据，先去做几道题吧～</view>
    <view v-else class="list">
      <view v-for="it in items" :key="it.knowledge" class="card">
        <view class="row">
          <text class="kp">{{ it.knowledge }}</text>
          <text :class="['badge', it.mastery]">{{ masteryLabel(it.mastery) }}</text>
        </view>
        <view class="bar"><view class="bar-inner" :style="{ width: it.accuracy + '%' }" :class="it.mastery"></view></view>
        <text class="meta">正确率 {{ it.accuracy }}% · 作答 {{ it.attempt }} · 正确 {{ it.correct }}</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { studentApi } from '@/api/student.ts'

const items = ref<any[]>([])
const loading = ref(true)

onMounted(async () => {
  try {
    const res = await studentApi.knowledgeMastery()
    items.value = res.data?.items || []
  } finally { loading.value = false }
})

function masteryLabel(m: string) {
  return ({ mastered: '已掌握', reviewing: '巩固中', weak: '薄弱' } as Record<string,string>)[m] || m
}
</script>

<style scoped>
.kg-page { min-height:100vh; background:#f0f2f5; }
.nav { padding:24rpx 32rpx; background:#fff; border-bottom:1rpx solid #eee; }
.title { font-size:32rpx; font-weight:bold; }
.hint { text-align:center; color:#999; padding:160rpx 0; font-size:26rpx; }
.list { padding:24rpx; }
.card { background:#fff; border-radius:12rpx; padding:24rpx; margin-bottom:20rpx; }
.row { display:flex; justify-content:space-between; align-items:center; margin-bottom:16rpx; }
.kp { font-size:28rpx; font-weight:bold; color:#333; }
.badge { font-size:22rpx; padding:4rpx 16rpx; border-radius:20rpx; }
.badge.mastered{background:#e8f5e9;color:#4caf50;}
.badge.reviewing{background:#fff3e0;color:#ff9800;}
.badge.weak{background:#ffebee;color:#f44336;}
.bar { height:12rpx; background:#eee; border-radius:6rpx; overflow:hidden; margin-bottom:12rpx; }
.bar-inner { height:100%; }
.bar-inner.mastered{background:#4caf50;} .bar-inner.reviewing{background:#ff9800;} .bar-inner.weak{background:#f44336;}
.meta { font-size:22rpx; color:#999; }
</style>
```

### 步骤 1.5 　重启 + 验证
重启后端。学生作答若干客观题后（题目需有 `knowledge_points`）：
```bash
curl -H "Authorization: Bearer <STUDENT_TOKEN>" http://localhost:8001/api/v1/student/knowledge-mastery
```
期望：`data.items` 含各知识点的 `accuracy/mastery`，弱项在前；无作答时 `items=[]`。
前端：进入"知识图谱"页 → 真实列表（非"功能开发中"）。

### 步骤 1.6 　回归 / 回滚
- 仅新增接口与重写占位页，不影响其它。回滚：删路由/import/方法、还原 vue 占位页、删 `knowledge_views.py`。

### F9 · DoD
☑ 真实聚合数据  ☑ 占位文案消失  ☑ 无 schema 变更

---

## F8. PDF 导出真化（reportlab）

**问题**：`apps/study/student_views.py:194-329` 生成 `.html`，注释明说"待 reportlab 可用后替换"。
**目标**：用 reportlab 内置 CJK 字体 `STSong-Light` 生成真 PDF；接口签名/响应不变，仅文件扩展名 `.pdf`。
**避坑**：reportlab 的 `Paragraph` 只认其迷你 XML、**不认 HTML**，故题干用**纯文本 `stem`**（下方代码已采用），否则 `stem_html` 里的 `<p>/<img>/&nbsp;` 等会让导出 500。
**可选·对齐需求文档**：把 `apps/study/student_views.py` 的 `EXPORT_RATE_LIMIT` 由 10 改为 3（次/小时），并在 `export_pdf` 中加 `questions_data = questions_data[:50]`（max 50 题/PDF，需求文档 Phase 2 要求）。
**涉及文件**：
- 改 `apps/study/student_views.py`（新增 `_build_pdf`，`export_pdf` 改用之）
- 改 `requirements.txt`（**共享·@对方**·追加 `reportlab`）

### 步骤 2.1 　requirements.txt 追加依赖
文件：`requirements.txt`（共享），末尾追加：
```
reportlab>=4.0,<5.0
```
安装：
```bash
venv/Scripts/python.exe -m pip install "reportlab>=4.0,<5.0"
```

### 步骤 2.2 　student_views.py 新增 _build_pdf
文件：`apps/study/student_views.py`，在 `_build_html` 函数**下方**新增 `_build_pdf`（保留 `_build_html` 不删，最小改动）：
```python
def _build_pdf(export_type: str, questions: list, include_answers: bool) -> bytes:
    """用 reportlab 生成 PDF（内置 STSong-Light 支持中文，无需字体文件）。"""
    from reportlab.lib.pagesizes import A4
    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer
    from reportlab.pdfbase import pdfmetrics
    from reportlab.pdfbase.cidfonts import UnicodeCIDFont
    from reportlab.lib.units import mm
    import io

    pdfmetrics.registerFont(UnicodeCIDFont('STSong-Light'))
    buf = io.BytesIO()
    doc = SimpleDocTemplate(buf, pagesize=A4,
                            topMargin=15*mm, bottomMargin=15*mm,
                            leftMargin=15*mm, rightMargin=15*mm)
    styles = getSampleStyleSheet()
    h1 = ParagraphStyle('ZhH1', parent=styles['Title'], fontName='STSong-Light', fontSize=18)
    body = ParagraphStyle('ZhBody', parent=styles['Normal'], fontName='STSong-Light', fontSize=11, leading=18)
    story = []

    type_label = '错题本' if export_type == 'wrongbook' else '任务题目'
    story.append(Paragraph(type_label, h1))
    story.append(Spacer(1, 6*mm))
    story.append(Paragraph(f'导出时间：{timezone.now().strftime("%Y-%m-%d %H:%M:%S")}　题目数：{len(questions)}', body))
    story.append(Spacer(1, 6*mm))

    for i, q in enumerate(questions, 1):
        qtype = ExamQuestion.QUESTION_TYPE_LABELS.get(q['question_type'], q['question_type'])
        story.append(Paragraph(f'第{i}题（{qtype}）', body))
        story.append(Paragraph(q['stem'] or '', body))  # 纯文本，避免 stem_html 的 HTML 让 reportlab 报错
        for opt in q.get('options_html', []):
            story.append(Paragraph(f'{opt["label"]}. {opt["content"]}', body))
        if include_answers:
            if q.get('answer'):
                story.append(Paragraph(f'<b>答案：</b>{q["answer"]}', body))
            if q.get('analysis'):
                story.append(Paragraph(f'<b>解析：</b>{q["analysis"]}', body))
        story.append(Spacer(1, 4*mm))

    doc.build(story)
    return buf.getvalue()
```

### 步骤 2.3 　export_pdf 改用 _build_pdf
同一文件 `export_pdf`，把"生成文件"段（现有第 313–323 行附近）的文件名与写入替换：
```python
    # --- generate file ---
    export_dir = os.path.join(settings.MEDIA_ROOT, 'exports')
    os.makedirs(export_dir, exist_ok=True)
    filename = f'{export_type}_{uuid.uuid4().hex[:12]}.pdf'        # ← .html 改 .pdf
    filepath = os.path.join(export_dir, filename)

    pdf_bytes = _build_pdf(export_type, questions_data, include_answers)   # ← 真实 PDF
    with open(filepath, 'wb') as f:
        f.write(pdf_bytes)

    download_url = f'{settings.MEDIA_URL}exports/{filename}'
    return Response({
        'code': 0, 'message': '导出成功',
        'data': {'download_url': download_url, 'question_count': len(questions_data)},
        'trace_id': trace_id,
    })
```

### 步骤 2.4 　重启 + 验证
重启后端。先取一个错题 question_id（阶段 1 已造）：
```bash
curl -X POST http://localhost:8001/api/v1/student/export/pdf \
  -H "Authorization: Bearer <STUDENT_TOKEN>" -H "Content-Type: application/json" \
  -d "{\"export_type\":\"wrongbook\",\"item_ids\":[<QUESTION_ID>],\"include_answers\":true}"
```
期望 `download_url` 以 `.pdf` 结尾；浏览器打开 `http://localhost:8000<download_url>`（经前端代理 `http://localhost:5273<download_url>`）→ 下载 PDF，中文正常、含题干/选项/答案解析。

### 步骤 2.5 　回归 / 回滚
- 仅替换导出产物格式，请求/响应结构不变，前端 `export.vue` 无需改。回滚：把 `export_pdf` 文件名与写入还原为 `_build_html`/`.html`（或保留 `_build_pdf` 不调用）。

### F8 · DoD
☑ 产出真 .pdf  ☑ 中文正常  ☑ 接口契约不变  ☑ 前端无需改

---

## F2. 克隆任务到班级学生可见性（教师端·需协作）

**问题**：`apps/missions/views.py:304-362` `mission_clone_with_class` 创建副本并设 `class_obj`，但 `status` 默认 `draft`、且不创建 `StudentMissionProgress` → 目标班级学生看不到。
**归属**：`apps/missions/views.py` 属教师端，**由对方实施**，我方不动该文件、只提供片段与流程。

### 方案 A（推荐）：克隆后自动发布 + fan-out（对方插入）
在 `mission_clone_with_class` 的 `return Response(...)` **之前**插入（与 `mission_publish` 同款逻辑）：
```python
    # [教师端·对方插入] 克隆后自动发布并为目标班级学生建进度
    clone.status = 'published'
    clone.save(update_fields=['status'])

    if clone.class_obj_id:
        from apps.institutions.models import ClassStudent
        from apps.study.models import StudentMissionProgress
        student_ids = ClassStudent.objects.filter(
            class_obj_id=clone.class_obj_id, status='active'
        ).values_list('student_id', flat=True)
        for sid in student_ids:
            StudentMissionProgress.objects.get_or_create(
                mission=clone, student_user_id_id=sid,
                defaults={'progress_status': 'not_started', 'progress_percent': 0},
            )
```

### 方案 B（零代码）：流程约定
不改代码。约定"克隆是草稿 → 教师手动点发布"。已有 `mission_publish` 会 fan-out；阶段 1 的 F1 信号会兜底**之后入班**的学生。教师端 UI 需在克隆成功后提示"请到任务列表发布该副本"。

### 验证（任一方案生效后）
- 教师克隆任务 A → 班级 B（B 中已有学生）。
- 方案 A：B 班学生首页立即看到副本。方案 B：教师发布后，B 班学生首页看到。
- 之后再有学生加入 B 班 → F1 信号回填 → 也能看到。

### F2 · DoD（对方实施后）
☑ 克隆到目标班级的学生可见任务  ☑ 后入班学生也能看到（F1 兜底）

---

## 4. 本阶段端到端冒烟

| # | 操作 | 期望 |
|---|---|---|
| 1 | 学生做若干客观题（题目有知识点）→ 进"知识图谱"页 | 真实知识点掌握度列表，弱项在前（**F9**） |
| 2 | 错题本 → 导出 PDF | 下载到 `.pdf`，中文正常（**F8**） |
| 3 | 教师克隆任务到新班级（方案A） | 新班级学生首页看到副本（**F2**） |

## 5. 本阶段文件复核

| 文件 | 类型 | 归属 | 功能 |
|---|---|---|---|
| `apps/study/knowledge_views.py` | 新 | 学生端 | F9 |
| `apps/study/student_urls.py` | 改(+1路由) | 学生端 | F9 |
| `uniapp/src/api/student.ts` | 改(追加) | **共享·@对方** | F9 |
| `uniapp/src/pages/student/knowledge-graph.vue` | 改(整文件替换) | 学生端 | F9 |
| `apps/study/student_views.py` | 改(新增_build_pdf+改export_pdf) | 学生端 | F8 |
| `requirements.txt` | 改(追加1行) | **共享·@对方** | F8 |
| `apps/missions/views.py` | 改(对方插入片段) | **教师端·对方** | F2 |

共享：`api/student.ts`、`requirements.txt`（均末尾追加）；教师端：`missions/views.py`（对方改）。学生端专有文件零冲突。
