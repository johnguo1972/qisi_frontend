# 学生端功能完善实施方案

> 范围：仅 `role_type = "student"` 的前后端功能。
> 基线：2026-07-07 对 `apps/study`、`apps/wrongbook`、`apps/missions`、`apps/institutions`、`uniapp/src/pages/student` 的逐行核查结论。
> 原则：代码独立、改动最小、风格与现有一致、无假数据（全部走真实接口）、不删除/不破坏既有业务代码。
> 本文档只出方案，**不改代码**。

---

## 0. 阅读指南

- 第 1 节：9 项待完善功能总览（优先级 + 类型 + 涉及端）。
- 第 2 节：协作冲突规避（两人同时改前后端，必须先读）。
- 第 3 节：数据库变更汇总（全方案仅 1 处 schema 变更）。
- 第 4 节：F1~F9 逐功能方案（现状证据 → 后端接口+完整代码 → 前端页面+完整代码 → 联调点 → 涉及文件）。
- 第 5 节：全量文件清单（新建 / 修改，归属标注）。
- 第 6 节：实施顺序 + 端到端测试清单。

代码风格约定（与现网一致，所有新增代码遵守）：
- 响应封装：`{'code': 0, 'message': 'success', 'data': {...}, 'trace_id': make_trace_id()}`
- `make_trace_id = uuid.uuid4().hex[:16]`（现有各 view 文件各自定义，新增文件同样自带）
- 视图：函数式 `@api_view([...])` + `@permission_classes([IsAuthenticated])`
- 前端请求：`import { get, post } from '@/utils/request.ts'`，`BASE_URL='/api/v1'`
- 前端页面：`<script setup lang="ts">` + `uni.navigateTo`，样式 `rpx`

---

## 1. 待完善功能总览

| 编号 | 功能 | 问题类型 | 优先级 | 涉及端 | 一句话现状 |
|---|---|---|---|---|---|
| F1 | 后入班学生看不到任务 | 链路断 | P0 | 后端 | `join_by_code` 加入班级后不回填 `StudentMissionProgress` |
| F2 | 克隆任务到班级学生看不到 | 链路断 | P1 | **教师端**(需协作) | `mission_clone_with_class` 既不发布也不建 progress |
| F3 | 主观题一律判错 | 逻辑 bug | P0 | 前后端 | `_check_answer` 对主观题 `return False`，全部进错题本 |
| F4 | 变式题练习无闭环 | 链路断 + bug | P0 | 前后端 | 变式题接口只返回极简字段、无提交接口、前端跳到 guidance 占位页 |
| F5 | 任务详情越权 IDOR | 安全 | P1 | 后端 | `student_mission_detail` 不校验学生是否属于该任务班级 |
| F6 | 学生接口无角色校验 | 安全 | P1 | 后端 | `/api/v1/student/*` 只校验登录，不校验 `role_type=='student'` |
| F7 | AI 辅导返回假数据 | 桩 | P1 | 前后端 | `guidance_views` 硬编码字符串，未用真实 `ai_answer_b/c` 与 LLM |
| F8 | PDF 导出是 HTML 占位 | 桩 | P2 | 后端 | `export_pdf` 生成 `.html`，未接 reportlab |
| F9 | 学生知识图谱是占位页 | 未实现 | P2 | 前后端 | 页面"功能开发中"，无学生侧后端 |

---

## 2. 协作冲突规避方案（必读）

### 2.1 开发与提交纪律

- **直接在 `main` 分支开发，不切 feature 分支。**
- **执行者一律不 `git add` / `git commit` / `git push`**，改动全部保留为工作区未提交状态；**由用户审阅确认后手动提交**。
- `main` 现有未提交改动（requirements.txt 修复、README、.gitignore、start.bat、word 文档、端口改动等）**保持不动、照常叠加 F 改动**；最终提交时由用户把"既有改动 / F 改动"分开成不同 commit。
- 与对方协作时仅协调共享文件（见 2.2 / 2.3），不涉及分支合并。

### 2.2 文件归属约定（避免两人改同一文件）

| 归属 | 路径 | 处理 |
|---|---|---|
| **学生端·我方专有** | `apps/study/*`（除共享信号注册）、`apps/wrongbook/*`、`uniapp/src/pages/student/*`、新增的 `apps/study/receivers.py / ai_helper.py / middleware.py / permissions.py / knowledge_views.py / variant_views.py`(wrongbook) | 我方独占，**对方不改** |
| **共享·需协调** | `config/settings.py`、`config/urls.py`、`requirements.txt`、`uniapp/src/pages.json`、`uniapp/src/api/student.ts` | 我方仅做**追加式**最小改动（末尾追加 / 新增一行），改动点在第 5 节逐一列出，提交前在群里 @ 对方 |
| **教师端·对方专有** | `apps/missions/views.py`、`apps/review/*`、`apps/parser/*`、`apps/knowledge/teacher_api_views.py`、`uniapp/src/pages/teacher/*` | 我方**不动**；F2 仅出代码片段交对方实施 |

### 2.3 降低冲突的具体手法
- **能用新文件就不改旧文件**：F1 用信号接收器（新文件）而非改 `join_by_code`；F4 变式提交用新 `variant_views.py`；F6 用新 `permissions.py` + 给学生视图加 `IsStudent`（不用中间件，因 JWT 鉴权晚于中间件）。
- **共享文件追加式**：`requirements.txt` 末尾加 `reportlab`；`pages.json` 在 `pages` 数组末尾追加一项；`api/student.ts` 在 `wrongbookApi` 对象内追加方法。（F6 已改为不触碰任何共享文件。）
- **提交规范**：执行者不提交；用户审阅后手动提交，建议每个 F 一个 commit、前缀 `[student]`，便于 review 与回滚。

---

## 3. 数据库变更汇总

**全方案仅 1 处 schema 变更**（F3），其余全部复用既有表/字段。

| 表 | 变更 | 来源功能 | 迁移文件 |
|---|---|---|---|
| `answer_attempt` | 新增列 `is_subjective_pending TINYINT/BOOL DEFAULT 0`（Django `BooleanField(default=False)`） | F3 | `apps/study/migrations/0003_answerattempt_is_subjective_pending.py` |

复用（不改 schema）：
- `student_mission_progress`（F1 回填）
- `wrong_book_item.variant_done_count` / `retry_count` / `status`（F4 更新）
- `mastery_record`（F4 变式掌握度、F9 知识掌握度读写）
- `ai_guidance_session`（F7 持久化对话）
- `answer_attempt.question_id`（IntegerField，变式题作答复用，无需 FK）

---

## 4. 逐功能详细方案

---

### F1. 后入班学生任务可见性修复（P0·后端）

**现状证据**：
- `apps/study/student_views.py:30` `student_home` 查 `StudentMissionProgress.filter(student_user_id=request.user)`。
- `StudentMissionProgress` 全仓库仅由 `apps/missions/views.py:243-256` `mission_publish` 创建（发布时为当时在班学生 fan-out）。
- `apps/institutions/student_views.py:50-68` `join_by_code` 成功后**不回填** → 学生在任务发布**之后**才加入班级，首页永远空。

**方案**：用 Django **信号**，在 `ClassStudent` 新建时自动为该班级所有已发布任务回填 `StudentMissionProgress`。不改 `join_by_code`（避免动共享/教师端代码），逻辑放在学生端 `apps/study`。

**数据库**：无变更（复用 `student_mission_progress`）。

**后端代码**：

新增 `apps/study/receivers.py`（新文件）：
```python
"""学生端信号接收器：学生加入班级后回填任务进度。"""
from django.db.models.signals import post_save
from django.dispatch import receiver
from apps.institutions.models import ClassStudent
from apps.missions.models import LearningMission
from .models import StudentMissionProgress


@receiver(post_save, sender=ClassStudent)
def backfill_mission_progress_on_join(sender, instance, created, **kwargs):
    """新增活跃班级成员时，为该班级所有已发布任务补建 StudentMissionProgress。"""
    if not created or instance.status != 'active':
        return
    missions = LearningMission.objects.filter(
        class_obj_id=instance.class_obj_id, status='published'
    )
    for mission in missions:
        StudentMissionProgress.objects.get_or_create(
            mission=mission,
            student_user_id_id=instance.student_id,
            defaults={'progress_status': 'not_started', 'progress_percent': 0},
        )
```

修改 `apps/study/apps.py`（学生端专有文件；当前无 `ready()`，新增）：
```python
from django.apps import AppConfig


class StudyConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.study'
    verbose_name = '学习作答'

    def ready(self):
        # 注册学生端信号接收器（导入即注册）
        from . import receivers  # noqa: F401
```

**联调验证点**：
1. 教师向班级 A 发布任务 T（此时学生 S 不在 A）。
2. 学生 S 通过邀请码加入 A → 后台应自动生成 `StudentMissionProgress(mission=T, student=S)`。
3. S 刷新首页 `GET /api/v1/student/home` → 能看到任务 T。
4. 退班后再加入仍生效（`quit_class` 物理删 `ClassStudent`，重新加入触发 `created=True`）。

**涉及文件**：新建 `apps/study/receivers.py`；修改 `apps/study/apps.py`（+`ready()`）。均为学生端专有，零共享冲突。

---

### F2. 克隆任务到班级学生可见性（P1·教师端·需协作）

**现状证据**：`apps/missions/views.py:304-362` `mission_clone_with_class` 创建副本，设了 `class_obj`、`end_at`，但 `status` 默认 `draft`，且**不创建 `StudentMissionProgress`** → 目标班级学生看不到。

**归属**：`apps/missions/views.py` 属教师端，**由对方实施**。我方提供如下片段交对方，在 `mission_clone_with_class` 末尾 `return` 前插入（与 `mission_publish` 同款逻辑）：

```python
# [教师端·需协作插入] 克隆后自动发布并为目标班级学生建进度
clone.status = 'published'
clone.save(update_fields=['status'])

if clone.class_obj_id:
    from apps.institutions.models import ClassStudent
    from apps.study.models import StudentMissionProgress
    student_ids = ClassStudent.objects.filter(
        class_obj_id=clone.class_obj_id, status='active'
    ).values_list('student_id', flat=True)
    for sid in student_ids:
        StudentMissionProgress.objects.get_or_create(
            mission=clone, student_user_id_id=sid,
            defaults={'progress_status': 'not_started', 'progress_percent': 0},
        )
```

> 备注：若对方倾向"克隆后由教师手动发布"，则无需改代码，仅文档约定流程即可（克隆是草稿 → 教师点发布 → 已有 `mission_publish` fan-out，且 F1 信号会兜底后续入班学生）。

**涉及文件**：`apps/missions/views.py`（教师端，对方改）。

---

### F3. 主观题判分修复（P0·前后端）

**现状证据**：`apps/study/answer_views.py:15-29`
```python
def _check_answer(question, answer_content):
    if question.question_type in ('single_choice', 'multiple_choice'):
        ...
        return selected == correct
    return False   # ← 主观题一律 False：进错题本、0 分、显示"错误"
```
影响 `submit_answer`、`retry_answer`，以及 `growth_summary` 的正确率统计。

**方案**：引入"待批阅"三态。客观题照旧自动判分；主观题标记 `is_subjective_pending=True`，**不进错题本**、不计正确率、反馈"已提交，待老师批阅"。

**数据库**：`answer_attempt` 新增 `is_subjective_pending BOOL DEFAULT 0`。

**后端代码**：

1) `apps/study/models.py` `AnswerAttempt` 内新增字段（学生端专有文件）：
```python
class AnswerAttempt(models.Model):
    ...
    is_correct = models.BooleanField(default=False)
    is_subjective_pending = models.BooleanField(default=False)   # ← 新增
    score = models.DecimalField(max_digits=5, decimal_places=2, default=0.00)
    ...
```

2) 新增迁移 `apps/study/migrations/0003_answerattempt_is_subjective_pending.py`：
```python
from django.db import migrations, models


class Migration(migrations.Migration):
    dependencies = [('study', '0002_favorite')]

    operations = [
        migrations.AddField(
            model_name='answerattempt',
            name='is_subjective_pending',
            field=models.BooleanField(default=False),
        ),
    ]
```
（也可直接 `python manage.py makemigrations study` 生成，文件名以自动生成为准。）

3) `apps/study/answer_views.py` 改 `_check_answer` 与 `_handle_submit_answer`（替换现有两个函数体；其余视图不动）：
```python
SUBJECTIVE_TYPES = ('fill_blank', 'short_answer', 'essay', 'true_false', 'computation', 'proof')


def _check_answer(question, answer_content: dict):
    """客观题返回 True/False；主观题返回 None（待批阅）。"""
    if question.question_type in ('single_choice', 'multiple_choice'):
        selected = set(answer_content.get('selected_options', []))
        correct_str = getattr(question, 'answer', '') or ''
        if correct_str:
            correct = set(correct_str.replace(' ', '').upper())
        else:
            ai_a = question.ai_answer_a or {}
            correct = set(str(ai_a.get('answer', '')).replace(' ', '').upper())
        return selected == correct
    return None   # 主观题无法自动判分


def _handle_submit_answer(request, question_id, answer_content, mission_id, level_id, source):
    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    result = _check_answer(q, answer_content)
    is_pending = result is None
    is_correct = False if is_pending else result

    attempt_no = AnswerAttempt.objects.filter(
        student_user_id=request.user, question_id=question_id
    ).count() + 1

    attempt = AnswerAttempt.objects.create(
        student_user_id=request.user,
        mission_id=mission_id, level_id=level_id,
        question_id=question_id, attempt_no=attempt_no,
        answer_content=answer_content, is_correct=is_correct,
        is_subjective_pending=is_pending,
        score=100.00 if is_correct else 0.00,
        submit_source=source,
    )

    if level_id:
        lp = StudentLevelProgress.objects.filter(
            level_id=level_id, student_user_id=request.user
        ).first()
        if lp:
            lp.attempt_count += 1
            if is_correct:
                lp.pass_score = max(lp.pass_score, 100.00)
            lp.save()

    # 仅"客观题答错"才进错题本；主观题待批阅不进
    if (not is_correct) and (not is_pending):
        WrongBookItem.objects.get_or_create(
            student_user_id=request.user, question_id=question_id,
            defaults={'status': 'not_reviewed'}
        )

    if is_pending:
        feedback = '主观题已提交，等待老师批阅'
    else:
        feedback = generate_feedback(is_correct, q, attempt_no)

    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'is_correct': is_correct,
            'is_pending': is_pending,           # ← 新增字段
            'score': float(attempt.score),
            'feedback': feedback,
            'attempt_id': attempt.id,
            'suggest_guidance': (not is_correct) and (not is_pending),
        }, 'trace_id': make_trace_id(),
    })
```

4) `apps/study/student_views.py` `growth_summary` 排除待批阅（学生端专有）：
```python
def growth_summary(request):
    from apps.wrongbook.models import WrongBookItem, MasteryRecord
    base = AnswerAttempt.objects.filter(student_user_id=request.user)
    total_attempts = base.exclude(is_subjective_pending=True).count()      # ← 排除待批阅
    total_correct = base.filter(is_correct=True).count()
    mastered_count = MasteryRecord.objects.filter(
        student_user_id=request.user, mastery_status='mastered').count()
    wrong_count = WrongBookItem.objects.filter(student_user_id=request.user).count()
    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'total_attempts': total_attempts,
            'total_correct': total_correct,
            'accuracy': round(total_correct / max(total_attempts, 1) * 100, 1),
            'mastered_count': mastered_count,
            'wrong_book_count': wrong_count,
        }, 'trace_id': make_trace_id(),
    })
```

**前端代码**：`uniapp/src/pages/student/answer.vue` 反馈区处理"待批阅"（学生端专有，仅改反馈分支）：
```vue
<!-- feedback-panel 内 feedback-card 的 class 与文案 -->
<view v-if="feedback" class="feedback-card" :class="feedbackType">
  <view class="feedback-header">
    <text class="feedback-icon">
      {{ feedbackType === 'correct' ? '✅' : (feedbackType === 'pending' ? '⏳' : '❌') }}
    </text>
    <text class="feedback-title">
      {{ feedbackType === 'correct' ? '回答正确' : (feedbackType === 'pending' ? '已提交，待批阅' : '回答错误') }}
    </text>
  </view>
  <text class="feedback-text">{{ feedback }}</text>
  <view class="feedback-actions">
    <button v-if="suggestGuidance" @click="startGuidance" class="btn-guidance">进入引导模式</button>
    <button v-else @click="nextQuestion" class="btn-next">{{ hasNext ? '下一题' : '完成' }}</button>
  </view>
</view>
```
脚本里 `submitAnswer` 内：
```ts
feedback.value = res.data?.feedback || ''
feedbackType.value = res.data?.is_pending ? 'pending'
  : (res.data?.is_correct ? 'correct' : 'incorrect')
suggestGuidance.value = res.data?.suggest_guidance || false
```
并在 `<style>` 追加 `.feedback-card.pending { background: #eef6ff; }`。

**联调验证点**：
- 客观题答错 → 进错题本、显示 ❌、正确率分母 +1。
- 主观题任意答案 → 不进错题本、显示 ⏳"已提交，待批阅"、正确率分母不变。
- 成长页正确率不再被主观题拉低。

**涉及文件**：改 `apps/study/models.py`、`apps/study/answer_views.py`、`apps/study/student_views.py`；新建 `apps/study/migrations/0003_*.py`；改 `uniapp/src/pages/student/answer.vue`。全为学生端专有。

---

### F4. 变式题练习闭环（P0·前后端）⭐ 重点

**现状证据**：
- `apps/wrongbook/services.py:5-28` `find_variant_questions` 只返回 `{id, question_no, question_type, difficulty}`，**无题干/选项/图片** → 无法答题。
- `apps/wrongbook/urls.py` 仅有 `variants` 的 GET，**无提交/判分接口**。
- `wrongbook-variants.vue:126-130` `startPractice` 跳到 `/pages/student/guidance`（占位页），**跳错**。
- `WrongBookItem.variant_done_count` 定义了但全仓库**从没被写**。

**方案**（三段闭环：展示完整题目 → 独立做题页提交 → 更新错题计数/掌握度）：
1. 后端：`variants` 接口返回**完整题目**（题干/选项/图片/难度）；新增 `variant-submit` 提交+判分+计数+掌握度。
2. 前端：新增**独立**做题页 `variant-practice.vue`（参照 `answer.vue` 结构，但代码独立、不复用 answer.vue），`wrongbook-variants.vue` 的"开始练习"改跳新页。

**数据库**：无变更（复用 `wrong_book_item.variant_done_count`、`answer_attempt`、`mastery_record`）。

#### 后端

1) `apps/wrongbook/services.py` —— 增强返回完整字段（学生端/wrongbook 专有）：
```python
"""Wrong book services: variant question recommendation."""
from apps.parser.models import ExamQuestion


def _question_brief(q):
    """组装一道题的完整展示数据（含选项/图片）。"""
    return {
        'id': q.id,
        'question_no': q.question_no,
        'question_type': q.question_type,
        'subject': q.subject,
        'difficulty': float(q.difficulty) if q.difficulty else None,
        'stem': q.stem,
        'stem_html': getattr(q, 'stem_html', None),
        'images': [{'url': img.file_path} for img in q.images.all().order_by('sort_order')],
        'options': [{'label': o.option_label, 'content': o.content}
                    for o in q.options.all().order_by('sort_order')],
    }


def find_variant_questions(question_id: int, limit: int = 3) -> list:
    """同学科 + 同难度（可空）+ 排除原题，返回完整题目数据。"""
    try:
        original = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return []

    qs = ExamQuestion.objects.filter(subject=original.subject).exclude(pk=question_id)
    if original.difficulty is not None:
        qs = qs.filter(difficulty=original.difficulty)
    qs = qs.prefetch_related('images', 'options')[:limit]
    return [_question_brief(q) for q in qs]
```

2) 新增 `apps/wrongbook/variant_views.py`（新文件，独立，含判分 + 计数 + 掌握度）：
```python
"""变式题练习：提交答案、判分、更新错题计数与掌握度。"""
import uuid
from django.db import transaction
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from apps.study.models import AnswerAttempt
from .models import WrongBookItem, MasteryRecord


def make_trace_id():
    return uuid.uuid4().hex[:16]


OBJECTIVE_TYPES = ('single_choice', 'multiple_choice')


def _grade(question, answer_content):
    """客观题 True/False；主观题 None（待批阅）。"""
    if question.question_type in OBJECTIVE_TYPES:
        selected = set(answer_content.get('selected_options', []))
        correct_str = getattr(question, 'answer', '') or ''
        if correct_str:
            correct = set(correct_str.replace(' ', '').upper())
        else:
            ai_a = question.ai_answer_a or {}
            correct = set(str(ai_a.get('answer', '')).replace(' ', '').upper())
        return selected == correct
    return None


def _update_mastery(student, question_id: int, is_correct: bool):
    """按题目维度更新掌握度（mastered 需连续答对，简化为：答对即 mastered）。"""
    mr, _ = MasteryRecord.objects.get_or_create(
        student_user_id=student, mastery_type='question', target_code=str(question_id),
        defaults={'mastery_status': 'not_mastered', 'mastery_score': 0},
    )
    if is_correct:
        mr.mastery_score = min(100, float(mr.mastery_score) + 50)
        if mr.mastery_score >= 100:
            mr.mastery_status = 'mastered'
    else:
        mr.mastery_score = max(0, float(mr.mastery_score) - 20)
        mr.mastery_status = 'reviewing'
    mr.save()


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def variant_submit(request, item_id):
    """POST /api/v1/student/wrong-book/<item_id>/variant-submit
    Body: { question_id, answer_content, mode? }
    """
    trace_id = make_trace_id()
    question_id = request.data.get('question_id')
    answer_content = request.data.get('answer_content', {})

    # 校验错题归属
    try:
        item = WrongBookItem.objects.get(pk=item_id, student_user_id=request.user)
    except WrongBookItem.DoesNotExist:
        return Response({'code': 404, 'message': '错题不存在', 'data': None, 'trace_id': trace_id}, status=404)

    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': trace_id}, status=404)

    result = _grade(q, answer_content)
    is_pending = result is None
    is_correct = False if is_pending else result

    with transaction.atomic():
        attempt = AnswerAttempt.objects.create(
            student_user_id=request.user,
            mission_id=None, level_id=None,
            question_id=question_id,
            attempt_no=AnswerAttempt.objects.filter(
                student_user_id=request.user, question_id=question_id).count() + 1,
            answer_content=answer_content,
            is_correct=is_correct,
            is_subjective_pending=is_pending,
            score=100.00 if is_correct else 0.00,
            submit_source='variant',
        )
        # 错题状态推进：not_reviewed → reviewing(首次练) → mastered(累计练对≥3)
        if item.status == 'not_reviewed':
            item.status = 'reviewing'
        if is_correct and not is_pending:
            item.variant_done_count = (item.variant_done_count or 0) + 1
            if item.variant_done_count >= 3:
                item.status = 'mastered'
        item.save(update_fields=['variant_done_count', 'status'])
        _update_mastery(request.user, question_id, is_correct and not is_pending)

    if is_pending:
        feedback = '主观题已提交，等待老师批阅'
    elif is_correct:
        feedback = '做对了！该错题掌握度已提升。'
    else:
        feedback = '再想想，注意与原题的相同考点。'

    return Response({
        'code': 0, 'message': 'success',
        'data': {
            'is_correct': is_correct,
            'is_pending': is_pending,
            'score': float(attempt.score),
            'feedback': feedback,
            'variant_done_count': item.variant_done_count,
            'item_status': item.status,
        }, 'trace_id': trace_id,
    })
```

3) `apps/wrongbook/urls.py` 增加路由（wrongbook 专有）：
```python
from django.urls import path
from . import views
from . import variant_views

app_name = 'wrongbook'
urlpatterns = [
    path('', views.wrongbook_list, name='wrongbook-list'),
    path('<int:item_id>', views.wrongbook_detail, name='wrongbook-detail'),
    path('<int:item_id>/variants', views.wrongbook_variants, name='wrongbook-variants'),
    path('<int:item_id>/variant-submit', variant_views.variant_submit, name='variant-submit'),  # ← 新增
    path('mastery', views.mastery_list, name='mastery-list'),
]
```

> 注：`F3` 在 `AnswerAttempt` 加了 `is_subjective_pending`，`variant_views` 同步使用；如 F3 未先合入，此处去掉该字段即可（互不阻塞，但建议 F3 先行）。

#### 前端

1) `uniapp/src/api/student.ts` `wrongbookApi` 增加方法（共享文件，**追加**）：
```ts
export const wrongbookApi = {
  list: () => get('/student/wrong-book'),
  detail: (id: number) => get(`/student/wrong-book/${id}`),
  variants: (id: number) => get(`/student/wrong-book/${id}/variants`),
  // ↓ 新增
  variantSubmit: (itemId: number, data: { question_id: number; answer_content: object }) =>
    post(`/student/wrong-book/${itemId}/variant-submit`, data),
}
```

2) 新增独立做题页 `uniapp/src/pages/student/variant-practice.vue`（参照 `answer.vue` 结构，代码独立；接收 `itemId`，拉取变式题列表，逐题作答）：
```vue
<template>
  <view class="answer-page">
    <view class="question-panel">
      <view class="question-header">
        <text class="q-no">同类题 {{ currentIndex + 1 }}/{{ questions.length }}</text>
        <text class="q-type">{{ typeLabel(currentQuestion.question_type) }}</text>
      </view>

      <view v-if="currentQuestion.stem" class="stem">{{ currentQuestion.stem }}</view>

      <view v-if="isObjective" class="options-grid">
        <view v-for="opt in currentQuestion.options" :key="opt.label"
              class="option-card" :class="{ selected: selectedOptions.includes(opt.label) }"
              @click="toggleOption(opt.label)">
          <view class="option-label">{{ opt.label }}</view>
          <text class="option-content">{{ opt.content }}</text>
        </view>
      </view>

      <view v-else class="subjective-area">
        <textarea v-model="textAnswer" placeholder="请输入答案" class="text-input" />
      </view>

      <button class="submit-btn" @click="submit" :disabled="submitting">
        {{ submitting ? '提交中...' : '提交' }}
      </button>
    </view>

    <view class="feedback-panel">
      <view v-if="feedback" class="feedback-card" :class="feedbackType">
        <text class="feedback-title">
          {{ feedbackType === 'correct' ? '✅ 正确' : (feedbackType === 'pending' ? '⏳ 待批阅' : '❌ 不正确') }}
        </text>
        <text class="feedback-text">{{ feedback }}</text>
        <button v-if="!submitting" class="btn-next" @click="next">
          {{ hasNext ? '下一题' : '完成' }}
        </button>
      </view>
      <view v-else class="feedback-placeholder"><text>提交后显示结果</text></view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { wrongbookApi } from '@/api/student.ts'

interface Opt { label: string; content: string }
interface Q { id: number; question_type: string; stem?: string; options?: Opt[] }

const itemId = ref(0)
const questions = ref<Q[]>([])
const currentIndex = ref(0)
const selectedOptions = ref<string[]>([])
const textAnswer = ref('')
const feedback = ref('')
const feedbackType = ref('')
const submitting = ref(false)

const currentQuestion = computed(() => questions.value[currentIndex.value] || ({} as Q))
const isObjective = computed(() =>
  ['single_choice', 'multiple_choice'].includes(currentQuestion.value.question_type))
const hasNext = computed(() => currentIndex.value < questions.value.length - 1)

onMounted(async () => {
  const pages = getCurrentPages()
  const page = pages[pages.length - 1] as any
  itemId.value = parseInt(page.options.itemId || '0')
  if (!itemId.value) { uni.showToast({ title: '缺少错题ID', icon: 'none' }); return }
  try {
    const res = await wrongbookApi.variants(itemId.value)
    questions.value = res.data || []
    if (!questions.value.length) uni.showToast({ title: '暂无同类题', icon: 'none' })
  } catch { uni.showToast({ title: '加载失败', icon: 'none' }) }
})

function toggleOption(label: string) {
  const i = selectedOptions.value.indexOf(label)
  if (i >= 0) selectedOptions.value.splice(i, 1)
  else selectedOptions.value.push(label)
}

async function submit() {
  if (!currentQuestion.value.id) return
  submitting.value = true
  const answer_content = isObjective.value
    ? { selected_options: selectedOptions.value }
    : { text: textAnswer.value }
  try {
    const res = await wrongbookApi.variantSubmit(itemId.value, {
      question_id: currentQuestion.value.id, answer_content,
    })
    feedback.value = res.data?.feedback || ''
    feedbackType.value = res.data?.is_pending ? 'pending'
      : (res.data?.is_correct ? 'correct' : 'incorrect')
  } catch { uni.showToast({ title: '提交失败', icon: 'none' }) }
  finally { submitting.value = false }
}

function next() {
  feedback.value = ''; feedbackType.value = ''
  selectedOptions.value = []; textAnswer.value = ''
  if (hasNext.value) currentIndex.value++
  else uni.navigateBack()
}

function typeLabel(t: string) {
  return ({ single_choice: '单选', multiple_choice: '多选', fill_blank: '填空',
    short_answer: '简答', essay: '论述', computation: '计算', proof: '证明',
    true_false: '判断' } as Record<string, string>)[t] || t
}
</script>

<style scoped>
/* 复用 answer.vue 的样式即可（.answer-page/.question-panel/.options-grid/.option-card/
   .subjective-area/.text-input/.submit-btn/.feedback-panel/.feedback-card 等），
   为保持代码独立，直接拷贝一份到此文件，不 import answer.vue。 */
.answer-page { display: flex; min-height: 100vh; background: #f0f2f5; }
.question-panel { flex: 1; padding: 30rpx 40rpx; }
.question-header { display: flex; justify-content: space-between; margin-bottom: 20rpx; }
.q-no { font-size: 24rpx; color: #999; } .q-type { font-size: 28rpx; font-weight: bold; color: #333; }
.stem { font-size: 28rpx; color: #333; line-height: 1.6; margin-bottom: 24rpx; }
.options-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 16rpx; margin-bottom: 30rpx; }
.option-card { border: 2rpx solid #ddd; border-radius: 12rpx; padding: 24rpx; background:#fff; }
.option-card.selected { border-color:#409eff; background:#ecf5ff; }
.option-label { width:40rpx;height:40rpx;border-radius:50%;background:#eee;
  display:flex;align-items:center;justify-content:center;font-size:22rpx;font-weight:bold;margin-bottom:12rpx;}
.option-card.selected .option-label { background:#409eff;color:#fff; }
.option-content { font-size:26rpx;color:#333; }
.subjective-area { margin-bottom: 30rpx; }
.text-input { width:100%;min-height:300rpx;border:1rpx solid #ddd;border-radius:12rpx;
  padding:20rpx;box-sizing:border-box;background:#fff;font-size:26rpx; }
.submit-btn { background:#409eff;color:#fff;font-size:28rpx;padding:20rpx 0;border-radius:8rpx; }
.submit-btn[disabled]{background:#ccc;}
.feedback-panel { width:340px;padding:30rpx 24rpx;background:#fff;border-left:1rpx solid #e8e8e8;display:flex;align-items:center; }
.feedback-card { padding:30rpx;border-radius:12rpx;width:100%; }
.feedback-card.correct{background:#e8f5e9;} .feedback-card.incorrect{background:#fff3e0;} .feedback-card.pending{background:#eef6ff;}
.feedback-title{font-size:28rpx;font-weight:bold;display:block;margin-bottom:12rpx;}
.feedback-text{font-size:24rpx;color:#333;display:block;margin-bottom:20rpx;line-height:1.6;}
.btn-next{background:#4caf50;color:#fff;font-size:24rpx;}
.feedback-placeholder{text-align:center;color:#ccc;font-size:26rpx;}
@media (max-width:768px){ .answer-page{flex-direction:column;} .feedback-panel{width:100%;border-left:none;border-top:1rpx solid #e8e8e8;} .options-grid{grid-template-columns:1fr;} }
</style>
```

3) `uniapp/src/pages/student/wrongbook-variants.vue` 修复跳转（学生端专有）：模板 `@click="startPractice(item.id)"` → `@click="startPractice()"`，并替换函数：
```ts
function startPractice() {
  // 进入独立“同类题练习”页，传错题 id
  uni.navigateTo({ url: `/pages/student/variant-practice?itemId=${wrongId.value}` })
}
```
（变式列表页卡片模板无需改——后端现已返回完整 `stem/options`，正常展示题干。）

4) `uniapp/src/pages.json` 注册新页（共享文件，**追加一项**）：
在 `pages` 数组中学生页区间追加：
```json
{
  "path": "pages/student/variant-practice",
  "style": { "navigationBarTitleText": "同类题练习" }
}
```

**联调验证点**：
1. 错题本进入某错题 → 同类题 → 点"开始练习" → 进入 `variant-practice` 页，题目带题干/选项。
2. 作答客观题：答对 → `variant_done_count +1`、掌握度提升；答错 → 计数不变。
3. 作答主观题 → 显示"待批阅"，不计入对错。
4. 同一道原错题练对 ≥3 次 → `wrong_book_item.status` 推进为 `mastered`；`mastery_record` 同步。

**涉及文件**：新建 `apps/wrongbook/variant_views.py`、`uniapp/src/pages/student/variant-practice.vue`；改 `apps/wrongbook/services.py`、`apps/wrongbook/urls.py`、`uniapp/src/api/student.ts`、`uniapp/src/pages/student/wrongbook-variants.vue`、`uniapp/src/pages.json`。

---

### F5. 任务详情越权修复（P1·后端）

**现状证据**：`apps/study/student_views.py:77` `LearningMission.objects.get(pk=mission_id, status='published')`，不校验该学生是否有该任务的进度 → 知道 mission_id 即可跨班查看。

**方案**：增加"该学生存在对应 `StudentMissionProgress`"的校验。

**数据库**：无变更。

**后端代码**（学生端专有，`student_mission_detail` 开头追加校验）：
```python
@api_view(['GET'])
@permission_classes([IsAuthenticated])
def student_mission_detail(request, mission_id):
    try:
        mission = LearningMission.objects.get(pk=mission_id, status='published')
    except LearningMission.DoesNotExist:
        return Response({'code': 404, 'message': '任务不存在或未发布', 'data': None, 'trace_id': make_trace_id()}, status=404)

    # ← 新增：校验学生有权访问（存在进度记录）
    if not StudentMissionProgress.objects.filter(
            mission=mission, student_user_id=request.user).exists():
        return Response({'code': 403, 'message': '无权访问该任务', 'data': None, 'trace_id': make_trace_id()}, status=403)

    levels = []
    ...  # 其余不变
```

> 同理建议对 `student_level_detail` 增加校验：该 level 所属 mission 必须存在该学生的进度（防止直接用 level_id 越权）。可一并加。

**联调验证点**：学生 A 用 B 班任务的 mission_id 请求 → 403；用自己任务 → 正常。

**涉及文件**：改 `apps/study/student_views.py`（学生端专有）。

---

### F6. 学生接口角色校验（P1·后端）

**现状证据**：所有 `/api/v1/student/*` 视图仅 `IsAuthenticated`，不校验 `role_type=='student'`（`apps/study/student_views.py:22`、`apps/wrongbook/views.py:16` 等）。

**方案**：新增 DRF 权限类 `IsStudent`，挂到每个学生视图。**不用中间件**——本项目鉴权是 JWT（`OptionalJWTAuthentication`，`config/settings.py:122`），DRF 在视图层才解析 Bearer token（晚于 Django 中间件），中间件里 `request.user` 仍是 `AnonymousUser`，用中间件判角色会把合法学生也 403；DRF 权限类在鉴权之后执行，能拿到真实 user。

**数据库**：无变更。

**后端代码**：

新增 `apps/study/permissions.py`（新文件）：
```python
from rest_framework import permissions


class IsStudent(permissions.BasePermission):
    """仅允许 role_type=='student'。"""
    def has_permission(self, request, view):
        return getattr(request.user, 'role_type', None) == 'student'
```

给每个学生视图把 `@permission_classes([IsAuthenticated])` 改为 `@permission_classes([IsAuthenticated, IsStudent])` 并 `from apps.study.permissions import IsStudent`：
- `apps/study/student_views.py`（6 个视图）、`apps/study/answer_views.py`（3）、`apps/study/guidance_views.py`（2）、`apps/wrongbook/views.py`（4）
- 新建的 `apps/wrongbook/variant_views.py`、`apps/study/knowledge_views.py`：创建时即带上

**联调验证点**：用教师 token 调 `/api/v1/student/home` → 403；学生 token → 正常；未带 token → 401。

**涉及文件**：新建 `apps/study/permissions.py`；改 `apps/study/{student_views,answer_views,guidance_views}.py`、`apps/wrongbook/views.py`（均为学生端专有，**不涉及共享文件**）。

---

### F7. AI 辅导接入真实数据（P1·前后端）

**现状证据**：`apps/study/guidance_views.py:56` C 模式硬编码 `'你觉得这道题的关键条件是什么？'`；`:107` 回复固定 `'注意观察图形中的关键信息'`，3 轮强制结束。**未使用**题目真实的 `ai_answer_b/c`，也**未调用** LLM。

**参考实现**：教师端 `apps/missions/views.py:420-604` `start_teacher_guidance/teacher_guidance_reply` 已是真实实现：B 模式用 `ai_answer_b.options/hint`，C 模式用 `ai_answer_c.first_question/steps` 并以 `_call_qwen` 真实评价学生回答。学生端照此范式独立实现，并把会话持久化到 `AIGuidanceSession`（替代教师端的内存字典）。

**数据库**：无变更（`ai_guidance_session.content_log_json` 存对话步骤索引）。

#### 后端

新增 `apps/study/ai_helper.py`（新文件，独立 LLM 调用，与教师端 `_call_qwen` 同款但学生端专用）：
```python
"""学生端引导：轻量 Qwen 调用。"""
import os
import httpx

QWEN_API_URL = 'https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions'


def call_qwen_for_guidance(system_prompt: str, user_prompt: str, model: str = 'qwen3.6-flash') -> str:
    """对学生回复做简短评价。失败返回安全兜底文案（非异常）。"""
    api_key = os.environ.get('QWEN_API_KEY', '')
    if not api_key:
        return '（AI 暂不可用）'
    headers = {'Authorization': f'Bearer {api_key}', 'Content-Type': 'application/json'}
    payload = {'model': model, 'messages': [
        {'role': 'system', 'content': system_prompt},
        {'role': 'user', 'content': user_prompt}],
        'max_tokens': 300, 'temperature': 0.7}
    try:
        with httpx.Client(timeout=60.0, trust_env=False) as client:
            resp = client.post(QWEN_API_URL, json=payload, headers=headers)
            resp.raise_for_status()
            return resp.json()['choices'][0]['message']['content']
    except Exception as e:
        return f'（AI 评价暂时不可用：{e.__class__.__name__}）'
```

重写 `apps/study/guidance_views.py`（学生端专有文件，替换桩实现；签名/路由不变，前端无需改调用）：
```python
import json
import uuid
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from apps.study.models import AIGuidanceSession
from .ai_helper import call_qwen_for_guidance


def make_trace_id():
    return uuid.uuid4().hex[:16]


def _as_dict(field):
    if not field:
        return {}
    if isinstance(field, dict):
        return field
    try:
        return json.loads(field)
    except Exception:
        return {}


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def start_guidance(request):
    """S-06: 启动引导，使用题目真实 ai_answer_b/c。"""
    question_id = request.data.get('question_id')
    mode_type = request.data.get('mode_type', 'B')
    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    ai_b = _as_dict(q.ai_answer_b)
    ai_c = _as_dict(q.ai_answer_c)

    session = AIGuidanceSession.objects.create(
        student_user_id=request.user, question_id=question_id,
        mode_type=mode_type, session_status='running',
        content_log_json={'step_index': 0, 'steps': []},
    )

    if mode_type == 'B':
        options = ai_b.get('options') or []
        hint = ai_b.get('hint') or '请仔细阅读题干，思考关键条件。'
        return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                         'data': {'session_id': session.id, 'mode': 'B', 'hint': hint, 'options': options}})

    # C 模式：取真实首问
    first_q = ai_c.get('first_question') or ai_c.get('question') or '请从题干中找出你认为最关键的一个条件，并说明理由。'
    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'session_id': session.id, 'mode': 'C', 'question': first_q}})


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def guidance_reply(request, session_id):
    """S-07: 真实推进引导（B 走脚本，C 调 LLM 评价 + 步进 ai_answer_c.steps）。"""
    try:
        session = AIGuidanceSession.objects.get(pk=session_id, student_user_id=request.user)
    except AIGuidanceSession.DoesNotExist:
        return Response({'code': 404, 'message': '引导会话不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)
    if session.session_status != 'running':
        return Response({'code': 4001, 'message': '引导已结束', 'data': None, 'trace_id': make_trace_id()}, status=400)

    user_reply = (request.data.get('reply') or '').strip()
    try:
        q = ExamQuestion.objects.get(pk=session.question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    log = session.content_log_json or {'step_index': 0, 'steps': []}
    log.setdefault('steps', []).append({'user': user_reply})
    log['step_index'] = log.get('step_index', 0) + 1
    step_index = log['step_index']

    # C→B 降级（连续无效输入）
    if session.mode_type == 'C' and len(user_reply) < 3:
        session.invalid_input_count += 1
        if session.invalid_input_count >= 2:
            session.mode_type = 'B'
            session.session_status = 'downgraded'
            session.content_log_json = log
            session.save()
            return Response({'code': 0, 'message': '已降级到B模式', 'trace_id': make_trace_id(),
                             'data': {'mode': 'B', 'reason': '连续无效输入', 'is_completed': False}})

    ai_b = _as_dict(q.ai_answer_b)
    ai_c = _as_dict(q.ai_answer_c)

    if session.mode_type == 'B':
        next_hint = ai_b.get('next_hint') or ai_b.get('hint') or '很好，继续思考下一个关键点。'
        is_completed = step_index >= 3
        if is_completed:
            next_hint = '引导完成！可以返回做题页继续。'
        session.content_log_json = log
        session.session_status = 'completed' if is_completed else 'running'
        session.save()
        return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                         'data': {'next_hint': next_hint, 'is_completed': is_completed, 'mode': 'B'}})

    # C 模式：真实 LLM 评价 + 步进 ai_answer_c.steps
    evaluation = call_qwen_for_guidance(
        '你是一位耐心的老师，对学生回答给出 1-2 句简明评价与鼓励。',
        f'题目：{q.stem}\n参考答案：{q.answer or "见解析"}\n学生回答：{user_reply}\n请评价。',
    )
    steps = ai_c.get('steps') or ai_c.get('dialogue') or []
    next_question = None
    if step_index < len(steps):
        st = steps[step_index]
        next_question = st.get('question') or st.get('prompt') if isinstance(st, dict) else st
    if not next_question:
        next_question = '引导完成！你已经很好地理解了这道题。' if step_index >= 3 else '很好，请继续思考下一步。'
    is_completed = step_index >= 3

    session.content_log_json = log
    session.session_status = 'completed' if is_completed else 'running'
    session.save()

    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'evaluation': evaluation, 'next_hint': next_question,
                              'next_question': next_question, 'is_completed': is_completed, 'mode': 'C'}})
```

> 关键：所有内容来自 `ai_answer_b/c`（教师 AI 流水线真实生成）与 `call_qwen_for_guidance`（真实 LLM），**无硬编码假数据**；`ai_answer_b/c` 缺失时仅给"操作引导性"兜底文案（非题目答案）。

#### 前端

`uniapp/src/pages/student/guidance.vue` 渲染 C 模式评价（学生端专有，`sendReplyInternal` 内追加）：
```ts
async function sendReplyInternal(reply: string) {
  try {
    const res = await studentApi.guidanceReply(sessionId.value, reply)
    // C 模式：优先显示真实评价，再显示下一个引导问题
    const seg: string[] = []
    if (res.data?.evaluation) seg.push(`老师评价：${res.data.evaluation}`)
    const hint = res.data?.next_hint || res.data?.next_question
    if (hint) seg.push(hint)
    if (seg.length) messages.value.push({ role: 'system', content: seg.join('\n\n') })
    if (res.data?.is_completed) isCompleted.value = true
    if (res.data?.mode === 'B' && res.data?.reason) {
      mode.value = 'B'
      messages.value.push({ role: 'system', content: `已降级到B模式：${res.data.reason}` })
    }
  } catch { uni.showToast({ title: '发送失败', icon: 'none' }) }
}
```

**联调验证点**：
- 选一道已跑过 AI 流水线（`ai_answer_b/c` 非空）的题；B 模式出现的选项/提示应来自数据库真实数据；C 模式回复后出现真实 LLM 评价 + 下一步问题。
- 连续两次输入 <3 字 → 自动降级 B。

**涉及文件**：新建 `apps/study/ai_helper.py`；改 `apps/study/guidance_views.py`、`uniapp/src/pages/student/guidance.vue`。

---

### F8. PDF 导出真化（P2·后端）

**现状证据**：`apps/study/student_views.py:194-329` `_build_html` + 写 `.html`，注释明说"待 reportlab 可用后替换"。

**方案**：引入 `reportlab`，用其内置 CJK CID 字体 `STSong-Light`（无需字体文件），把 `_build_html` 的等价内容画成真 PDF；接口签名/响应不变，仅文件扩展名 `.pdf`。
**避坑**：reportlab `Paragraph` 只认其迷你 XML、不认 HTML，故题干用**纯文本 `stem`**（下方代码已采用），否则 `stem_html` 的 `<p>/<img>/&nbsp;` 等会让导出 500。
**可选·对齐需求文档**：`EXPORT_RATE_LIMIT` 由 10 改 3（次/小时）、`export_pdf` 内加 `questions_data = questions_data[:50]`（max 50 题/PDF）。

**数据库**：无变更。

**依赖**：`requirements.txt` 末尾追加（共享文件）：
```
reportlab>=4.0,<5.0
```

**后端代码**（学生端专有；新增 `_build_pdf` 替换 `_build_html` + 写文件段，其余 `export_pdf` 逻辑不变）：

```python
def _build_pdf(export_type: str, questions: list, include_answers: bool) -> bytes:
    """用 reportlab 生成 PDF（内置 STSong-Light 支持中文）。"""
    from reportlab.lib.pagesizes import A4
    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer
    from reportlab.pdfbase import pdfmetrics
    from reportlab.pdfbase.cidfonts import UnicodeCIDFont
    from reportlab.lib.units import mm
    import io

    pdfmetrics.registerFont(UnicodeCIDFont('STSong-Light'))
    buf = io.BytesIO()
    doc = SimpleDocTemplate(buf, pagesize=A4,
                            topMargin=15*mm, bottomMargin=15*mm,
                            leftMargin=15*mm, rightMargin=15*mm)
    styles = getSampleStyleSheet()
    h1 = ParagraphStyle('ZhH1', parent=styles['Title'], fontName='STSong-Light', fontSize=18)
    body = ParagraphStyle('ZhBody', parent=styles['Normal'], fontName='STSong-Light', fontSize=11, leading=18)
    story = []

    type_label = '错题本' if export_type == 'wrongbook' else '任务题目'
    story.append(Paragraph(type_label, h1))
    story.append(Spacer(1, 6*mm))
    story.append(Paragraph(f'导出时间：{timezone.now().strftime("%Y-%m-%d %H:%M:%S")}　题目数：{len(questions)}', body))
    story.append(Spacer(1, 6*mm))

    for i, q in enumerate(questions, 1):
        qtype = ExamQuestion.QUESTION_TYPE_LABELS.get(q['question_type'], q['question_type'])
        story.append(Paragraph(f'第{i}题（{qtype}）', body))
        story.append(Paragraph(q['stem'] or '', body))  # 纯文本，避免 stem_html 的 HTML 让 reportlab 报错
        for opt in q.get('options_html', []):
            story.append(Paragraph(f'{opt["label"]}. {opt["content"]}', body))
        if include_answers:
            if q.get('answer'):
                story.append(Paragraph(f'<b>答案：</b>{q["answer"]}', body))
            if q.get('analysis'):
                story.append(Paragraph(f'<b>解析：</b>{q["analysis"]}', body))
        story.append(Spacer(1, 4*mm))

    doc.build(story)
    return buf.getvalue()
```

`export_pdf` 中替换"生成文件"段（写文件名改 `.pdf`、内容用 `_build_pdf`）：
```python
    # --- generate file ---
    export_dir = os.path.join(settings.MEDIA_ROOT, 'exports')
    os.makedirs(export_dir, exist_ok=True)
    filename = f'{export_type}_{uuid.uuid4().hex[:12]}.pdf'        # ← .html 改 .pdf
    filepath = os.path.join(export_dir, filename)

    pdf_bytes = _build_pdf(export_type, questions_data, include_answers)   # ← 真实 PDF
    with open(filepath, 'wb') as f:
        f.write(pdf_bytes)

    download_url = f'{settings.MEDIA_URL}exports/{filename}'
    return Response({
        'code': 0, 'message': '导出成功',
        'data': {'download_url': download_url, 'question_count': len(questions_data)},
        'trace_id': trace_id,
    })
```

> 前端 `export.vue` 无需改动（仍用 `download_url` 下载，浏览器直接打开 PDF）。

**联调验证点**：调用 `POST /api/v1/student/export/pdf` → 下载到的文件为 `.pdf`，中文正常显示，含题目/选项/（可选）答案解析。

**涉及文件**：改 `apps/study/student_views.py`、`requirements.txt`（共享，追加 1 行）。

---

### F9. 学生知识图谱 / 知识掌握度（P2·前后端）

**现状证据**：`uniapp/src/pages/student/knowledge-graph.vue:11-12` 仅"功能开发中 / 敬请期待"；无学生侧后端接口。

**方案（最小真实版）**：不画复杂图谱，先做"**按知识点的掌握度列表**"——从学生已有 `AnswerAttempt` 关联 `ExamQuestion.knowledge_points` 实时聚合正确率（真实数据，无需额外写入）。后续可在同一数据上叠加可视化。

**数据库**：无变更（`ExamQuestion.knowledge_points` 为 JSON，存 `[{'id','module'},...]`）。

#### 后端

新增 `apps/study/knowledge_views.py`（新文件）：
```python
"""学生知识掌握度：按知识点聚合作答正确率。"""
import uuid
from collections import defaultdict
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from .models import AnswerAttempt


def make_trace_id():
    return uuid.uuid4().hex[:16]


@api_view(['GET'])
@permission_classes([IsAuthenticated])
def knowledge_mastery(request):
    """GET /api/v1/student/knowledge-mastery
    返回每个知识点的 attempt/correct/accuracy（排除主观题待批阅）。
    """
    attempts = AnswerAttempt.objects.filter(
        student_user_id=request.user, is_subjective_pending=False
    ).select_related(None)
    # 取题目知识点
    qids = [a.question_id for a in attempts]
    q_map = {q.id: q for q in ExamQuestion.objects.filter(id__in=qids)}

    agg = defaultdict(lambda: {'attempt': 0, 'correct': 0})
    for a in attempts:
        q = q_map.get(a.question_id)
        if not q:
            continue
        kps = q.knowledge_points or []
        for kp in kps:
            label = (kp.get('module') if isinstance(kp, dict) else str(kp)) or '未分类'
            agg[label]['attempt'] += 1
            if a.is_correct:
                agg[label]['correct'] += 1

    items = []
    for label, v in agg.items():
        acc = round(v['correct'] / max(v['attempt'], 1) * 100, 1)
        items.append({
            'knowledge': label,
            'attempt': v['attempt'],
            'correct': v['correct'],
            'accuracy': acc,
            'mastery': 'mastered' if acc >= 85 else ('reviewing' if acc >= 60 else 'weak'),
        })
    items.sort(key=lambda x: x['accuracy'])  # 弱项在前

    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'items': items, 'total': len(items)}})
```

`apps/study/student_urls.py` 增加路由（学生端专有）：
```python
from .knowledge_views import knowledge_mastery   # ← 新增 import
urlpatterns = [
    ...
    path('knowledge-mastery', knowledge_mastery, name='knowledge-mastery'),   # ← 新增
]
```

#### 前端

`uniapp/src/api/student.ts` `studentApi` 追加（共享文件，追加）：
```ts
  knowledgeMastery: () => get('/student/knowledge-mastery'),
```

重写 `uniapp/src/pages/student/knowledge-graph.vue`（学生端专有，去掉占位文案，渲染真实列表）：
```vue
<template>
  <view class="kg-page">
    <view class="nav"><text class="title">知识掌握度</text></view>
    <view v-if="loading" class="hint">加载中...</view>
    <view v-else-if="!items.length" class="hint">暂无作答数据，先去做几道题吧～</view>
    <view v-else class="list">
      <view v-for="it in items" :key="it.knowledge" class="card">
        <view class="row">
          <text class="kp">{{ it.knowledge }}</text>
          <text :class="['badge', it.mastery]">{{ masteryLabel(it.mastery) }}</text>
        </view>
        <view class="bar"><view class="bar-inner" :style="{ width: it.accuracy + '%' }" :class="it.mastery"></view></view>
        <text class="meta">正确率 {{ it.accuracy }}% · 作答 {{ it.attempt }} · 正确 {{ it.correct }}</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { studentApi } from '@/api/student.ts'

const items = ref<any[]>([])
const loading = ref(true)

onMounted(async () => {
  try {
    const res = await studentApi.knowledgeMastery()
    items.value = res.data?.items || []
  } finally { loading.value = false }
})

function masteryLabel(m: string) {
  return ({ mastered: '已掌握', reviewing: '巩固中', weak: '薄弱' } as Record<string,string>)[m] || m
}
</script>

<style scoped>
.kg-page { min-height:100vh; background:#f0f2f5; }
.nav { padding:24rpx 32rpx; background:#fff; border-bottom:1rpx solid #eee; }
.title { font-size:32rpx; font-weight:bold; }
.hint { text-align:center; color:#999; padding:160rpx 0; font-size:26rpx; }
.list { padding:24rpx; }
.card { background:#fff; border-radius:12rpx; padding:24rpx; margin-bottom:20rpx; }
.row { display:flex; justify-content:space-between; align-items:center; margin-bottom:16rpx; }
.kp { font-size:28rpx; font-weight:bold; color:#333; }
.badge { font-size:22rpx; padding:4rpx 16rpx; border-radius:20rpx; }
.badge.mastered{background:#e8f5e9;color:#4caf50;}
.badge.reviewing{background:#fff3e0;color:#ff9800;}
.badge.weak{background:#ffebee;color:#f44336;}
.bar { height:12rpx; background:#eee; border-radius:6rpx; overflow:hidden; margin-bottom:12rpx; }
.bar-inner { height:100%; }
.bar-inner.mastered{background:#4caf50;} .bar-inner.reviewing{background:#ff9800;} .bar-inner.weak{background:#f44336;}
.meta { font-size:22rpx; color:#999; }
</style>
```

**联调验证点**：学生做完若干题后访问 → 按知识点列出正确率与掌握等级，弱项排前；无作答时显示空态。

**涉及文件**：新建 `apps/study/knowledge_views.py`；改 `apps/study/student_urls.py`、`uniapp/src/api/student.ts`、`uniapp/src/pages/student/knowledge-graph.vue`。

---

## 5. 全量涉及文件清单（新建 / 修改 / 归属）

### 5.1 新建文件（零冲突）

| 文件 | 功能 | 归属 |
|---|---|---|
| `apps/study/receivers.py` | F1 信号回填 | 学生端 |
| `apps/study/migrations/0003_answerattempt_is_subjective_pending.py` | F3 迁移 | 学生端 |
| `apps/study/ai_helper.py` | F7 LLM 调用 | 学生端 |
| `apps/study/knowledge_views.py` | F9 知识掌握度 | 学生端 |
| `apps/wrongbook/variant_views.py` | F4 变式提交 | 学生端 |
| `apps/study/permissions.py` | F6 角色权限类 | 学生端 |
| `uniapp/src/pages/student/variant-practice.vue` | F4 同类题做题页 | 学生端 |

### 5.2 修改文件（学生端专有，对方不改）

| 文件 | 改动点 | 功能 |
|---|---|---|
| `apps/study/apps.py` | 新增 `ready()` 注册信号 | F1 |
| `apps/study/models.py` | `AnswerAttempt` + `is_subjective_pending` | F3 |
| `apps/study/answer_views.py` | `_check_answer`/`_handle_submit_answer` 三态 | F3 |
| `apps/study/student_views.py` | `growth_summary` 排除待批阅；`student_mission_detail` 加权限 | F3/F5 |
| `apps/study/guidance_views.py` | 重写为真实数据+LLM | F7 |
| `apps/study/student_urls.py` | +`knowledge-mastery` 路由 | F9 |
| `apps/wrongbook/services.py` | `find_variant_questions` 返回完整字段 | F4 |
| `apps/wrongbook/urls.py` | +`variant-submit` 路由 | F4 |
| `apps/wrongbook/views.py` | +IsStudent | F6 |
| `uniapp/src/pages/student/answer.vue` | 反馈区支持"待批阅" | F3 |
| `uniapp/src/pages/student/guidance.vue` | 渲染 C 模式评价 | F7 |
| `uniapp/src/pages/student/wrongbook-variants.vue` | `startPractice` 改跳 variant-practice | F4 |
| `uniapp/src/pages/student/knowledge-graph.vue` | 重写为真实掌握度 | F9 |

### 5.3 共享文件（追加式，提交前 @ 对方）

| 文件 | 改动点 | 功能 |
|---|---|---|
| `requirements.txt` | 末尾 +`reportlab>=4.0,<5.0` | F8 |
| `uniapp/src/api/student.ts` | `wrongbookApi.variantSubmit`、`studentApi.knowledgeMastery` | F4/F9 |
| `uniapp/src/pages.json` | `pages` 末尾 +`variant-practice` 页 | F4 |

### 5.4 教师端文件（对方实施，我方只给片段）

| 文件 | 改动点 | 功能 |
|---|---|---|
| `apps/missions/views.py` | `mission_clone_with_class` 末尾追加发布+fan-out | F2 |

---

## 6. 实施顺序与端到端测试清单

### 6.1 建议顺序（按依赖与优先级）
1. **F3**（主观题三态 + 迁移）—— 其余多处依赖 `is_subjective_pending`，先合。
2. **F1**（入班回填信号）—— 独立、P0。
3. **F5 + F6**（权限加固）—— 安全，独立。
4. **F4**（变式题闭环）—— 含前端新页，体量最大。
5. **F7**（AI 辅导真数据）—— 含 LLM 调用。
6. **F9**（知识掌握度）。
7. **F8**（PDF，需新依赖 reportlab）。
8. **F2**（交对方，跟踪闭环）。

### 6.2 端到端冒烟脚本（学生账号）
前置：教师账号建班 → 建任务(关关卡/题) → 发布；学生账号加入该班。

| 步骤 | 期望 |
|---|---|
| 学生 `GET /student/home` | 能看到任务（F1：后入班也能看到） |
| `GET /student/missions/<id>` 用**别班** id | 403（F5） |
| 进入关卡答客观题（错） | 进错题本、显示 ❌、正确率分母 +1（F3） |
| 答主观题 | 显示 ⏳ 待批阅、不进错题本、正确率分母不变（F3） |
| 错题本 → 同类题 → 开始练习 | 进入 `variant-practice`，题目有题干选项（F4） |
| 同类题答对 3 次 | `variant_done_count=3`、`status=mastered`、`mastery_record` 更新（F4） |
| 答错后进引导(B/C) | B 选项/C 评价来自真实 `ai_answer_b/c` 与 LLM，无"注意观察图形"硬编码（F7） |
| 教师账号调 `/student/home` | 403（F6） |
| 导出错题本 | 下载到 `.pdf`、中文正常（F8） |
| 知识图谱页 | 列出各知识点正确率与等级，弱项在前（F9） |

### 6.3 回归检查（确保不破坏既有）
- 客观题作答、错题自动入库、关卡进度、成长统计在 F3 后行为一致（仅主观题分支变化）。
- `join_by_code` / `quit_class` / `mission_publish` 原有行为不变（F1 仅新增信号）。
- 教师端任务管理、试卷解析、审校链路不受影响（未改教师端代码）。

---

## 附：风险与备注
- F3 迁移仅加可空布尔列，对历史数据无影响（默认 False）。
- F7 依赖 `QWEN_API_KEY`（`.env` 已配）；未配置时 `call_qwen_for_guidance` 返回安全兜底，不抛异常、不阻塞流程。
- F8 `reportlab` 的 `STSong-Light` 为内置 CID 字体，无需随仓库附带字体文件；如需其他字体再行扩展。
- F9 的知识点聚合依赖 `ExamQuestion.knowledge_points` 已被 AI 流水线回填；未回填的题会归到"未分类"。
- 全程未修改教师端业务代码、未删除任何既有函数；既有视图函数体仅在 F3/F5/F7 内做了"替换函数体/前插校验"的最小必要改动。
