# 阶段 2 · 体验与安全（F7 → F6 → F5）细化执行清单

> 前置：阶段 1（F3/F1/F4）已完成并通过冒烟。
> 本文档只产出执行步骤，不改代码；每步可独立验证、可回滚。
> 严格遵循：独立、最小改动、风格一致、无假数据、不破坏既有、文件归属标注。

---

## 0. 前置与约定

> **执行与提交纪律**：直接在 `main` 分支开发，不切分支；执行者一律不 `git add` / `git commit` / `git push`，改动保留为工作区未提交状态，**由用户审阅后手动提交**；`main` 现有未提交改动保持不动、照常叠加 F 改动。

### 0.1 环境
- 后端 `venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001`（改完重启）；Docker `qisi-postgres` 已起；前端 `npm run dev:h5`。
- `.env` 中 `QWEN_API_KEY` 已配置（F7 真实 LLM 调用依赖）。

### 0.2 测试数据准备
A. **学生 token**（沿用阶段 1 0.2，19513916986）。
B. **教师 token**（用于 F6/F5 负向测试）——shell：
```python
venv/Scripts/python.exe manage.py shell
>>> from apps.accounts.models import UserAccount
>>> from rest_framework_simplejwt.tokens import RefreshToken
>>> t, _ = UserAccount.objects.get_or_create(mobile='15883633570', defaults={'role_type':'teacher','display_name':'测试教师'})
>>> print('TEACHER_TOKEN', str(RefreshToken.for_user(t).access_token))
```
C. **找一道已生成 ai_answer_b/c 的题**（F7 用，需教师端 AI 流水线跑过）：
```python
>>> from apps.parser.models import ExamQuestion
>>> q = ExamQuestion.objects.exclude(ai_answer_b__isnull=True).exclude(ai_answer_b={}).first()
>>> print('GUIDE_QID', q.id if q else None)
```
> 若无此类题：F7 的 B 模式会回退到通用引导文案、C 模式回退到首问默认值（仍非硬编码"注意观察图形"），但为充分验证建议先用教师端给 1~2 道题跑一次 AI 生成。

### 0.3 本阶段 DoD
- ✅ AI 辅导 B 模式选项/提示来自题目真实 `ai_answer_b`；C 模式调用真实 LLM 评价 + 步进 `ai_answer_c.steps`，**无任何硬编码假数据**。
- ✅ `/api/v1/student/*` 仅学生角色可访问，教师/其它角色 token 一律 403。
- ✅ 学生只能访问自己有进度的任务/关卡，跨班级 mission_id 返回 403。
- ✅ 既有学生功能无回归；未改教师端业务代码。

---

## F7. AI 辅导接入真实数据

**问题**：`apps/study/guidance_views.py:56` C 模式硬编码 `'你觉得这道题的关键条件是什么？'`；`:107` 回复固定 `'注意观察图形中的关键信息'`，3 轮强制结束。未用真实 `ai_answer_b/c`，未调 LLM。
**参考**：教师端 `apps/missions/views.py:420-604` 已有真实实现（`ai_answer_b.options/hint`、`ai_answer_c.first_question/steps`、`_call_qwen` 评价）。学生端照此范式独立实现，并用 `AIGuidanceSession` 持久化（替代教师端内存字典）。
**涉及文件**（学生端专有）：
- 新建 `apps/study/ai_helper.py`
- 改 `apps/study/guidance_views.py`（重写两个视图函数体）
- 改 `uniapp/src/pages/student/guidance.vue`（渲染 C 模式评价）

### 步骤 1.1 　新建 ai_helper.py
文件：`apps/study/ai_helper.py`（新文件，完整内容）：
```python
"""学生端引导：轻量 Qwen 调用（独立于教师端 _call_qwen）。"""
import os
import httpx

QWEN_API_URL = 'https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions'


def call_qwen_for_guidance(system_prompt: str, user_prompt: str, model: str = 'qwen3.6-flash') -> str:
    """对学生回复做简短评价。失败返回安全兜底文案（不抛异常，不阻塞流程）。"""
    api_key = os.environ.get('QWEN_API_KEY', '')
    if not api_key:
        return '（AI 暂不可用）'
    headers = {'Authorization': f'Bearer {api_key}', 'Content-Type': 'application/json'}
    payload = {'model': model, 'messages': [
        {'role': 'system', 'content': system_prompt},
        {'role': 'user', 'content': user_prompt}],
        'max_tokens': 300, 'temperature': 0.7}
    try:
        with httpx.Client(timeout=60.0, trust_env=False) as client:
            resp = client.post(QWEN_API_URL, json=payload, headers=headers)
            resp.raise_for_status()
            return resp.json()['choices'][0]['message']['content']
    except Exception as e:
        return f'（AI 评价暂时不可用：{e.__class__.__name__}）'
```

### 步骤 1.2 　重写 guidance_views.py
文件：`apps/study/guidance_views.py`，**整文件替换**为（签名/路由不变，前端调用不改）：
```python
import json
import uuid
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from apps.parser.models import ExamQuestion
from apps.study.models import AIGuidanceSession
from .ai_helper import call_qwen_for_guidance


def make_trace_id():
    return uuid.uuid4().hex[:16]


def _as_dict(field):
    if not field:
        return {}
    if isinstance(field, dict):
        return field
    try:
        return json.loads(field)
    except Exception:
        return {}


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def start_guidance(request):
    """S-06: 启动引导，使用题目真实 ai_answer_b/c。"""
    question_id = request.data.get('question_id')
    mode_type = request.data.get('mode_type', 'B')
    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    ai_b = _as_dict(q.ai_answer_b)
    ai_c = _as_dict(q.ai_answer_c)

    session = AIGuidanceSession.objects.create(
        student_user_id=request.user, question_id=question_id,
        mode_type=mode_type, session_status='running',
        content_log_json={'step_index': 0, 'steps': []},
    )

    if mode_type == 'B':
        options = ai_b.get('options') or []
        hint = ai_b.get('hint') or '请仔细阅读题干，思考关键条件。'
        return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                         'data': {'session_id': session.id, 'mode': 'B', 'hint': hint, 'options': options}})

    first_q = ai_c.get('first_question') or ai_c.get('question') or '请从题干中找出你认为最关键的一个条件，并说明理由。'
    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'session_id': session.id, 'mode': 'C', 'question': first_q}})


@api_view(['POST'])
@permission_classes([IsAuthenticated])
def guidance_reply(request, session_id):
    """S-07: 真实推进引导（B 走脚本，C 调 LLM 评价 + 步进 ai_answer_c.steps）。"""
    try:
        session = AIGuidanceSession.objects.get(pk=session_id, student_user_id=request.user)
    except AIGuidanceSession.DoesNotExist:
        return Response({'code': 404, 'message': '引导会话不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)
    if session.session_status != 'running':
        return Response({'code': 4001, 'message': '引导已结束', 'data': None, 'trace_id': make_trace_id()}, status=400)

    user_reply = (request.data.get('reply') or '').strip()
    try:
        q = ExamQuestion.objects.get(pk=session.question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    log = session.content_log_json or {'step_index': 0, 'steps': []}
    log.setdefault('steps', []).append({'user': user_reply})
    log['step_index'] = log.get('step_index', 0) + 1
    step_index = log['step_index']

    # C→B 降级（连续无效输入）
    if session.mode_type == 'C' and len(user_reply) < 3:
        session.invalid_input_count += 1
        if session.invalid_input_count >= 2:
            session.mode_type = 'B'
            session.session_status = 'downgraded'
            session.content_log_json = log
            session.save()
            return Response({'code': 0, 'message': '已降级到B模式', 'trace_id': make_trace_id(),
                             'data': {'mode': 'B', 'reason': '连续无效输入', 'is_completed': False}})

    ai_b = _as_dict(q.ai_answer_b)
    ai_c = _as_dict(q.ai_answer_c)

    if session.mode_type == 'B':
        next_hint = ai_b.get('next_hint') or ai_b.get('hint') or '很好，继续思考下一个关键点。'
        is_completed = step_index >= 3
        if is_completed:
            next_hint = '引导完成！可以返回做题页继续。'
        session.content_log_json = log
        session.session_status = 'completed' if is_completed else 'running'
        session.save()
        return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                         'data': {'next_hint': next_hint, 'is_completed': is_completed, 'mode': 'B'}})

    # C 模式：真实 LLM 评价 + 步进 ai_answer_c.steps
    evaluation = call_qwen_for_guidance(
        '你是一位耐心的老师，对学生回答给出 1-2 句简明评价与鼓励。',
        f'题目：{q.stem}\n参考答案：{q.answer or "见解析"}\n学生回答：{user_reply}\n请评价。',
    )
    steps = ai_c.get('steps') or ai_c.get('dialogue') or []
    next_question = None
    if step_index < len(steps):
        st = steps[step_index]
        next_question = st.get('question') or st.get('prompt') if isinstance(st, dict) else st
    if not next_question:
        next_question = '引导完成！你已经很好地理解了这道题。' if step_index >= 3 else '很好，请继续思考下一步。'
    is_completed = step_index >= 3

    session.content_log_json = log
    session.session_status = 'completed' if is_completed else 'running'
    session.save()

    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {'evaluation': evaluation, 'next_hint': next_question,
                              'next_question': next_question, 'is_completed': is_completed, 'mode': 'C'}})
```

### 步骤 1.3 　guidance.vue 渲染 C 模式评价
文件：`uniapp/src/pages/student/guidance.vue`，替换 `sendReplyInternal` 函数（现有第 100–116 行）：
```ts
async function sendReplyInternal(reply: string) {
  try {
    const res = await studentApi.guidanceReply(sessionId.value, reply)
    // C 模式：先显示真实 LLM 评价，再显示下一个引导问题
    const seg: string[] = []
    if (res.data?.evaluation) seg.push(`老师评价：${res.data.evaluation}`)
    const hint = res.data?.next_hint || res.data?.next_question
    if (hint) seg.push(hint)
    if (seg.length) messages.value.push({ role: 'system', content: seg.join('\n\n') })
    if (res.data?.is_completed) isCompleted.value = true
    if (res.data?.mode === 'B' && res.data?.reason) {
      mode.value = 'B'
      messages.value.push({ role: 'system', content: `已降级到B模式：${res.data.reason}` })
    }
  } catch (e) {
    uni.showToast({ title: '发送失败', icon: 'none' })
  }
}
```

### 步骤 1.4 　重启后端
```bash
venv/Scripts/python.exe manage.py runserver 0.0.0.0:8001
```

### 步骤 1.5 　验证（真实数据）
用 0.2 的 STUDENT_TOKEN、GUIDE_QID。

(a) 启动 C 模式 → 首问来自 `ai_answer_c.first_question`（非"你觉得这道题的关键条件是什么？"硬编码）：
```bash
curl -X POST http://localhost:8001/api/v1/student/guidance/sessions \
  -H "Authorization: Bearer <STUDENT_TOKEN>" -H "Content-Type: application/json" \
  -d "{\"question_id\":<GUIDE_QID>,\"mode_type\":\"C\"}"
```
记下返回的 `session_id`。

(b) 回复 → 真实 LLM 评价 + 步进（耗时约 2–6s）：
```bash
curl -X POST http://localhost:8001/api/v1/student/guidance/sessions/<SESSION_ID>/reply \
  -H "Authorization: Bearer <STUDENT_TOKEN>" -H "Content-Type: application/json" \
  -d "{\"reply\":\"我认为关键是题干里的等量关系\"}"
```
期望 JSON 含：`evaluation`（LLM 真实评价文本，非"注意观察图形中的关键信息"）、`next_hint`/`next_question`（来自 `ai_answer_c.steps`）、`is_completed`。

(c) 前端：做题页答错 → 进引导 → C 模式对话区出现"老师评价：…"再接下一问；连续两次输入 <3 字 → 自动降级 B。

### 步骤 1.6 　回归检查
- `AIGuidanceSession` 仍正常创建/更新；`get_mode_a`、作答链路不受影响。
- B 模式（默认）仍返回 `options/hint`。

### 步骤 1.7 　回滚
- 还原 `guidance_views.py`（旧桩实现）、`guidance.vue` 的 `sendReplyInternal`；删 `ai_helper.py`。

### F7 · DoD
☑ B/C 内容来自真实 `ai_answer_b/c` 与 LLM  ☑ 无"注意观察图形"等硬编码  ☑ 会话持久化到 DB  ☑ C→B 降级仍生效

---

## F6. 学生接口角色校验（DRF 权限类）

**问题**：所有 `/api/v1/student/*` 视图仅 `IsAuthenticated`，不校验 `role_type=='student'`。教师 token 可调学生接口。
**为什么不能用中间件**：本项目鉴权是 JWT（`config/settings.py:122` `OptionalJWTAuthentication`），DRF 在**视图层**才解析 Bearer token，**晚于 Django 中间件**；中间件里 `request.user` 还是 `AnonymousUser` → 用中间件判角色会把**合法学生也 403**。故必须用 **DRF 权限类**（在鉴权之后执行，能拿到真实 user）。
**目标**：新增 `IsStudent` 权限类，挂到每个学生视图；非学生 403。
**涉及文件**：
- 新建 `apps/study/permissions.py`（学生端专有）
- 改各学生视图装饰器（学生端专有）：`student_views.py`、`answer_views.py`、`guidance_views.py`、`wrongbook/views.py`；新建的 `variant_views.py`/`knowledge_views.py` 创建时即带上

### 步骤 2.1 　新建 permissions.py
文件：`apps/study/permissions.py`（新文件，完整内容）：
```python
from rest_framework import permissions


class IsStudent(permissions.BasePermission):
    """仅允许 role_type=='student'。"""
    def has_permission(self, request, view):
        return getattr(request.user, 'role_type', None) == 'student'
```

### 步骤 2.2 　给每个学生视图挂 IsStudent
对下列视图，把 `@permission_classes([IsAuthenticated])` 改为 `@permission_classes([IsAuthenticated, IsStudent])`，并在文件头加 `from apps.study.permissions import IsStudent`：
- `apps/study/student_views.py`：student_home / student_mission_detail / student_level_detail / growth_summary / export_pdf / upload_attempt_image
- `apps/study/answer_views.py`：submit_answer / retry_answer / get_mode_a
- `apps/study/guidance_views.py`：start_guidance / guidance_reply
- `apps/wrongbook/views.py`：wrongbook_list / wrongbook_detail / wrongbook_variants / mastery_list
- 新建的 `apps/wrongbook/variant_views.py`（F4）、`apps/study/knowledge_views.py`（F9）：创建时即用 `@permission_classes([IsAuthenticated, IsStudent])`

> 均为学生端专有文件（对方不改），每处仅一行装饰器 + 一行 import，冲突面小。

### 步骤 2.3 　重启后端
中间件在启动时加载，需重启进程。

### 步骤 2.4 　验证
(a) 教师 token 调学生接口 → 403：
```bash
curl -H "Authorization: Bearer <TEACHER_TOKEN>" http://localhost:8001/api/v1/student/home
```
期望 HTTP 403（DRF 权限拒绝）。
(b) 学生 token → 200（沿用阶段 1）。
(c) 未带 token → 401（DRF `IsAuthenticated` 先于 `IsStudent` 拦下）。

### 步骤 2.5 　回归检查
- 非 `/api/v1/student/` 路径（auth/questions/missions/admin 等）未挂 `IsStudent`，不受影响。
- 学生侧所有功能（home/mission/answer/wrong-book/growth/export…）仍 200。

### 步骤 2.6 　回滚
- 各视图还原 `@permission_classes([IsAuthenticated])`、删 import；删 `permissions.py`。

### F6 · DoD
☑ 教师 token 访问学生接口 403  ☑ 学生 token 正常  ☑ 其它路径无影响  ☑ 仅学生端文件（无共享文件改动）

---

## F5. 任务详情越权（IDOR）修复

**问题**：`apps/study/student_views.py:77` `LearningMission.objects.get(pk=mission_id, status='published')`，不校验学生是否属于该任务班级 → 知道 mission_id 即可跨班查看。
**目标**：学生必须有该任务的 `StudentMissionProgress` 才能访问详情/关卡。
**涉及文件**（学生端专有）：改 `apps/study/student_views.py`。

### 步骤 3.1 　student_mission_detail 增加权限校验
文件：`apps/study/student_views.py`，在 `student_mission_detail` 取到 mission 之后、构造 levels 之前**插入**校验（现有第 77–80 行 `try/except` 之后）：
```python
@api_view(['GET'])
@permission_classes([IsAuthenticated])
def student_mission_detail(request, mission_id):
    """S-02: Student mission detail."""
    try:
        mission = LearningMission.objects.get(pk=mission_id, status='published')
    except LearningMission.DoesNotExist:
        return Response({'code': 404, 'message': '任务不存在或未发布', 'data': None, 'trace_id': make_trace_id()}, status=404)

    # ← 新增：校验学生有权访问（存在任务进度记录）
    if not StudentMissionProgress.objects.filter(
            mission=mission, student_user_id=request.user).exists():
        return Response({'code': 403, 'message': '无权访问该任务', 'data': None, 'trace_id': make_trace_id()}, status=403)

    levels = []
    ...  # 其余不变
```

### 步骤 3.2（建议）　student_level_detail 同步加校验
同一文件 `student_level_detail`（现第 103–144 行），在取到 level 后插入：该 level 所属 mission 必须存在该学生的进度：
```python
    try:
        level = MissionLevel.objects.get(pk=level_id)
    except MissionLevel.DoesNotExist:
        return Response({'code': 404, 'message': '关卡不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    # ← 新增：校验该关卡所属任务学生有权访问
    if level.mission_id and not StudentMissionProgress.objects.filter(
            mission_id=level.mission_id, student_user_id=request.user).exists():
        return Response({'code': 403, 'message': '无权访问该关卡', 'data': None, 'trace_id': make_trace_id()}, status=403)

    rels = MissionQuestionRel.objects.filter(level=level)
    ...  # 其余不变
```
> 若 `level.mission_id` 可能为空（自由关卡），按业务酌情；多数关卡都属某任务。

### 步骤 3.3 　重启后端

### 步骤 3.4 　验证
- 学生 A（仅属 1 班）请求**别班**已发布任务的 mission_id → 403。
- 请求自己班的任务 → 正常 200。
- 用 mission_id 探测别班任务是否存在（404 vs 403 信息泄漏）：本方案对"已发布但无权限"返回 403、"不存在/未发布"返回 404——可接受；若需更强可统一返回 404。

### 步骤 3.5 　回归检查
- 阶段 1 的入班回填（F1）已保证在班学生有 progress → 正常访问不受影响。
- 教师端任务详情接口（`/api/v1/missions/...`）不受影响（不同路由）。

### 步骤 3.6 　回滚
- 删除两处插入的 `if not StudentMissionProgress...` 校验块。

### F5 · DoD
☑ 跨班访问任务/关卡 403  ☑ 本班访问正常  ☑ 入班回填学生不受影响

---

## 4. 本阶段端到端冒烟

| # | 操作 | 期望 |
|---|---|---|
| 1 | 教师token 调 `/student/home` | 403（**F6**） |
| 2 | 学生token 调 `/student/home` | 200（**F6** 不误伤） |
| 3 | 学生答错 → 进引导(C) → 回复 | 出现真实 LLM 评价 + 步进问题（**F7**） |
| 4 | 学生用别班 mission_id 调 `/student/missions/<id>` | 403（**F5**） |

## 5. 本阶段文件复核

| 文件 | 类型 | 归属 | 功能 |
|---|---|---|---|
| `apps/study/ai_helper.py` | 新 | 学生端 | F7 |
| `apps/study/guidance_views.py` | 改(重写) | 学生端 | F7 |
| `uniapp/src/pages/student/guidance.vue` | 改 | 学生端 | F7 |
| `apps/study/permissions.py` | 新 | 学生端 | F6 |
| `apps/study/{answer_views,guidance_views,student_views}.py`、`apps/wrongbook/views.py` | 改(+IsStudent) | 学生端 | F6 |
| `apps/study/student_views.py` | 改(前插校验) | 学生端 | F5 |

本阶段全部为学生端专有文件（F6 已改为不触碰任何共享文件），零冲突。
