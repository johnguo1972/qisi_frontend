# 学生端待完善功能 — 改造方案（已验证版）

> 文档整理日期：2026/07/09  
> 版本：v3（两轮代码库验证，修复 17 个问题）  
> 项目：qisi_frontend（Django 5 + UniApp）  
> 原则：最小改动、独立模块、不牵连现有代码、业务逻辑缜密

---

## 目录

1. [引导模式 (Guidance)](#1-引导模式-guidance)
2. [AI 答案生成流程](#2-ai-答案生成流程)
3. [变式题推荐](#3-变式题推荐)
4. [PDF 导出](#4-pdf-导出)
5. [知识图谱](#5-知识图谱)
6. [照片录入题目 (photo-create)](#6-照片录入题目-photo-create)
7. [测试覆盖率](#7-测试覆盖率)
8. [Celery 任务监控](#8-celery-任务监控)
9. [涉及数据库表变更汇总](#9-涉及数据库表变更汇总)

---

## 1. 引导模式 (Guidance)

### 1.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| **C 模式** | 前端 `guidance.vue` 硬编码 `mode_type: 'B'`，C 模式未真正引导用户选择开启自由对话模式 |
| **B 模式** | `ai_answer_b` 为空时无 fallback，后端返回硬编码提示，体验差 |
| **降级逻辑** | 仅按输入长度 `<3` 判为无效，未考虑回答内容质量（如"不知道""不会"等语义） |
| **session 存储** | 学生端已使用 `AIGuidanceSession` 数据库模型（无问题）；教师端 `_teacher_guidance_sessions` 在 `apps/missions/views.py` 仍为内存字典（**本次不处理**，属于教师端功能） |

### 1.2 改造方案

#### 1.2.1 方案一：C 模式引导 — 前端入口 + 后端 LLM 实时生成

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `uniapp/src/pages/student/guidance.vue` | 前端引导页，增加 C 模式入口按钮，允许用户选择 B/C 模式（当前硬编码 B） |
| `apps/study/guidance_views.py` | `start_guidance`：C 模式若 `ai_answer_c` 无数据，降级 B 或返回静态提示；`guidance_reply`：增强降级判断逻辑 |
| `apps/study/ai_helper.py` | 新增 `call_qwen_for_guidance_with_question()` 方法，用于实时生成引导问题 |
| `apps/study/student_urls.py` | 无需改动 |

**⚠️ 已验证：** 路由 `/api/v1/student/guidance/sessions` 和 `/api/v1/student/guidance/sessions/<id>/reply` 已注册，直接可用。

**具体改动：**

**1) `guidance.vue` — 增加模式选择入口**

当前 `guidance.vue` 的 `onMounted` 固定传 `mode_type: 'B'`。  
**⚠️ 注意：** `guidance.vue` 的路由参数只有 `questionId`（来自 `answer.vue` 和 `wrongbook.vue` 的导航），**没有 `questionType`**。  
不能直接引用 `questionType` 变量。改为让后端自动推荐模式：

```typescript
// 当前代码（需要修改的）
onMounted(async () => {
  if (!questionId.value) return

  try {
    // 改为传空字符串，让后端根据题型自动推荐 B/C 模式
    const res = await studentApi.startGuidance({
      question_id: questionId.value,
      mode_type: ''  // 空字符串 = 后端自动推荐
    })
    sessionId.value = res.data?.session_id || 0
    mode.value = res.data?.mode || 'B'
    if (res.data?.hint) {
      messages.value.push({ role: 'system', content: res.data.hint })
    }
    if (res.data?.options) {
      options.value = res.data.options
    }
    if (res.data?.question) {
      messages.value.push({ role: 'system', content: res.data.question })
    }
  } catch (e) {
    uni.showToast({ title: '启动引导失败', icon: 'none' })
  }
})
```

**2) `guidance_views.py` — 后端自动推荐模式 + C 模式 LLM 实时生成**

```python
# start_guidance 函数 — 增加自动推荐模式逻辑
@api_view(['POST'])
@permission_classes([IsAuthenticated, IsStudent])
def start_guidance(request):
    """S-06: 启动引导。当 mode_type 为空时根据题型自动推荐。"""
    question_id = request.data.get('question_id')
    mode_type = request.data.get('mode_type', 'B')
    try:
        q = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return Response({'code': 404, 'message': '题目不存在', 'data': None, 'trace_id': make_trace_id()}, status=404)

    # 前端未指定模式时，根据题型自动推荐
    if not mode_type:
        if q.question_type in ('single_choice', 'multiple_choice'):
            mode_type = 'B'  # 选择题 → 引导选择
        else:
            mode_type = 'C'  # 主观题 → 自由对话

    ai_b = _as_dict(q.ai_answer_b)
    ai_c = _as_dict(q.ai_answer_c)
    # ... 后续逻辑不变
```

**3) `ai_helper.py` — 新增可执行引导问题生成方法**

```python
"""学生端引导：轻量 Qwen 调用（独立于教师端 _call_qwen）。"""
import os
import json
import httpx
import logging

logger = logging.getLogger(__name__)

QWEN_API_URL = 'https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions'

# ... 保留现有 call_qwen_for_guidance ...

GUIDANCE_GENERATE_SYSTEM_PROMPT = """你是一位擅长苏格拉底式教学的中学教师。
根据题目信息和学生可能的知识水平，设计3-5个递进式引导问题。
每个问题应引导学生自主思考，而非直接给出答案。

输出严格JSON格式，不要包含markdown代码块：
{
  "steps": [
    {"question": "第一个引导问题", "hint": "提示学生思考的方向"},
    {"question": "第二个引导问题", "hint": "进一步深入提示"}
  ]
}"""

def call_qwen_for_guidance_with_question(stem: str, answer: str = "") -> dict:
    """实时调用 LLM 生成引导问题，用于 C 模式 ai_answer_c 为空时的降级兜底。
    
    返回: {"steps": [{"question": "...", "hint": "..."}]} 或空 dict
    """
    api_key = os.environ.get('QWEN_API_KEY', '')
    if not api_key:
        logger.warning('QWEN_API_KEY not set, cannot generate guidance')
        return {}

    user_prompt = f"""请为以下题目设计引导问题：
题干：{stem}
答案：{answer or '见解析'}
请设计3-5个递进式引导问题，帮助学生自主思考。"""

    headers = {
        'Authorization': f'Bearer {api_key}',
        'Content-Type': 'application/json',
    }
    payload = {
        'model': 'qwen3.6-flash',
        'messages': [
            {'role': 'system', 'content': GUIDANCE_GENERATE_SYSTEM_PROMPT},
            {'role': 'user', 'content': user_prompt},
        ],
        'max_tokens': 2000,
        'temperature': 0.7,
        'response_format': {'type': 'json_object'},
    }

    try:
        with httpx.Client(timeout=60.0, trust_env=False) as client:
            resp = client.post(QWEN_API_URL, json=payload, headers=headers)
            resp.raise_for_status()
            content = resp.json()['choices'][0]['message']['content']
            result = json.loads(content)
            if result and isinstance(result, dict) and 'steps' in result:
                return result
            return {}
    except Exception as e:
        logger.warning(f'Guidance generation failed: {e}')
        return {}
```

**4) `guidance_views.py` — 使用新方法作为降级兜底**

```python
# start_guidance 中 C 模式的处理
if mode_type == 'C':
    # 优先使用 ai_answer_c
    first_q = (ai_c.get('first_question') or ai_c.get('question') or
               (ai_c.get('steps') or [{}])[0].get('question') if ai_c.get('steps') else None)
    
    if not first_q:
        # ai_answer_c 为空，实时调用 LLM 生成
        try:
            generated = call_qwen_for_guidance_with_question(q.stem, q.answer)
            if generated and generated.get('steps'):
                ai_c = generated
                first_q = generated['steps'][0]['question']
        except Exception:
            pass
    
    if not first_q:
        # 降级到 B 模式
        mode_type = 'B'
        options = ai_b.get('options') or []
        hint = ai_b.get('hint') or '请仔细阅读题干，思考关键条件。'
        return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                         'data': {'session_id': session.id, 'mode': 'B', 'hint': hint, 'options': options}})
```

#### 1.2.2 方案二：降级逻辑增强

**当前：** 仅 `len(user_reply) < 3` 判断无效输入  
**改为：** 增加语义层面的无效输入判断

```python
# guidance_views.py — 在 guidance_reply 函数之前新增辅助函数
def _is_invalid_reply(reply: str) -> bool:
    """判断学生回答是否有效（简单规则，无需 LLM）。"""
    if not reply or len(reply) < 3:
        return True
    # 常见无效回答模式
    INVALID_PATTERNS = ['不知道', '不会', '不懂', '没想法', '跳过', 'pass', '不会做', '不想做']
    if any(p in reply for p in INVALID_PATTERNS):
        return True
    return False
```

然后在 `guidance_reply` 中将 `len(user_reply) < 3` 替换为 `_is_invalid_reply(user_reply)`：

```python
# 原来的：if session.mode_type == 'C' and len(user_reply) < 3:
# 改为：
if session.mode_type == 'C' and _is_invalid_reply(user_reply):
    session.invalid_input_count += 1
    if session.invalid_input_count >= 2:
        session.mode_type = 'B'
        session.session_status = 'downgraded'
        ...
```

#### 1.2.3 方案三：session 清理

**改动点：** 在 `start_guidance` 中，每次启动新 session 前，清理过期 session：

```python
# start_guidance 函数开头
from django.utils import timezone
from datetime import timedelta

# 清理 24 小时前的运行中 session（避免堆积）
AIGuidanceSession.objects.filter(
    student_user_id=request.user,
    session_status='running',
    created_at__lt=timezone.now() - timedelta(hours=24)
).update(session_status='expired')
```

**⚠️ 已验证：** `AIGuidanceSession` 的 `session_status` 字段为 `CharField(max_length=20, default='running')`，无 choices 约束，因此 `'expired'` 值合法。且 `guidance_reply` 中检查 `session.session_status != 'running'`，已过期 session 会正确返回 400 错误。

---

## 2. AI 答案生成流程

### 2.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 自动触发 | `ai_answer_a/b/c` 的生成依赖教师手动触发 review 流程，解析完成后未自动触发 |
| 批量生成 | 已有 `batch_ai_process_questions` Celery 任务，但缺少自动批量触发机制 |
| 失败重试 | AI 生成失败时无自动重试/降级机制 |

### 2.2 改造方案

#### 2.2.1 方案一：解析完成后自动触发 AI 答案生成（使用信号）

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `apps/study/receivers.py` | **推荐位置**：新增 signal，监听 `ExamQuestion` 的 `post_save`（因为 study 已有 `ready()` 注册模式） |
| `apps/parser/__init__.py` | **备选位置**：若要放在 parser 中，需在 `ParserConfig` 加 `ready()` 方法（当前没有） |
| `apps/common/batch_tasks.py` | 新增 `single_generate_ai_answers` 任务 |
| `apps/parser/models.py` | 可选：在 `ExamQuestion` 增加 `ai_generation_error` 字段 |

**⚠️ 已验证：** `apps/study/receivers.py` 已存在，且 `apps/study/apps.py` 的 `ready()` 方法已注册 `receivers`。  
**⚠️ 已验证：** `apps/parser/__init__.py` 虽有 `ParserConfig`，但**没有 `ready()` 方法**，因此不能直接放 signal 到 `apps/parser/signals.py`（新建文件不会被加载）。

**推荐方案：使用 `apps/study/receivers.py`（最解耦，无需改动 app config）**

**`apps/study/receivers.py` 新增代码：**

```python
"""学生端信号接收器：学生加入班级后回填任务进度 + AI 答案自动生成。"""
from django.db.models.signals import post_save
from django.dispatch import receiver
from apps.institutions.models import ClassStudent
from apps.missions.models import LearningMission
from apps.parser.models import ExamQuestion
from .models import StudentMissionProgress
from apps.common.batch_tasks import single_generate_ai_answers


@receiver(post_save, sender=ClassStudent)
def backfill_mission_progress_on_join(sender, instance, created, **kwargs):
    """新增活跃班级成员时，为该班级所有已发布任务补建 StudentMissionProgress。"""
    if not created or instance.status != 'active':
        return
    missions = LearningMission.objects.filter(
        class_obj_id=instance.class_obj_id, status='published'
    )
    for mission in missions:
        StudentMissionProgress.objects.get_or_create(
            mission=mission,
            student_user_id_id=instance.student_id,
            defaults={'progress_status': 'not_started', 'progress_percent': 0},
        )


@receiver(post_save, sender=ExamQuestion)
def auto_trigger_ai_generation(sender, instance, created, **kwargs):
    """当题目解析完成（parse_status='auto_parsed'）且未生成过 AI 答案时，自动触发。
    
    ⚠️ 注意：post_save 在每次 save() 都会触发，但条件 `not instance.ai_answer_a` 
    确保 save_results_to_question 写入后不会再次触发，无递归风险。
    """
    if instance.parse_status == 'auto_parsed' and not instance.ai_answer_a:
        single_generate_ai_answers.delay(instance.id)
```

**`apps/common/batch_tasks.py` 新增任务：**

```python
@shared_task(bind=True, max_retries=2, default_retry_delay=30)
def single_generate_ai_answers(self, question_id: int, model: str = None):
    """为单道题生成 A/B/C 模式 AI 答案（轻量任务，不跑全流程）。
    
    此任务被 auto_trigger_ai_generation signal 触发，也可手动调用。
    
    Args:
        question_id: 题目 ID
        model: 可选 AI 模型覆盖（如 'qwen3.6-plus'），默认由 AIReviewService 决定
    """
    logger = logging.getLogger(__name__)
    try:
        from apps.parser.models import ExamQuestion
        service = AIReviewService()
        results = service.process_question_full(question_id, model=model)
        service.save_results_to_question(question_id, results)
        
        # 记录失败信息（可选，需 ExamQuestion 有 ai_generation_error 字段）
        if results.get('errors'):
            question = ExamQuestion.objects.get(id=question_id)
            if hasattr(question, 'ai_generation_error') or 'ai_generation_error' in [f.name for f in ExamQuestion._meta.get_fields()]:
                question.ai_generation_error = json.dumps(results['errors'])
                question.save(update_fields=['ai_generation_error'])
        
        return {'status': 'success', 'question_id': question_id}
    except Exception as e:
        logger.exception(f'AI generation failed for question {question_id}: {e}')
        raise self.retry(exc=e, countdown=30 * (2 ** self.request.retries))
```

**⚠️ 注意：** 需要在 `single_generate_ai_answers` 函数内 `from apps.parser.models import ExamQuestion`（或全局 import），因为 `batch_tasks.py` 顶部没有 import `ExamQuestion`。

#### 2.2.2 方案二：批量生成 AI 答案

**已有实现：** `apps/common/batch_tasks.py` 中的 `batch_ai_process_questions`  
**前端接口：** 教师端增加"批量生成 AI 答案"按钮，调用现有 API。  
**无需新增后端代码。**

#### 2.2.3 方案三：AI 生成失败重试/降级

**当前机制：** `AIReviewService._call_ai` 已有 3 次重试（5s/8s/10s）  
**需要增强：** 在 `process_question_full` 中，某一模式失败不影响其他模式（当前已实现：每个模式独立 try/except）

---

## 3. 变式题推荐

### 3.1 当前问题

`wrongbook/services.py` 仅按学科+难度简单匹配，未使用 AI 或知识点相似度，推荐质量低。

### 3.2 改造方案

#### 方案一：基于知识点匹配（推荐，改动最小）

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `apps/wrongbook/services.py` | 重写 `find_variant_questions`，优先按知识点匹配，再按学科+难度 |
| `apps/wrongbook/variant_views.py` | 无需改动（调用不变） |

**⚠️ 已验证：** `ExamQuestion.knowledge_points` 字段为 `JSONField`，存储格式为 `[{"id": 1, "module": "一元二次方程"}, ...]`。  
`__contains` 查询在 SQLite（JSON1 扩展）和 PostgreSQL 中均支持。

**核心逻辑：**

```python
def find_variant_questions(question_id: int, limit: int = 3) -> list:
    """按知识点匹配变式题（优先于学科+难度）。"""
    try:
        original = ExamQuestion.objects.get(pk=question_id)
    except ExamQuestion.DoesNotExist:
        return []

    # 获取原题知识点
    original_kps = original.knowledge_points or []
    kp_modules = [kp.get('module') for kp in original_kps if isinstance(kp, dict) and kp.get('module')]

    qs = ExamQuestion.objects.exclude(pk=question_id)

    if kp_modules:
        # 优先匹配知识点：JSONField 的 __contains 查询数组元素
        from django.db.models import Q
        kp_query = Q()
        for module in kp_modules:
            kp_query |= Q(knowledge_points__contains=[{'module': module}])
        matched = qs.filter(kp_query).prefetch_related('images', 'options')
        matched_count = matched.count()
        if matched_count >= limit:
            results = matched[:limit]
        else:
            # 知识点匹配不足时，补充同科目+同难度
            remainder = limit - matched_count
            remainder_qs = qs.exclude(id__in=list(matched.values_list('id', flat=True)))
            if original.difficulty is not None:
                remainder_qs = remainder_qs.filter(
                    subject=original.subject,
                    difficulty=original.difficulty
                )
            else:
                remainder_qs = remainder_qs.filter(subject=original.subject)
            results = list(matched) + list(remainder_qs[:remainder])
    else:
        # 原题无知识点，按学科+难度匹配（当前逻辑）
        if original.difficulty is not None:
            qs = qs.filter(subject=original.subject, difficulty=original.difficulty)
        else:
            qs = qs.filter(subject=original.subject)
        results = qs.prefetch_related('images', 'options')[:limit]

    return [_question_brief(q) for q in results]
```

**无需数据库表变更，不涉及新模型。**

---

## 4. PDF 导出

### 4.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 排版 | 当前用 reportlab 基础排版，缺少水印、知识点标签、页码 |
| 水印 | 无学生信息水印 |
| 知识点标签 | 题目未附带知识点标签信息 |
| 图片 | PDF 中未包含题目图片（仅文本） |

### 4.2 改造方案

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `apps/study/student_views.py` | 增强 `_build_pdf()` 函数，加入水印、知识点标签、页码、图片 |
| `uniapp/src/pages/student/export.vue` | 增加导出选项（水印内容输入） |

**⚠️ 已验证 — 关键问题：**

1. **`export_pdf` 中的 `.values()` 查询缺少 `knowledge_points` 字段**  
   当前代码：`qs = ExamQuestion.objects.filter(...).values('id', 'question_no', 'question_type', 'stem', 'stem_html', 'answer', 'analysis')`  
   没有 `knowledge_points`，导致 PDF 中知识点标签无法工作。  
   **修复：** 在 `.values()` 中增加 `'knowledge_points'`。

2. **`WatermarkDocTemplate` 子类化 `afterPage()` 方式不可靠**  
   Reportlab 的 `SimpleDocTemplate` 使用 `onPage` 回调机制更稳定。  
   不应子类化 `afterPage()`，而是使用 `PageTemplate` 的 `onPage` 参数。

**核心改动：** `_build_pdf()` 增强版

```python
def _build_pdf(export_type: str, questions: list, include_answers: bool,
               watermark_text: str = "") -> bytes:
    """增强版 PDF 生成：水印、知识点标签、页码、图片。"""
    from reportlab.lib.pagesizes import A4
    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.platypus import (SimpleDocTemplate, Paragraph, Spacer, 
                                     Image as RLImage)
    from reportlab.pdfbase import pdfmetrics
    from reportlab.pdfbase.cidfonts import UnicodeCIDFont
    from reportlab.lib.units import mm
    from reportlab.lib import colors
    import io

    pdfmetrics.registerFont(UnicodeCIDFont('STSong-Light'))
    buf = io.BytesIO()

    # 使用 onPage 回调实现水印和页码（比子类化 afterPage 更可靠）
    def on_page(canvas, doc):
        if watermark_text:
            canvas.saveState()
            canvas.setFont('STSong-Light', 40)
            canvas.setFillColor(colors.Color(0, 0, 0, alpha=0.08))
            canvas.rotate(45)
            canvas.drawString(200, -200, watermark_text)
            canvas.restoreState()
        # 页码
        canvas.saveState()
        canvas.setFont('STSong-Light', 9)
        canvas.setFillColorRGB(0.5, 0.5, 0.5)
        canvas.drawCentredString(A4[0] / 2, 15 * mm, f'- {doc.page} -')
        canvas.restoreState()

    doc = SimpleDocTemplate(
        buf, pagesize=A4,
        topMargin=15*mm, bottomMargin=20*mm,
        leftMargin=15*mm, rightMargin=15*mm,
    )
    # 应用 onPage 回调
    from reportlab.platypus import PageTemplate, Frame
    frame = Frame(
        doc.leftMargin, doc.bottomMargin,
        doc.width, doc.height,
        id='normal'
    )
    doc.addPageTemplates([
        PageTemplate(frames=[frame], onPage=on_page),
    ])

    styles = getSampleStyleSheet()
    body = ParagraphStyle('ZhBody', parent=styles['Normal'],
                          fontName='STSong-Light', fontSize=11, leading=18)
    h1 = ParagraphStyle('ZhH1', parent=styles['Title'],
                        fontName='STSong-Light', fontSize=18)
    story = []

    type_label = '错题本' if export_type == 'wrongbook' else '任务题目'
    story.append(Paragraph(type_label, h1))
    story.append(Spacer(1, 6*mm))
    story.append(Paragraph(f'导出时间：{timezone.now().strftime("%Y-%m-%d %H:%M:%S")}　题目数：{len(questions)}', body))
    story.append(Spacer(1, 6*mm))

    for i, q in enumerate(questions, 1):
        qtype = ExamQuestion.QUESTION_TYPE_LABELS.get(q['question_type'], q['question_type'])
        story.append(Paragraph(f'第{i}题（{qtype}）', body))

        # 知识点标签
        kps = q.get('knowledge_points') or []
        if kps:
            kp_text = "知识点：" + "、".join(
                kp.get('module', '') if isinstance(kp, dict) else str(kp)
                for kp in kps[:3]
            )
            story.append(Paragraph(f'<font color="#666666" size="9">{kp_text}</font>', body))

        story.append(Paragraph(q['stem'] or '', body))
        for opt in q.get('options_html', []):
            story.append(Paragraph(f'{opt["label"]}. {opt["content"]}', body))

        # 图片
        for img_url in q.get('image_urls', []):
            img_path = str(settings.MEDIA_ROOT / img_url)
            if os.path.exists(img_path):
                try:
                    img = RLImage(img_path, width=400, height=200)
                    story.append(img)
                except Exception:
                    pass

        if include_answers:
            if q.get('answer'):
                story.append(Paragraph(f'<b>答案：</b>{q["answer"]}', body))
            if q.get('analysis'):
                story.append(Paragraph(f'<b>解析：</b>{q["analysis"]}', body))
        story.append(Spacer(1, 4*mm))

    doc.build(story)
    return buf.getvalue()
```

**⚠️ 同时需要修改 `export_pdf` 中的 `.values()` 查询，增加 `knowledge_points`：**

在 `export_pdf` 函数中，找到两处 `.values()` 调用（`wrongbook` 和 `mission` 分支），增加 `'knowledge_points'`：

```python
qs = ExamQuestion.objects.filter(id__in=list(items)).values(
    'id', 'question_no', 'question_type', 'stem', 'stem_html',
    'answer', 'analysis', 'knowledge_points',  # <-- 增加
)
```

**前端 `export.vue` 增加水印选项：**

```vue
<!-- 在 export.vue 的模板中，选项卡片区域增加 -->
<view class="option-card">
  <text class="card-title">水印设置</text>
  <view class="input-row">
    <text class="input-label">水印文字</text>
    <input v-model="watermarkText" placeholder="留空则无水印" class="watermark-input" />
  </view>
</view>
```

对应的 script 中增加 `watermarkText` 变量：

```typescript
const watermarkText = ref('')
```

在 `handleExport` 中传入：

```typescript
const res = await exportApi.exportPdf({
  export_type: exportType.value,
  item_ids: itemIds.value,
  include_answers: includeAnswers.value,
  watermark_text: watermarkText.value,  // 新增
})
```

**API 参数增强：** `export_pdf` 接收可选 `watermark_text` 参数：

```python
@api_view(['POST'])
@permission_classes([IsAuthenticated, IsStudent])
def export_pdf(request):
    ...
    watermark_text = request.data.get('watermark_text', '')
    ...
    pdf_bytes = _build_pdf(export_type, questions_data, include_answers, watermark_text)
```

**前端 `export.ts` 类型更新：** TypeScript 类型需增加 `watermark_text` 字段：

```typescript
// 在 uniapp/src/api/student.ts 中
export const exportApi = {
  exportPdf: (data: { 
    export_type: string
    item_ids: number[]
    include_answers: boolean
    watermark_text?: string  // <-- 新增可选字段
  }) => post<{ download_url?: string; url?: string }>('/student/export/pdf', data),
}
```

---

## 5. 知识图谱

### 5.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 后端 | 仅提供树形数据（`knowledge/views.py`），无个性化掌握度 |
| 前端 | `knowledge-graph.vue` 为列表展示，非图谱可视化 |
| 个性化 | 缺少基于学生答题数据的知识点掌握度可视化 |

### 5.2 改造方案

#### 方案一：增强现有掌握度接口（推荐，改动最小）

**当前后端已有：** `knowledge_mastery` 接口（`apps/study/knowledge_views.py`）已按知识点聚合掌握度。  
**当前前端已有：** `knowledge-graph.vue` 已展示列表式掌握度。

**需要增强的点：**

| 文件 | 改动说明 |
|------|---------|
| `apps/study/knowledge_views.py` | 返回数据增加层级结构（学科→学段→年级→知识点），支持树形渲染 |
| `uniapp/src/pages/student/knowledge-graph.vue` | 增加树形图切换（使用 ECharts 或递归组件） |
| `uniapp/src/api/student.ts` | 无需改动 |

**⚠️ 已验证：** `knowledge_mastery` 路由为 `/api/v1/student/knowledge-mastery`，前端 API 为 `studentApi.knowledgeMastery()`，均正确。

**后端 `knowledge_mastery` 增强：**

```python
@api_view(['GET'])
@permission_classes([IsAuthenticated, IsStudent])
def knowledge_mastery(request):
    """增强版：返回树形知识点掌握度 + 列表。"""
    attempts = AnswerAttempt.objects.filter(
        student_user_id=request.user, is_subjective_pending=False
    )
    qids = [a.question_id for a in attempts]
    q_map = {q.id: q for q in ExamQuestion.objects.filter(id__in=qids)}

    agg = defaultdict(lambda: {'attempt': 0, 'correct': 0})
    for a in attempts:
        q = q_map.get(a.question_id)
        if not q:
            continue
        for kp in (q.knowledge_points or []):
            label = (kp.get('module') if isinstance(kp, dict) else str(kp)) or '未分类'
            agg[label]['attempt'] += 1
            if a.is_correct:
                agg[label]['correct'] += 1

    items = []
    for label, v in agg.items():
        acc = round(v['correct'] / max(v['attempt'], 1) * 100, 1)
        items.append({
            'knowledge': label,
            'attempt': v['attempt'],
            'correct': v['correct'],
            'accuracy': acc,
            'mastery': 'mastered' if acc >= 85 else ('reviewing' if acc >= 60 else 'weak'),
        })
    items.sort(key=lambda x: x['accuracy'])  # 弱项在前

    # 新增：组织树形结构（按 knowledge_points 表层级）
    from apps.knowledge.models import KnowledgePoint
    kp_names = [item['knowledge'] for item in items]
    kp_records = KnowledgePoint.objects.filter(
        module__in=kp_names
    ).values('module', 'subject', 'stage', 'grade_index', 'grade_name', 'chapter')
    kp_map = {r['module']: r for r in kp_records}

    # 构建树：subject -> stage -> grade -> [knowledge items]
    tree = {}
    kp_label_map = dict(KnowledgePoint.SUBJECT_CHOICES)
    stage_label_map = KnowledgePoint.STAGE_LABELS
    grade_label_map = KnowledgePoint.GRADE_LABELS

    for item in items:
        kp = kp_map.get(item['knowledge'])
        if not kp:
            continue
        subj = kp['subject']
        stage = kp['stage']
        grade_key = f"{kp['grade_index']}_{kp['grade_name']}"

        # 学科层
        if subj not in tree:
            tree[subj] = {
                'name': kp_label_map.get(subj, subj),
                'type': 'subject',
                'children': {},
            }
        # 学段层
        stage_node = tree[subj]['children']
        if stage not in stage_node:
            stage_node[stage] = {
                'name': stage_label_map.get(stage, stage),
                'type': 'stage',
                'children': {},
            }
        # 年级层
        grade_node = stage_node[stage]['children']
        if grade_key not in grade_node:
            grade_node[grade_key] = {
                'name': grade_label_map.get(kp['grade_index'], kp['grade_name']),
                'type': 'grade',
                'children': [],
            }
        grade_node[grade_key]['children'].append({
            'name': item['knowledge'],
            'type': 'knowledge',
            'accuracy': item['accuracy'],
            'mastery': item['mastery'],
            'attempt': item['attempt'],
            'correct': item['correct'],
        })

    # 扁平化树
    def flatten(node):
        if isinstance(node, dict) and 'children' in node:
            children = node['children']
            if isinstance(children, dict):
                node['children'] = [flatten(v) for v in children.values()]
            elif isinstance(children, list):
                node['children'] = [flatten(c) for c in children]
        return node

    tree_data = [flatten(v) for v in tree.values()]

    return Response({'code': 0, 'message': 'success', 'trace_id': make_trace_id(),
                     'data': {
                         'items': items,        # 列表（保持原有）
                         'tree': tree_data,     # 树形（新增）
                         'total': len(items),
                     }})
```

**前端 `knowledge-graph.vue` 增强：** 增加"树形视图"按钮，使用递归组件展示树形知识点。

**无需数据库表变更。**

---

## 6. 照片录入题目 (photo-create)

### 6.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 流程 | 拍照录入流程存在，但缺少完整的 OCR + 解析闭环 |
| 简化版 | 与完整试卷解析流水线相比是简化版，缺少位置检测、跨页合并等 |

### 6.2 改造方案

#### 方案一：复用完整解析流水线（推荐，代码独立）

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `apps/study/photo_views.py` | `photo_create_question` 中调用 `single_generate_ai_answers.delay()` 自动生成 AI 答案 |
| `apps/study/urls.py` | 无需改动 |

**⚠️ 已验证：** `photo_create_question` 路由为 `/api/v1/questions/photo-create/`，注册在 `apps/study/urls.py` 中。  
**⚠️ 注意：** `photo_create_question` 的权限是 `@permission_classes([IsAuthenticated])`（无 `IsStudent`），教师和学生均可使用。

**具体改动：**

```python
# photo_views.py — 在 photo_create_question 创建 question 成功后
from apps.common.batch_tasks import single_generate_ai_answers

# 在创建 question 和关联数据后，返回前：
# 异步触发 AI 答案生成（不阻塞返回）
single_generate_ai_answers.delay(question.id)

# 返回时增加提示
return Response({
    'code': 0,
    'message': '识别成功，AI 答案正在生成中...',
    'data': {
        'question_id': question.id,
        'system_id': system_id,
        'parsed': parsed,
        'ai_generation_status': 'pending',
    },
    'trace_id': make_trace_id(),
})
```

**无需数据库表变更。**

---

## 7. 测试覆盖率

### 7.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 测试文件 | 仅有 `test_models_quick.py` 和 `test_integration.py` |
| 核心流水线 | 缺少核心解析流水线的单元测试 |

### 7.2 改造方案

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `tests/test_ai_pipeline.py` | 新增 AI 答案生成流水线测试 |
| `tests/test_student_api.py` | 新增学生端 API 测试（引导、答题、错题本） |
| `tests/test_pdf_export.py` | 新增 PDF 导出测试 |

**测试用例清单：**

**1) `tests/test_ai_pipeline.py`** — 测试范围：
- `AIReviewService.process_question_full` 调用（mock LLM 接口）
- 知识点分析结果解析
- A/B/C 模式答案 JSON 解析
- 失败重试逻辑
- `save_results_to_question` 数据持久化

**2) `tests/test_student_api.py`** — 测试范围：
- 引导模式 B/C 启动与回复
- 降级逻辑（连续无效输入）
- 答题提交与判分
- 错题本列表与详情
- 变式题推荐
- 知识掌握度聚合

**3) `tests/test_pdf_export.py`** — 测试范围：
- PDF 生成不崩溃
- 水印渲染
- 知识点标签
- 导出限流

**无需数据库表变更。**

---

## 8. Celery 任务监控

### 8.1 当前问题

| 子项 | 问题描述 |
|------|---------|
| 超时检测 | 已有 `periodic_stale_task_check` |
| 实时推送 | 缺少任务进度实时推送（WebSocket/SSE） |
| 告警机制 | 缺少失败任务的告警通知 |

### 8.2 改造方案

#### 8.2.1 SSE 进度推送（推荐，比 WebSocket 轻量）

**涉及文件：**

| 文件 | 改动说明 |
|------|---------|
| `apps/study/student_views.py` | 新增 SSE 端点（复用 study 已有视图文件，避免新建 URL 文件） |
| `apps/study/student_urls.py` | 注册 SSE 路由 |
| `uniapp/src/pages/teacher/parser-progress.vue` | 前端使用 EventSource 接收进度 |

**⚠️ 已验证：** `apps/parser/` 目录下没有 `urls.py` 文件，`apps/parser/views.py` 也不存在。  
因此 SSE 端点应放在已有视图文件和 URL 配置中，推荐 `apps/study/student_views.py` 和 `apps/study/student_urls.py`。

**⚠️ 已验证：** `StreamingHttpResponse` 可以配合 `@api_view` 使用，但需注意 DRF 的 `@api_view` 会在认证失败时返回 DRF 响应而非 SSE 流。认证成功后，`StreamingHttpResponse` 直接返回给客户端，不经过 DRF 的渲染器。

**后端 SSE 实现：**

```python
# 在 apps/study/student_views.py 末尾新增

from django.http import StreamingHttpResponse
from django.core.cache import cache
from apps.common.batch_tasks import PROGRESS_KEY_PREFIX

@api_view(['GET'])
@permission_classes([IsAuthenticated])
def task_progress_stream(request, task_id):
    """SSE 端点：实时推送任务进度。
    
    使用说明：前端通过 EventSource 或 fetch + ReadableStream 消费。
    GET /api/v1/student/tasks/<task_id>/progress
    """
    def event_stream():
        import json
        import time
        last_data = None
        while True:
            progress_data = cache.get(f'{PROGRESS_KEY_PREFIX}{task_id}')
            if progress_data and progress_data != last_data:
                last_data = progress_data
                yield f'data: {progress_data}\n\n'
                try:
                    status = json.loads(progress_data).get('status')
                    if status in ('completed', 'failed', 'cancelled'):
                        break
                except (json.JSONDecodeError, TypeError):
                    pass
            # 没有新数据时，发送心跳保持连接
            else:
                yield ': heartbeat\n\n'
            time.sleep(2)
    
    response = StreamingHttpResponse(
        event_stream(),
        content_type='text/event-stream',
    )
    response['Cache-Control'] = 'no-cache'
    response['X-Accel-Buffering'] = 'no'  # 禁用 nginx 缓冲
    return response
```

**`apps/study/student_urls.py` 注册路由：**

```python
urlpatterns = [
    # ... 现有路由 ...
    path('tasks/<str:task_id>/progress', task_progress_stream, name='task-progress'),
]
```

#### 8.2.2 失败告警

**方案：** 在 `periodic_stale_task_check` 中增加通知逻辑

```python
# apps/parser/tasks.py — 在 periodic_stale_task_check 中
def _notify_task_failure(task):
    """发送任务失败通知（站内信 + 可选邮件/钉钉）。"""
    # 当前项目暂无 Notification 模型，先记录日志
    import logging
    logger = logging.getLogger(__name__)
    logger.warning(f'Task {task.id} failed: {task.error_message}')
    # TODO: 后续可接入钉钉/邮件通知
```

**无需数据库表变更，无需新增模型。**

---

## 9. 涉及数据库表变更汇总

| 功能模块 | 是否变更 | 变更内容 | 迁移脚本 |
|---------|---------|---------|---------|
| 引导模式 | **否** | 仅逻辑改动，不涉及表结构 | — |
| AI 答案生成 | **可选** | 可在 `ExamQuestion` 表新增 `ai_generation_error` 字段 | 见下方 9.1 |
| 变式题推荐 | **否** | 仅逻辑改动 | — |
| PDF 导出 | **否** | 仅逻辑改动 | — |
| 知识图谱 | **否** | 仅逻辑改动 | — |
| 照片录入 | **否** | 仅逻辑改动 | — |
| 测试覆盖率 | **否** | 纯测试文件 | — |
| Celery 监控 | **否** | 仅逻辑改动 | — |

### 9.1 可选：ExamQuestion 新增 ai_generation_error 字段

**迁移脚本：** `apps/parser/migrations/0008_add_ai_generation_error.py`

```python
from django.db import migrations, models

class Migration(migrations.Migration):
    dependencies = [
        ('parser', '0007_add_ai_processing_fields'),
    ]

    operations = [
        migrations.AddField(
            model_name='examquestion',
            name='ai_generation_error',
            field=models.JSONField(null=True, blank=True, verbose_name='AI生成错误记录'),
        ),
    ]
```

**回滚脚本：**

```python
operations = [
    migrations.RemoveField(
        model_name='examquestion',
        name='ai_generation_error',
    ),
]
```

---

## 附录：验证过程中发现的问题与修复记录

| # | 问题 | 严重程度 | 修复方式 |
|---|------|---------|---------|
| 1 | `guidance.vue` 引用 `questionType` 变量但不存在（路由参数只有 `questionId`） | **Bug** | 改为后端自动推荐模式，前端传空字符串 |
| 2 | `call_qwen_for_guidance_with_question` 是骨架代码 `...` | **Bug** | 提供完整可执行实现 |
| 3 | `apps/parser/` 无 `signals.py` 和 `apps.py` 的 `ready()` 方法，signal 不会被加载 | **Bug** | 将 signal 放在已有 `apps/study/receivers.py` 中 |
| 4 | `single_generate_ai_answers` 引用 `question` 变量但未查询 | **Bug** | 增加 `ExamQuestion.objects.get(id=question_id)` 查询 |
| 5 | PDF 导出 `.values()` 查询缺少 `knowledge_points` 字段 | **Bug** | 在 `.values()` 中增加 `'knowledge_points'` |
| 6 | `WatermarkDocTemplate.afterPage()` 子类化方式在 reportlab 中不稳定 | **Bug** | 改用 `PageTemplate` + `onPage` 回调 |
| 7 | SSE 端点使用 `@api_view` 但返回 `StreamingHttpResponse`（非 DRF Response） | **需确认** | `@api_view` 能正确处理 `StreamingHttpResponse`（非 DRF Response 会原样通过），无需改动 |
| 8 | SSE 端点缺少 `PROGRESS_KEY_PREFIX` 导入 | **Bug** | 增加 `from apps.common.batch_tasks import PROGRESS_KEY_PREFIX` |
| 9 | 文档引用 `apps/parser/urls.py` 但文件不存在 | **Bug** | 路由注册改为 `apps/study/student_urls.py` |
| 10 | 文档引用 `apps/parser/views.py` 但文件不存在 | **Bug** | SSE 视图放在 `apps/study/student_views.py` |
| 11 | `_build_pdf` 中 `from reportlab.pdfgen import canvas` 未使用 | **清理** | 移除未使用的 import |
| 12 | 引导模式 session 清理时 `expired` 状态值有效性 | **确认** | `CharField` 无 choices 约束，值合法 |
| 13 | `photo_create_question` 权限为 `IsAuthenticated`，学生可访问 | **确认** | 无需改动 |
| 14 | `single_generate_ai_answers` 函数签名缺少 `model` 参数 | **Bug** | 已修复：增加 `model: str = None` 参数 |
| 15 | `_build_pdf` 中 `kps = q.get('knowledge_points')` 重复判断 | **Bug** | 已修复：改为 `kps = q.get('knowledge_points') or []` |
| 16 | `exportApi.exportPdf` TypeScript 类型缺少 `watermark_text` | **Bug** | 已修复：增加可选字段 `watermark_text?: string` |
| 17 | 原始需求中 `teacher_guidance_sessions` 内存字典是教师端问题 | **确认** | 教师端 `apps/missions/views.py` 的 `_teacher_guidance_sessions` 内存字典，不属于学生端范围，本次不处理 |

---

## 总结：各功能改动量评估

| 功能 | 后端改动文件 | 前端改动文件 | 改动量 | 复杂度 |
|------|------------|------------|--------|--------|
| 引导模式 | 2（guidance_views.py, ai_helper.py） | 1（guidance.vue） | 小 | 低 |
| AI 答案生成 | 2（receivers.py, batch_tasks.py） | 0 | 中 | 中 |
| 变式题推荐 | 1（services.py） | 0 | 小 | 低 |
| PDF 导出 | 1（student_views.py） | 1（export.vue） | 中 | 中 |
| 知识图谱 | 1（knowledge_views.py） | 1（knowledge-graph.vue） | 小 | 低 |
| 照片录入 | 1（photo_views.py） | 0 | 小 | 低 |
| 测试覆盖率 | 3（新增测试文件） | 0 | 中 | 低 |
| Celery 监控 | 2（student_views.py, student_urls.py） | 1 | 中 | 中 |

**总改动：** 后端 13 个文件，前端 3 个文件，1 个可选迁移脚本。  
**数据库变更：** 0 个必要变更，1 个可选新增字段。