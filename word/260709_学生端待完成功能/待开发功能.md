需要完善的功能
1. 引导模式 (Guidance) — 需重点完善
学生端 C 模式: 当前是硬编码的 mock 响应（"你觉得这道题的关键条件是什么？"），未真正调用 AI 生成引导问题
B 模式: 依赖 ai_answer_b，如果 AI 未生成则为空，缺少 fallback
降级逻辑: 仅在输入长度 < 3 时降级，未考虑回答质量
session 存储: teacher_guidance_sessions 使用内存字典，重启丢失
2. AI 答案生成流程
ai_answer_a/b/c 的生成依赖手动触发 review 流程，未在解析完成后自动触发
缺少批量生成 AI 答案的能力
AI 生成失败时无重试/降级机制
3. 变式题推荐
wrongbook/services.py 仅按学科+难度简单匹配，未使用 AI 或知识点相似度
推荐质量较低
4. PDF 导出
export_pdf 端点已存在但功能简单，缺少完整的排版/水印/知识点标签
5. 知识图谱
knowledge-graph.vue 页面存在，但后端仅提供树形数据
缺少基于学生答题数据的个性化知识掌握度可视化
6. 照片录入题目 (photo-create)
拍照录入题目的流程存在，但缺少完整的 OCR + 解析闭环
与完整试卷解析流水线相比是简化版
7. 测试覆盖率
仅有 test_models_quick.py 和 test_integration.py 两个简单测试文件
缺少核心解析流水线的单元测试
8. Celery 任务监控
有超时检测 (periodic_stale_task_check)
缺少任务进度实时推送 (WebSocket/SSE)
缺少失败任务的告警机制