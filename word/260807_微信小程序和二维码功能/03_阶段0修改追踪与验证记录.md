# 阶段 0 修改追踪与验证记录

> 版本：v1.1 | 日期：2026-08-07 | 对应方案：`01_二维码功能需求文档.md` v1.2.3

## 1. 记录范围和结论

本记录对应阶段 0 的 B-01～B-04、T-01～T-26。目标是修复现有项目的依赖、构建、任务/学生端、试卷、知识点、AI 页面和完整任务流问题，使后续二维码功能可以在稳定基线上实施。

结论：阶段 0 已放行。后端静态检查通过，前端 H5 和微信小程序构建通过；阶段 0 放行时全量测试为 **899 passed、0 failed、0 skipped**，随后又修复题库历史 UUID 知识点兼容问题，当前全量测试为 **900 passed、0 failed、0 skipped**。本阶段没有创建二维码业务表、二维码接口、微信登录接口或生产迁移，也没有改变原有二维码需求和后续实施顺序。

## 2. 修改前后验证对比

| 项目 | 修改前/基线 | 修改后 |
|---|---|---|
| 前端依赖 | 存在 UniApp 依赖版本漂移风险 | `npm ci` 成功；核心 `@dcloudio/*` 版本一致：`3.0.0-alpha-5020120260710001` |
| 小程序脚本 | 缺少 `build:mp-weixin` | 已补充，脚本执行成功 |
| 后端 Django 检查 | 需要建立门禁 | `python manage.py check` 无问题 |
| 迁移漂移 | 需要确认 | `python manage.py makemigrations --check --dry-run`：`No changes detected` |
| 测试 | `873 passed、26 failed` | 阶段 0：`899 passed、0 failed、0 skipped`；题库回归修复后：`900 passed、0 failed、0 skipped` |
| H5 构建 | 需要复核 | `npm run build:h5` 成功 |
| 微信小程序构建 | 缺少可执行脚本/未放行 | `npm run build:mp-weixin` 成功，产物为 `uniapp/dist/build/mp-weixin` |
| 运行态 | 需要确认服务能启动 | Django `/` 返回 200；未登录任务 API 返回预期 401；H5 开发服务返回 200 且 Vite ready |

## 3. 完整修改文件清单

以下为本次阶段 0 实际修改或新增的全部代码、配置和测试文件。原工作区中已存在的 `.env`、媒体测试文件、`word/需求.md`、`.claude/`、`qisi_backend.tar.gz`、`start.bat` 等未因本次任务回滚或覆盖。

### 3.1 前端依赖、构建和跨端组件

| 文件 | 为什么修改 | 修改内容和结果 |
|---|---|---|
| `uniapp/package.json` | 文档要求必须可执行微信小程序构建门禁 | 增加 `build:mp-weixin`；H5/MP 构建均成功 |
| `uniapp/src/pages/student/layout.vue` | UniApp 编译/运行时对动态组件写法兼容性不足，可能导致页面切换异常 | 改为显式条件渲染学生页面组件；H5/MP 构建通过 |
| `uniapp/src/pages/teacher/layout.vue` | 同类动态组件兼容问题可能影响教师工作台切换 | 改为显式条件渲染教师页面组件；构建通过 |
| `uniapp/src/pages/teacher/question-edit.vue` | 原生 `select` 不适合微信小程序；AI 控件契约缺失 | 改用 UniApp `picker`，接入共享 `QuestionAIControls`，保留题目导航兼容方法；契约测试通过 |
| `uniapp/src/pages/teacher/mission-create.vue` | 原生分页 `select` 在 MP 端不可用 | 改为 `picker` 和统一索引处理；构建通过 |
| `uniapp/src/pages/teacher/course-list.vue` | 原生学科/年级 `select` 跨端不稳定 | 改为 `picker`，补齐计算属性和事件处理；构建通过 |
| `uniapp/src/pages/teacher/question-bank.vue` | 多处原生 `select` 会导致小程序页面交互失效 | 全部改为 `picker`，保留筛选、分页和知识点选择；构建通过 |
| `uniapp/src/pages/teacher/favorites.vue` | 收藏筛选控件存在同样的跨端问题 | 改为 `picker`；构建通过 |
| `uniapp/src/components/StudentSidebar.vue` | 学生侧边栏年级选择使用原生控件 | 改为 `picker`；构建通过 |
| `uniapp/src/components/QuestionAIControls.vue` | 手动 AI UI 测试无法稳定定位五个动作，且不能用自动触发替代手动操作 | 显式保留“全部、题干、A、B、C”五个按钮；仅按钮触发处理；T-23～T-25 通过 |

### 3.2 后端现有接口和页面兼容修复

| 文件 | 为什么修改 | 修改内容和结果 |
|---|---|---|
| `apps/missions/urls.py` | 现有测试和前端契约要求无尾斜杠，POST/PUT 不能依赖 301/302 | 保留原尾斜杠路由，同时增加无尾斜杠别名；T-01～T-08 通过 |
| `apps/missions/views.py` | 无班级任务发布时，明确目标学生不会生成进度，完整流程无法继续 | 支持 `target_student_ids` 查询学生并创建进度；不改变权限校验；任务流通过 |
| `apps/missions/serializers.py` | PostgreSQL JSONField 不能直接序列化 UUID 目标学生列表 | 写入 JSONField 前转换为字符串；完整任务流通过 |
| `apps/papers/urls.py` | 非 UUID 的不存在试卷参数在路由层直接失败，无法返回约定 JSON 404 | 试卷参数改为字符串交给视图校验；接口测试通过 |
| `apps/papers/views.py` | 匿名上传会把 `AnonymousUser` 写入外键；解析接口缺少无效 ID 处理；旧页面缺路由 | 修复匿名用户处理、UUID 校验和可追踪解析任务响应；补充试卷列表/详情兼容页面；T-09～T-15 通过 |
| `apps/study/student_urls.py` | 现有指导回复测试使用整数兼容 URL | 增加整数 session ID 兼容路由，不改变 UUID 主路由；T-16 通过 |
| `config/urls.py` | 旧试卷页面、知识点 HTMX、无尾斜杠任务/错题本路由未完整注册 | 注册根试卷页面、旧 `/api/papers/`、任务/错题本无尾斜杠别名及知识点路由；相关测试通过 |
| `apps/knowledge/models.py` | 生产已有 `knowledge_points` 外部表，模型原主键/managed 配置与实际结构不一致 | 改为 `managed=False`、`BigAutoField`，避免 Django 接管或删除该表；T-17～T-22 通过 |
| `apps/knowledge/migrations/0001_initial.py` | 历史迁移仍声明接管知识点表，导致迁移漂移检查不符合外部表策略 | 将历史选项同步为 `managed=False`；未生成、未执行新迁移 |
| `apps/knowledge/htmx_urls.py` | 既有知识点表使用整数主键，HTMX 路由仍使用 UUID 转换器 | 改为整数转换器；知识点创建/删除/表单测试通过 |
| `apps/knowledge/templates/knowledge/fragments/chapter_points.html` | HTMX 章节点接口缺少稳定的 `kp-table` 片段 | 新增最小可用表格片段；T-18、T-19 通过 |
| `apps/knowledge/templates/knowledge/fragments/point_form.html` | 新建知识点表单缺少稳定的 `kp-form` 片段 | 新增兼容表单片段；T-20 通过 |

### 3.3 测试和隔离环境修复

这些修改用于修正测试前置条件、请求协议或隔离环境，不是删除断言、跳过失败或降低验证标准。

| 文件 | 为什么修改 | 修改内容和结果 |
|---|---|---|
| `tests/conftest.py` | `knowledge_points` 是外部表，测试数据库默认不会创建，导致测试被跳过或无法真实验证 | 在 pytest 隔离数据库中按实际结构创建测试表；生产数据库不受影响；最终 0 skipped |
| `apps/common/ai/tests/conftest.py` | AI 测试子树也需要同一外部知识点表前置条件 | 增加测试专用表初始化；AI 测试通过 |
| `tests/integration/test_student.py` | 学生详情接口要求已有已发布任务进度，原测试数据缺少业务前置条件 | 补充真实 `StudentMissionProgress`；保留接口断言；通过 |
| `tests/integration/test_full_flows.py` | 完整流程缺少目标学生，且 JSON body 未按 API 协议提交 | 补充目标学生、使用 JSON 请求；流程通过 |
| `tests/test_integration.py` | 完整流程未创建真实试卷题目，且缺少 Django DB 标记/JSON 请求格式 | 创建真实试卷和题目，补充 `django_db`、目标学生和 JSON 请求；通过 |
| `tests/integration/test_questions.py` | 历史题目可能在 `knowledge_points` JSON 中保存 UUID，而当前知识点表主键为整数 | 新增 UUID 知识点列表回归测试；题库接口测试 `8 passed` |

## 4. T-01～T-26 关闭情况

| 项目范围 | 关闭结果 |
|---|---|
| T-01～T-08 | 任务、关卡、学生详情无尾斜杠契约、发布进度和权限前置条件修复；全部通过 |
| T-09～T-15 | 试卷 API 兼容别名、上传校验、解析任务、无效 ID 404、旧页面路由修复；全部通过 |
| T-16 | AI 回复未知 fenced 内容过滤和会话数据隔离保持正确；通过 |
| T-17～T-22 | 外部知识点表元数据、HTMX 路由、模板和树接口修复；全部通过，未跳过 |
| T-23～T-25 | 五个显式 AI 操作按钮和题目编辑页共享控件委托恢复；全部通过 |
| T-26 | 从任务创建、发布、学生进度、题目作答到结果读取的完整流程通过 |

### 4.1 阶段 0 放行后的题库兼容性回归修复

本地教师端题库管理页面访问 `/api/v1/questions/` 时发现，部分历史题目的 `knowledge_points` JSON 保存了 UUID 类型知识点 ID，而当前外部 `knowledge_points` 表的主键是 BIGINT。`QuestionListSerializer.get_knowledge_points_display()` 原先直接使用 UUID 查询整数主键，触发 `ValueError: Field 'id' expected a number` 并返回 HTTP 500。

修复文件：`apps/study/serializers.py`。现在仅使用可转换为整数的 ID 查询当前知识点表；UUID 类型 ID 不参与整数主键查询，但保留 JSON 中的 `module` 作为展示名称。新增 `tests/integration/test_questions.py` 回归测试，验证题目列表返回 200 且历史知识点仍可展示。

修复结果：题库接口测试 `8 passed`；全量测试 `900 passed, 0 failed, 0 skipped`；未修改数据库结构、未清理历史题目数据、未改变题目 UUID 主键。

## 5. 验证命令和证据

在项目根目录执行：

```bash
python manage.py check
# System check identified no issues (0 silenced).

python manage.py makemigrations --check --dry-run
# No changes detected

python -m pytest -q
# 900 passed in 14.57s
```

在 `uniapp` 目录执行：

```bash
npm ci
npm ls @dcloudio/uni-app @dcloudio/uni-cli-shared @dcloudio/vite-plugin-uni
npm run build:h5
npm run build:mp-weixin
```

结果：依赖安装成功；三个核心包版本一致为 `3.0.0-alpha-5020120260710001`；H5 和微信小程序构建均显示 `DONE Build complete`。

运行态临时验证：

- Django `http://127.0.0.1:8000/` 返回 HTTP 200。
- 未登录请求 `http://127.0.0.1:8000/api/v1/questions/` 返回 HTTP 401，这是鉴权保护的预期结果，不是服务错误；教师身份的题库列表回归测试返回 HTTP 200。
- UniApp H5 开发服务 `http://127.0.0.1:5273/` 返回 HTTP 200，Vite 日志显示 `ready`。
- 临时服务和子进程已停止，端口已释放。

## 6. 遗留事项和边界

以下不是阶段 0 阻断项，但必须在发布前处理或完成验收记录：

1. `npm ci` 后审计报告显示 66 个依赖漏洞（33 low、17 moderate、16 high）。本次未执行 `npm audit fix`，避免无评审升级破坏 UniApp 版本；需单独建立依赖升级分支和回归计划。
2. 微信小程序构建提示小程序端暂不支持 `img` 标签选择器，当前构建仍成功。需要在微信开发者工具和真机中确认富文本图片样式；必要时将样式改为 HTML class 或组件级兼容写法。
3. 当前环境未连接微信开发者工具、真实微信登录、真机扫码和生产域名，因此这些发布验收不能由本次本地验证替代。
4. 阶段 0 没有实现二维码功能本身；阶段 1 仍必须按需求新增二维码模型、接口、权限、幂等、过期、图片上传和微信绑定闭环，并重新执行专项测试。

## 7. 安全和回滚说明

- 未执行 `makemigrations` 生成新文件，未执行 `migrate`，未触碰生产数据库结构。
- `knowledge_points` 仅按现有外部表结构修正 Django 模型和历史迁移元数据；测试表只在 pytest 隔离数据库创建。
- 任务、错题本等无尾斜杠路由以新增兼容别名为主，原有尾斜杠路由保留。
- 若需回退，可按本记录逐文件回退本次阶段 0 修改；不要回滚用户原有工作区改动，也不要删除测试生成的媒体文件而影响其他工作。

## 8. 前端跨端样式回归修复（2026-08-07）

### 8.1 发现的问题

题库管理页面中，科目 picker 由于 `width: 100%` 与内边距/边框叠加，在 H5 页面出现右侧溢出；picker 展示文字没有统一水平居中；题型、难度、知识点、标签和 UUID 筛选项使用固定宽度，窗口变窄时会挤压或溢出。检查同一批前端改动后，发现任务创建、我的精选、课程管理、题目编辑和学生侧栏也存在同类风险。

### 8.2 涉及文件和修复内容

| 文件 | 修复内容 |
|---|---|
| `uniapp/src/pages/teacher/question-bank.vue` | 科目、题型、难度、知识点和分页 picker 使用 `box-sizing:border-box`、flex 居中、文本省略；筛选栏允许换行；UUID 搜索框改为可伸缩宽度；增加 1100px/768px 窄屏布局，知识树与题目列表改为上下排列 |
| `uniapp/src/pages/teacher/mission-create.vue` | 科目、关卡类型、模式、分页 picker 居中；筛选区、输入框、下拉框增加最大宽度和盒模型约束；筛选按钮允许换行；左右面板和窄屏布局增加 `min-width:0` |
| `uniapp/src/pages/teacher/favorites.vue` | 知识树和主体面板增加盒模型/最小宽度约束；科目 picker 文字居中；搜索框防止撑宽；窄屏不再额外叠加左边距 |
| `uniapp/src/pages/teacher/course-list.vue` | 科目/年级 picker 居中；弹窗限制最大宽度；课程卡片在 900px/600px 下由三列降为两列/一列；表单小屏改为单列 |
| `uniapp/src/pages/teacher/question-edit.vue` | 题型和难度 picker 使用统一居中、溢出省略样式 |
| `uniapp/src/components/StudentSidebar.vue` | 学生年级 picker 文字居中并限制盒模型宽度 |
| `uniapp/src/pages/teacher/layout.vue` | 教师端布局增加最小宽度和盒模型约束，窄屏收紧内容边距 |
| `uniapp/src/pages/student/layout.vue` | 学生端布局增加最小宽度和盒模型约束，窄屏收紧内容边距 |

本次截图反馈后的二次精调：`favorites.vue` 将搜索框固定为 32px 高度并补充上下边界；`mission-create.vue` 将知识点输入框扩大为 320rpx、知识点下拉层扩大为 360rpx，并将题号搜索与其他筛选输入统一为 56rpx 高度、`min-height:56rpx` 和 `border-box`，避免继承通用输入框的 88rpx 最小高度。

### 8.3 验证结果

- `npm run build:h5`：成功，`DONE Build complete`。
- `npm run build:mp-weixin`：成功，产物目录为 `uniapp/dist/build/mp-weixin`。
- `git diff --check`：通过。
- 未改变页面路由、接口调用、角色权限或二维码业务逻辑。
- 微信小程序仍有既有 `img` 标签选择器非阻断提示，需在微信开发者工具/真机继续确认富文本图片样式。
## 9. 作业列表科目与时间显示优化（2026-08-07）

本次变更只完善教师端现有作业列表，不新增数据库字段、不生成迁移。任务科目优先来自关联课程；没有课程的历史任务兼容从关联题目读取。列表新增科目筛选，中文名称和现有英文/单字母编码均可筛选；时间统一显示为 `YYYY-MM-DD HH:mm`，不再显示秒和 `+08:00` 后缀。

修改文件：

- `apps/missions/serializers.py`：增加科目归一化、列表 `subject` 字段和历史题目兼容读取。
- `apps/missions/views.py`：增加 `subject` 查询参数，保留教师隔离和 `class_id` 条件。
- `uniapp/src/api/missions.ts`：补充列表返回字段和筛选参数类型。
- `uniapp/src/pages/teacher/mission-list.vue`：增加科目选择/重置、科目展示和时间格式化。
- `tests/integration/test_missions.py`：增加科目展示与筛选回归测试。

验证结果：`manage.py check` 通过；任务测试 `12 passed`；全量测试 `901 passed, 0 failed, 0 skipped`；`npm run build:h5` 和 `npm run build:mp-weixin` 均成功。小程序构建仅保留项目原有 `img` 选择器非阻断提示。

## 10. 教师端科目选项与课程弹窗优化（2026-08-07）

本次按题库管理页/精选页现有范围统一教师端科目，仅保留“物理、数学”：

- `mission-list.vue`：作业列表筛选从 9 项收敛为“全部科目、数学、物理”。
- `mission-create.vue`：作业创建页科目从 4 项收敛为物理、数学。
- `question-bank.vue`：题库科目显示统一为“物理、数学”，保留现有 `physics`/`math` 请求编码。
- `course-list.vue`：课程创建科目从 9 项收敛为数学、物理；科目选择改为弹窗内自定义下拉层，设置独立层级并允许超出内容区域展示，避免被创建课程弹窗遮挡；提交值仍是中文科目值。

本次只修改前端选项和弹层显示，不修改接口和数据库结构。`npm run build:h5` 和 `npm run build:mp-weixin` 均显示 `DONE Build complete`；保留项目原有的小程序 `img` 选择器非阻断提示。

## 11. 新建课程弹窗宽度溢出修复（2026-08-07）

修改 `uniapp/src/pages/teacher/course-list.vue`：弹窗设置明确的宽度/最大宽度和 `border-box`；弹窗直接子元素、表单组、表单输入框、文本域、双列表单和科目下拉层增加收缩与最大宽度约束；输入框改为 `display:block` 并纳入边框/内边距计算；窄屏保持单列布局。该修复只解决内容溢出，不改变课程接口、课程科目值或提交逻辑。

验证结果：`npm run build:h5` 和 `npm run build:mp-weixin` 均成功；`git diff --check` 通过。

## 12. 原生 picker 弹层遮挡统一修复（2026-08-07）

修复 `uniapp/src/pages/teacher/course-list.vue`：课程创建页年级选择不再使用会脱离弹窗层级的原生 selector，改为弹窗内自定义下拉；科目和年级均可在弹窗内正常展开、选择和收起。扫描教师端和共享组件后，在 `uniapp/src/App.vue` 增加全局 `.uni-picker-container`/`.uni-picker-custom` 层级兜底，覆盖课程练习、学生侧栏及其他自定义弹窗中的原生 picker，避免同类遮挡。

验证结果：`npm run build:h5` 和 `npm run build:mp-weixin` 均显示 `DONE Build complete`；小程序仍只有项目原有 `img` 选择器非阻断提示。
