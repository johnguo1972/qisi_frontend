# 本地与远程 main 最新代码差异对比报告

## 1. 报告目的

本报告用于记录 2026-08-30 对本地开发分支与远程 `main` 分支进行的只读差异检查结果，为后续代码同步、冲突处理和迁移整理提供依据。

本次检查未执行以下操作：

- 未执行 `git fetch`、`git pull` 或其他拉取操作；
- 未执行分支合并；
- 未修改代码、迁移、文档或数据库；
- 未提交或推送任何内容。

## 2. 对比基准

| 项目 | 值 |
|---|---|
| 当前本地分支 | `integration/remote-main-local-merge-20260826` |
| 当前本地提交 | `aee0a959867e96d35420af5b68f1cbb9259168f7` |
| 远程实时 `main` | `f62578a2c399d7df1b9b36034e9dbe85aca987cc` |
| 当前开发分支相对远程 | 远程领先 23 个提交，本地领先 0 个提交 |
| 本地 `main` 提交 | `b202fbb39f1c60909ef1c1aa2b8d1078bb2c06c7` |
| 本地 `main` 相对远程 | 远程领先 60 个提交，本地领先 0 个提交 |
| 本地工作区未提交路径 | 102 个 |
| 本地缓存 `origin/main` | `aee0a959867e96d35420af5b68f1cbb9259168f7` |

说明：本地缓存的 `origin/main` 仍是旧版本，不能代表远程实时 `main`。本报告中的远程最新提交通过只读方式查询远程引用确认。

从当前开发分支 `aee0a959` 到远程 `main` `f62578a2` 的净差异包含 60 个文件，其中与本地未提交路径存在同路径重叠的文件共 9 个；此外还存在 3 组数据库迁移编号冲突。

## 3. 远程新增提交明细

| 序号 | 提交 | 功能说明 | 数据库迁移 | 与本地实际差异 |
|---:|---|---|---|---|
| 1 | `68918a12` | 优化微信登录扫码等待状态 | 否 | 无同路径冲突 |
| 2 | `3c16945b` | 增加课程机构归属、课程协作、多角色和学科编码统一 | 是，`apps/courses/migrations/0003_course_scope_collaboration.py` | 该提交中的部分文件已在当前本地基线中存在，未形成新的最终净差异冲突 |
| 3 | `d8a2e382` | 调整微信设备登录测试契约 | 否 | 无同路径冲突 |
| 4 | `b6801ad2` | 增加受控知识目录和 AI 题目知识点探查流程 | 是，`knowledge 0002`、`parser 0008` | `knowledge/models.py`、两个管理命令初始化文件 |
| 5 | `2c0346df` | 增加 Word 课程资料异步转 PDF 和转换状态管理 | 是，`courses 0004` | `courses/models.py`、`serializers.py`、`views.py` |
| 6 | `550a9187` | 合并 Word 资料转换状态功能 | 是，重复包含 `courses 0004` | 与第 5 项相同 |
| 7 | `c74b63ec` | 定义题目关联关系流程文档 | 否 | 无同路径冲突 |
| 8 | `031a8d06` | 完善题目关联关系设计和实施文档 | 否 | 无同路径冲突 |
| 9 | `455edb4f` | 增加题目关联模型和候选题服务 | 是，`study 0005` | `study/models.py` |
| 10 | `bfdf2522` | 增加题目关联唯一性和规范化约束 | 修改 `study 0005` | `study/models.py` |
| 11 | `d5f3b299` | 增加题目关联后端 API | 否 | `study/urls.py` |
| 12 | `b3124517` | 在题库页面增加题目关联管理功能 | 否 | `uniapp/src/pages/teacher/question-bank.vue` |
| 13 | `d0102b87` | 增加题目关联分页和请求保护 | 否 | `uniapp/src/pages/teacher/question-bank.vue` |
| 14 | `a70c22b8` | 修复分页缩小后的关联题列表恢复 | 否 | 无同路径冲突 |
| 15 | `718e4d1a` | 规范题目关联迁移文件换行格式 | 仅修改迁移文件格式，数据库结构无新增 | 无同路径冲突 |
| 16 | `72bd1c6e` | 记录题目关联功能验证结果 | 否 | 无同路径冲突 |
| 17 | `4bd65942` | 强化题目关联业务约束 | 否 | 无同路径冲突 |
| 18 | `b86c5069` | 保留题目关联操作结果和提示 | 否 | `uniapp/src/pages/teacher/question-bank.vue` |
| 19 | `39a264e2` | 批量展示关联题知识点 | 否 | 无同路径冲突 |
| 20 | `60ab281a` | 忽略过期关联题分页请求错误 | 否 | 无同路径冲突 |
| 21 | `4cffae0e` | 限制关联题加载只处理最新请求 | 否 | 无同路径冲突 |
| 22 | `4d1b30ef` | 跳过已废弃的试卷 API 测试 | 否 | 无同路径冲突 |
| 23 | `f62578a2` | 支持题库多关键字搜索 | 否 | `uniapp/src/pages/teacher/question-bank.vue` |

## 4. 实际同路径重叠文件

以下文件是远程最新净差异与本地未提交工作区的交集。由于尚未执行合并，以下内容属于“潜在冲突/语义冲突”，不能直接认定为 Git 已产生的冲突标记。

### 4.1 `apps/courses/models.py`

- 远程功能：在 `CourseMaterial` 中增加 Word 转换状态、转换后 PDF 路径、转换错误、转换开始时间和完成时间。
- 本地功能：增加 `CourseClass`、`CourseHandout`，支持课程关联班级和讲义。
- 冲突类型：同一文件、不同功能。
- 处理原则：两部分都保留，不能用远程文件整体覆盖本地文件。

### 4.2 `apps/courses/serializers.py`

- 远程功能：增加课程资料转换字段、学科编码规范化校验。
- 本地功能：调整课程班级数量统计，统计有效的课程班级关系。
- 冲突类型：不同功能，但共享序列化器和校验逻辑。
- 处理原则：人工合并并检查字段、校验方法和 `class_count` 统计逻辑。

### 4.3 `apps/courses/views.py`

- 远程功能：Word 文件异步转换、预览状态判断、转换失败处理。
- 本地功能：课程-班级、课程-讲义关联，以及基于讲义生成任务。
- 冲突类型：不同功能，共享导入、模型和任务创建逻辑。
- 处理原则：不能直接覆盖，需要人工合并，并检查接口、导入和事务逻辑。

### 4.4 `apps/knowledge/models.py`

- 远程功能：新增 `KnowledgeTopic`、`KnowledgeTopicModule`，用于受控知识目录。
- 本地功能：新增 `QuestionKnowledgeMatch`，用于题目和知识点匹配。
- 冲突类型：同一文件、不同功能。
- 处理原则：两个模型都保留，合并导入、模型定义及对应迁移依赖。

### 4.5 `apps/study/models.py`

- 远程功能：新增 `QuestionRelation` 题目关联模型。
- 本地功能：为 `AnswerAttempt` 增加 `idempotency_key` 答题幂等字段。
- 冲突类型：同一文件、不同功能。
- 处理原则：两个功能都保留，不能直接使用远程版本覆盖本地版本。

### 4.6 `apps/study/urls.py`

- 远程功能：增加题目关联、候选题 API 路由。
- 本地功能：增加知识点匹配、重建匹配 API 路由。
- 冲突类型：同一文件、不同功能。
- 处理原则：两组路由都保留，检查导入、路径顺序和路由名称重复。

### 4.7 `uniapp/src/pages/teacher/question-bank.vue`

该文件包含两类差异：

- 远程功能：题目关联弹窗、候选题分页、解除关联、多关键字搜索。
- 本地功能：讲义管理、知识点待确认、生成讲义等入口。
- 冲突类型：同一页面、不同功能，双方功能都应保留。

其中搜索参数处理属于同一功能的差异：

- 远程版本保留 `uuid` 参数并增加 `keyword` 参数；
- 本地未提交版本改为只提交 `keyword` 参数。

按照“同一功能以远程为准”的规则，搜索参数逻辑应优先采用远程实现；本地新增的讲义和知识点入口仍需保留。

### 4.8 `apps/knowledge/management/__init__.py`

### 4.9 `apps/knowledge/management/commands/__init__.py`

这两个文件均属于管理命令包初始化文件，没有业务逻辑。本地文件是未跟踪文件，远程也是同类初始化文件。

- 冲突类型：同一基础功能。
- 处理原则：以远程文件为准，避免未跟踪文件阻止合并。

## 5. 数据库迁移冲突

### 5.1 `courses` 迁移编号冲突

远程新增：

```text
apps/courses/migrations/0004_coursematerial_conversion_status.py
```

本地未提交：

```text
apps/courses/migrations/0004_courseclass_coursehandout.py
apps/courses/migrations/0005_coursehandout_handout_courseclass_uq_course_class_and_more.py
```

两者的 `0004` 编号相同，但功能不同：

- 远程：Word 资料转换状态字段；
- 本地：课程班级和课程讲义关系。

两部分迁移都必须保留。后续整合时需要顺延本地迁移编号，并同步修改后续迁移依赖。

### 5.2 `knowledge` 迁移编号冲突

远程新增：

```text
apps/knowledge/migrations/0002_controlled_probe_taxonomy.py
```

本地未提交：

```text
apps/knowledge/migrations/0002_questionknowledgematch.py
apps/knowledge/migrations/0003_knowledge_point_id_bigint.py
```

两者的 `0002` 编号相同，但功能不同：

- 远程：受控知识目录和知识目录模块；
- 本地：题目知识点匹配和知识点外部表 ID 类型修正。

两组迁移都必须保留，需要重新排列本地迁移编号并更新依赖关系。

### 5.3 `study` 迁移编号冲突

远程新增：

```text
apps/study/migrations/0005_questionrelation.py
```

本地未提交：

```text
apps/study/migrations/0005_answerattempt_idempotency_key.py
```

两者的 `0005` 编号相同，但功能不同：

- 远程：题目关联模型及关联约束；
- 本地：答题记录幂等键。

两组迁移都必须保留，通常需要将本地迁移顺延到后续编号，并将其依赖改为远程 `0005`。

### 5.4 `parser` 迁移

远程新增：

```text
apps/parser/migrations/0008_controlled_probe_taxonomy.py
```

该迁移增加题目 `difficulty_level` 字段。当前未发现本地未提交文件与其同路径冲突，但远程 `study 0005` 依赖该迁移，整合时需要确保 parser 迁移先于 study 迁移执行。

## 6. 合并判断

当前不能直接执行 `git pull` 或直接覆盖合并，原因如下：

1. 存在 9 个同路径重叠文件；
2. 存在 3 组数据库迁移编号冲突；
3. `question-bank.vue` 同时存在同功能搜索逻辑差异和不同功能页面改动；
4. 多个 Django 模型、视图和路由文件需要保留双方功能；
5. 本地还有 102 个未提交路径，包括未跟踪代码、文档和数据库备份文件。

合并时应遵循以下规则：

- 同一文件、同一功能：远程版本优先；
- 同一文件、不同功能：双方功能都保留，人工合并；
- 迁移编号相同但功能不同：两个迁移都保留，重新编号并修正依赖；
- 只在本地完成整合和验证，不通过本次流程修改远程仓库；
- 在代码、迁移和运行验证完成前，不进行提交和推送。

## 7. 最终结论

远程最新 `main` 可以作为后续整合基准，但当前不具备直接合并条件。

本次检查确认：

- 远程 `main` 最新提交为 `f62578a2`；
- 当前本地开发分支落后 23 个提交；
- 本地 `main` 分支落后 60 个提交；
- 远程新增内容主要包括课程协作、Word 异步转换、受控知识目录、题目关联和题库多关键字搜索；
- 存在明确的代码文件重叠和迁移编号冲突；
- 未发现可以无条件直接覆盖的完整合并方案；
- 本次仅完成差异检查和文档记录，未修改代码、未拉取代码、未合并、未提交、未推送。
